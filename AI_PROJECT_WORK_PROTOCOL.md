# AI 协作项目轻量工作协议

> 版本：2.0（2026-08-23）  
> 适用范围：科研学习、代码实现、机器学习实验及其他需要多轮 AI 协作的任务。  
> 目标：先把方向和标准弄清楚，再快速执行；保留必要审查，但控制文档、推理和 token 成本。

## 一、四条总原则

1. **任何新项目或新任务，开始前必须先进行 Grill-me。**
2. **先明确目标、输入边界和验收标准，再写主体代码或运行正式实验。**
3. **思考与记录的深度应与风险相匹配，不为了流程完整而制造文档。**
4. **代码跑通不等于任务完成，最终结论必须由真实任务指标支持。**

---

## 二、Grill-me：每个任务的必经入口

Grill-me 的作用不是把用户“考倒”，而是在执行前发现双方都没有意识到的空白。它尤其适合用户正在学习、对领域和任务标准还不熟悉的情况。

AI 在 Grill-me 阶段必须：

- 先阅读用户提供的任务原文、代码、数据说明和已有记录；
- 能自己查到的问题自行检查，不反过来让用户解释源码；
- 一次只问一个真正会改变方案的问题；
- 每个问题同时给出通俗解释、可选答案、影响和推荐；
- 主动追问用户可能不知道应该询问的领域问题；
- 在目标、信息边界和成功标准明确前，不直接进入主体实现。

一份公开 Grill-me 实现也将其定位为“interviewer, not executor”，并强调先做功课再提问。[Grill-me 公开实现](https://github.com/stevegsax/grill-me/blob/main/SKILL.md)

### 深度控制

Grill-me 必须进行，但不必每次都很长：

- **全新领域或完整项目**：深入询问，直到核心问题闭合；
- **已有项目中的新模块或新方法**：只询问该模块的目标、接口、影响和验证方式；
- **明确的局部修改**：在内部快速检查“改什么、不能改什么、怎样验证”，没有歧义就直接执行；
- **用户已经明确回答过的问题**：读取记录，不重复询问。

### 精简提示模板

```text
现在先进行 Grill-me，不要立即实现。
请先阅读任务材料和现有代码，一次只问我一个最关键的问题。
每个问题说明为什么重要、不同答案如何影响方案，并给出你的推荐。
能从材料中查到的内容自行查证，不要问我。
重点澄清目标、测试输入、禁止信息、成功标准、数据划分、基线、资源和交付物。
核心问题闭合后，用简短项目契约总结，等我确认再开始主体工作。
```

### 退出 Grill-me 的最低条件

至少能够明确回答：

1. 最终到底要解决什么问题；
2. 测试时会提供什么输入，哪些信息不能使用；
3. 怎样才算成功，分母和测试对象是什么；
4. 用什么基线比较；
5. 时间、算力和最终交付物是什么；
6. 还有哪些内容是事实、我们的假设或需要外部确认的问题。

---

## 三、双向钢人：吸取思想，不无限辩论

双向钢人的核心价值是：不要让 AI 一味顺着当前方案，而要同时给出支持和反对它的最强理由。

它不是每轮都要执行，也不允许在同一个问题上反复空谈。默认只进行一轮，输出以下四项：

1. 当前方案成立的最强理由；
2. 当前方案可能失败的最强理由；
3. 真正决定结果的关键变量；
4. 成本最低、区分力最高的验证办法，以及当前判断。

只有出现新证据、关键假设变化或准备改变主体方向时，才重新进行。若一轮分析没有产生新的变量、实验或决策，立即停止，不继续消耗 token。

### 强制使用节点

- 选择或更换主体方法；
- 修改成功判据、测试输入或数据划分；
- 开始高成本正式实验；
- 结果明显异常；
- 连续调参没有提高最终指标；
- 准备声称方法优于基线或具有泛化性。

### 精简提示模板

```text
对当前决策做一轮简洁的双向钢人审查：
分别给出支持和反对当前方案的最强理由，指出决定结论的关键变量，
设计一个最低成本的验证，并给出明确判断。
不要扩展无关分支；没有新证据时不要在同一点继续辩论。
```

双向钢人是一种实用思考模板，而不是能代替实验的科学标准。[方法来源及基本结构](https://www.jxxy.net/ai/articles/deep-thinking-prompt-essence/)

---

## 四、G0–G8 主流程

### G0：任务标准审计

先保存和阅读任务原文，区分：

- 官方明确要求；
- 官方没有说明的内容；
- 我们自己增加的目标；
- 必须向发布者确认的歧义。

不得把自己的理解写成官方标准。

### G1：领域和基线速成

在陌生领域开始实现前，先弄懂：

- 任务的基本概念；
- 主流方法大致有哪些；
- 怎样评价好坏；
- 官方基线的真实输入、输出和评测方式；
- 数据字段的实际含义；
- 当前结果大概应该达到什么水平。

优先阅读源码、论文和官方文档。目标不是写完整综述，而是避免在错误认知上开工。

### G2：完成 Grill-me

执行第二节，关闭会影响主体方案的问题。尚未确认的问题要明确标出，不能默认为已解决。

### G3：确定方案并做一次反向审查

对主体方案进行一轮双向钢人和简短失败预演：

- 它为什么可能有效；
- 它最可能怎样失败；
- 最便宜的验证实验是什么；
- 如果失败，备选方向是什么。

### G4：冻结简短项目契约

用户确认以下内容后再进入正式实现：

```text
目标：
最终输入与输出：
测试时禁止使用的信息：
成功指标与统计分母：
训练/开发/测试划分：
基线、目标值、最低可接受值：
主体方法：
非目标：
资源与期限：
需要用户确认的高成本节点：
仍未确认的问题：
```

如果核心口径后来改变，只需在同一文档追加一条变更记录，不新建一份文档。

### G5：最小真实闭环

先用少量代表性样本跑通：

```text
真实输入 → 预处理 → 主方法 → 真实执行环境 → 最终指标 → 可视化检查
```

必须确认：

- 没有使用测试时拿不到的信息；
- 指标与人工观察一致；
- 成功和失败案例都检查过；
- 与基线采用同一协议；
- 数据和标签质量足以支撑后续工作。

只有最小闭环方向正确，才扩大实验。

### G6：小规模迭代

每轮实验只需简短记录：

```text
假设：为什么这个改动可能提高最终指标？
改动：本轮主要改变什么？
对照：与哪个版本比较？
通过条件：达到什么结果才保留？
结果：最终指标、异常和失败案例是什么？
决定：保留 / 放弃 / 再验证。
```

循环为：

```text
提出假设 → 最小修改 → 自动测试 → 小规模真实评测 → 看失败案例 → 决定
```

连续两轮没有改善最终指标，应重新检查表示、数据和方法假设，不继续机械调参。

### G7：正式评测前冻结

开始大规模或最终测试前，冻结：

- 代码和配置；
- 模型 checkpoint；
- 数据 manifest 和 split；
- 指标与评测脚本；
- 最终候选及其选择理由。

看过测试结果以后选择的新方案必须标记为事后结果，不能伪装成预先选定。

### G8：独立审查和交付

最后只做四项必要审查：

1. **任务审查**：是否真的满足原始要求；
2. **数据流审查**：是否使用了禁止信息或发生数据泄漏；
3. **结果审查**：报告数字能否从原始结果重新计算；
4. **复现审查**：从说明和命令能否重新运行。

评测应同时覆盖“是否完成、是否破坏其他要求、完成质量”，不能只依赖一个容易被钻空子的数字。[Anthropic Agent Evals](https://www.anthropic.com/engineering/demystifying-evals-for-ai-agents)

---

## 五、最小文档原则

默认一个项目只维护一个核心文档，例如 `PROJECT.md`，包含：

```text
1. 任务原文与标准审计
2. Grill-me 后的项目契约
3. 当前主体方法和数据流
4. G0–G8 当前状态
5. 关键决策与变更
6. 实验记录
7. 当前问题、下一步和复现命令
```

只有在内容确实过长或有独立读者时，才拆出：

- `README.md`：使用入口；
- `REPRODUCE.md`：复现步骤；
- `REPORT.md`：对外交付；
- 模块说明：复杂代码确有学习需要时再写。

不要为每个流程节点建立一份文档，不重复记录相同内容。短入口应当指向仓库中的真实来源，而不是变成不断膨胀、很快过期的百科全书；这也符合 OpenAI 关于仓库知识组织和渐进式披露的实践。[OpenAI Harness Engineering](https://openai.com/index/harness-engineering/)

---

## 六、必须保留的几条工程约束

### 信息和结论

- 区分来源事实、代码事实、推断和建议；
- 能从源码、配置和日志查到的内容，AI 自己查；
- 测试输入、成功判据、数据划分或主体方法改变时，必须通知用户；
- loss 下降不能冒充真实任务成功率提高；
- “泛化”必须说明是对新轨迹、新物体、同类新实例还是新类别泛化；
- 异常高的结果优先检查数据泄漏、统计分母和评测口径。

### 数据和实验

- 训练前抽查数据和标签质量；
- 基线和新方法必须采用同一评测协议；
- 先小测试，再正式实验；
- 最终测试集不能被反复用于调参；
- 失败结果和失败案例同样保留。

### 执行成本

- 预计超过 3 分钟的命令默认交给用户在终端运行；
- 独立命令尽量一次性并行给出；
- 命令前说明预计耗时、输出位置和完成标志；
- 不高频查询长任务；
- 不为不现实的边缘情况堆砌大量防御代码；
- 工作说明应清楚但避免重复和无关展开。

### 必须暂停并确认的节点

- 项目契约需要冻结；
- 准备修改核心任务口径；
- 准备开始高成本正式实验；
- 当前实现与用户理解的主体方法不一致；
- 测试时准备使用新的信息来源；
- 准备删除重要资产或形成对外提交。

---

## 七、每轮工作的精简汇报格式

```text
本轮目标：
完成内容：
验证证据：
结论：支持或不支持哪个假设？
尚存问题：
下一步：为什么值得做？
```

不得只汇报“代码写完了”或“训练跑完了”。如果本轮只是常规实现且结果清楚，可以合并为一段话，不必机械填写表格。

---

## 八、项目启动检查清单

```text
[ ] 已进行 Grill-me
[ ] 已保存和核对任务原文
[ ] 已区分官方要求、自设目标和未知项
[ ] 已理解基本方法、指标和基线
[ ] 已明确测试输入和禁止信息
[ ] 已明确成功标准、分母和数据划分
[ ] 已对主体方案做一次简短反向审查
[ ] 已由用户确认项目契约
[ ] 已通过少量样本跑通真实闭环
[ ] 已人工检查成功与失败案例
[ ] 已确认数据/演示质量
[ ] 大规模实验前已冻结候选和评测协议
[ ] 最终结果已通过任务、数据流、结果和复现审查
```

---

## 九、适合放入项目入口的最短版本

```text
任何新项目或任务开始前，必须先进行深度与风险相匹配的 Grill-me：先读材料，
一次只解决一个关键问题，明确目标、测试输入、禁止信息、成功标准、数据划分和基线。
全新或高风险项目遵循 G0–G8：任务审计、领域速成、Grill-me、方案反审、项目契约、
最小真实闭环、小规模迭代、正式冻结、独立审查。双向钢人只在关键决策点做一轮；
若没有产生新变量或验证实验就立即停止。默认把契约、决策、实验和工作记录合并在一个
PROJECT.md 中。先小规模验证方向，再进行高成本实验；任何结论都必须能追溯到真实证据。
```

这套协议追求的不是形式上的严密，而是用尽量少的额外成本，尽早发现最昂贵的方向错误。

# 灵巧手重定向项目全景文档（完整版 · 答辩级细节）

> 本文是项目的**唯一完整文档**，合并了原 METHODS_EXPLAINED.md 的全部内容，
> 并把每个方法的实现细节写到"答辩被问能答上来"的程度：公式、参数、维度、
> 为什么这么设计、失败时怎么诊断的、消融数字是多少。
> 结构：第一部分基础重定向任务 → 第二部分进阶任务 → 第三部分答辩问答预备 → 附录。

> **最终冻结说明（2026-09-01）**：基础任务现已冻结为“运动学初值 → Global CEM →
> Rank-5 关节协同 CEM → 两次独立确认 → 正式 1000 条复验”。正式参考成功率为
> Linker 43.9%、XHand 69.5%、Wuji 71.8%；通过更严格稳定运输门、可进入进阶训练的比例为
> 35.0%、63.0%、66.2%。冻结清单及全部输入/输出哈希见
> `retargeting/configs/final_retargeting_release_v1.json`。本文后半部分保留的一些 SLSQP、30 cm
> 和旧策略数字是研究过程记录，凡与本说明冲突，均以本说明和冻结清单为准。

---

# 阅读前先建立整体认识（零基础入口）

## 0.1 这个项目最后做成了什么

先用一句不带术语的话概括：

> 数据集里原本只有 Shadow Hand 的抓取动作。我们先把这些动作“翻译”成三种不同机器手能执行的动作，并在物理仿真中筛出真正抓得住的轨迹；然后再让神经网络学习在执行过程中根据物体状态修正手指动作。

项目现在已经完成两个阶段：

1. **基础重定向已完成并冻结**：三只目标手均完成正式 1000 条轨迹的生成、独立复验和质量审计。
2. **进阶策略重新开始**：训练集只使用通过稳定运输门的重定向轨迹；测试时只给初始任务状态，禁止读取未来专家动作或检索参考轨迹。第一轮比较逐步策略与动作块策略。

当前基础任务已经不是“还在选择方法”的阶段；它已经冻结。现在进入**进阶自主策略训练**，同时整理基础任务报告、视频、答辩材料和代码解释。最终结果速查见附录 A，实验原始过程以 `WORK_LOG.md` 为准。

## 0.2 先区分四种容易混在一起的东西

这是阅读整个项目最重要的表格：

| 名称 | 它是什么 | 是否已经通过物理验证 | 是否由神经网络产生 |
|---|---|---:|---:|
| Shadow 源轨迹 | 数据集提供的“原手怎么抓” | 数据集认为成功 | 否 |
| 目标手候选轨迹 | 重定向算法翻译出的“新手应该怎么动” | 否 | 基础方法中否 |
| 目标手专家轨迹 | 候选在 Isaac Gym 中稳定抓起并通过质量门 | 是 | 否 |
| 闭环策略轨迹 | 神经网络根据当前状态逐步发出的动作 | 需要重新评测 | 是 |

因此，“重定向完成”不等于“策略训练完成”，而“几何优化成功”也不等于“物理抓取成功”。本项目的完整链路是：

```text
Shadow源轨迹
    ↓ 运动学重定向
目标手候选轨迹
    ↓ Isaac Gym物理重放
通过稳定与运输门的专家轨迹
    ↓ 监督学习或强化学习
目标手闭环策略
    ↓ 再次在Isaac Gym评测
最终策略成功率
```

## 0.3 什么叫“灵巧手”“自由度”和“动作”

**灵巧手**可以理解为安装了多个可控关节的机器人手。它与普通二指夹爪的区别是：夹爪通常只控制“张开/闭合”，灵巧手可以分别改变拇指、食指、中指等关节，从而形成包络抓、捏取、侧抓等不同手型。

**自由度（DOF）**就是能够独立控制的运动量。例如，一个只能弯曲的手指有 1 个自由度；既能弯曲又能左右摆动，则至少有 2 个。自由度越多，通常越容易模仿复杂手型，但控制和优化也更复杂。

本项目把一帧动作分为两部分：

```text
[手腕6维, 手指关节若干维]
```

手腕 6 维是三维平移 `(x,y,z)` 加三维旋转 `(roll,pitch,yaw)`。手指维度取决于具体机器手，所以 Shadow、Linker、XHand、Wuji 的一帧动作分别是 28、12、18、26 维。

这里的动作不是 torque，也不是速度，而是**位置控制目标**：程序告诉 Isaac Gym“下一时刻希望每个关节到达这个角度”，仿真器内部的 PD 控制器再产生力，把关节拉向目标。

## 0.4 什么叫“重定向”

重定向不是简单复制关节角。原因是两只手的骨骼长度、关节数量、旋转方向和机械耦合都不同：Shadow 食指有多个独立关节，但 Linker 一根手指可能只有一个电机并通过机械联动带动两个关节。直接复制数字没有物理含义。

更准确地说，重定向是在回答：

> 已知 Shadow 在这一帧的掌心、指尖和指节位于哪里，目标手的手腕和关节应该取什么值，才能尽量形成相同的抓取功能？

对第 `t` 帧，输入是 Shadow 动作 `s_t`，未知量是目标手动作 `q_t`。程序用目标手 URDF 做正向运动学，计算 `q_t` 对应的掌心和指尖位置，然后不断调整 `q_t`，直到它们尽量接近 Shadow 的目标几何关系。求出的 `q_t` 就是这一帧的重定向结果。

这个过程与训练 MLP 不同：

- 没有跨轨迹共享、通过训练积累的网络权重；
- 每一帧都现场求一个带关节边界的最优化问题；
- 上一帧解只作为下一帧的初始猜测，使求解更快、更连续；
- 下次处理新轨迹时复用的是同一套目标函数和超参数，不是上一条轨迹的动作答案。

## 0.5 一条轨迹在时间上到底有多长

GraspM3 一条源轨迹有 70 帧，频率为 20 Hz，因此数据本身持续：

```text
70 ÷ 20 = 3.5 秒
```

Isaac Gym 物理仿真以 60 Hz 运行，所以一个源数据帧会在线性插值后执行 3 个物理步：

```text
70帧 × 3物理步 = 210个运动物理步
```

末尾再保持最后一个动作 30 个物理步，即 0.5 秒，用来检查物体会不会滑落。因此真正计入成功曲线的是 240 步、4.0 秒。开始前还有 30 个物体落稳步，即 0.5 秒，但这部分不属于抓取动作，也通常不录入视频。

```text
落稳0.5秒（不计抓取） → 执行3.5秒 → 末端保持0.5秒
```

所以“70 帧”和“240 步”并不矛盾：前者是 20 Hz 的动作数据，后者是 60 Hz 的物理执行记录。

## 0.6 为什么需要把整条轨迹交给手，而不是只给起点和终点

导航任务常能用“起点—终点”描述目标，但抓取不是只关心最终姿态。手必须按正确时序经历：

```text
在物体外张开 → 接近物体 → 手指闭合形成接触 → 保持抓形 → 手腕抬升
```

如果只给最终手型，手指可能在接近前就闭合、从错误方向撞飞物体，或者根本无法到达目标手型。整条轨迹同时规定了**空间路径**和**动作时机**。进阶策略的价值，就是在保留这一基本任务进度的同时，根据真实接触和物体滑移做闭环修正。

## 0.7 代码目录怎么对应研究流程

```text
retarget_research/
├── manifests/                 冻结“评测哪些物体和轨迹”
├── retargeting/
│   ├── configs/               三只手的关节、关键点和方法参数
│   ├── prepare/               检查URDF、关键点、物体和数据
│   ├── run/                   生成目标手候选轨迹
│   ├── evaluate/              几何评估、Isaac Gym重放、成功统计
│   └── test/                  维度、映射和协议测试
├── advanced_policy/
│   ├── prepare/               专家筛选、数据划分和训练数据生成
│   ├── models.py              BC、Temporal3、PhaseResidual、Diffusion
│   ├── train.py               监督学习训练
│   ├── residual_ppo.py        Actor-Critic与PPO更新
│   ├── train_residual_ppo_general.py  三手Residual RL训练环境
│   └── evaluate_policy_*.py   闭环策略评测
├── outputs/                   重定向候选和物理评测结果
├── reports/                   表格与报告材料
└── WORK_LOG.md                按时间记录的实验事实与决策
```

如果只想先理解主线，建议按以下顺序读本文：零基础入口 → 第一部分 §4–§7 → 第一部分 §10 → 第二部分 §1–§9 → 附录名词表。代码阅读顺序见附录 B。

---

# 第一部分：基础重定向任务

## 1. 任务定义

**任务**：把 Shadow Hand（仿真人手，22 个手指关节）的 1000 条成功抓取轨迹，重定向到三只结构不同的机器手（Linker O6 / XHand / Wuji），并在 Isaac Gym 中完整重放。主报告指标遵循参考代码的目标区域成功判据；同时用更严格的“末段稳定 + 接触不断 + 掌物相对滑移受限”筛选可训练专家。题目本身没有规定必须抬升 30 cm，30 cm 只保留为历史高抬升诊断。

**硬约束**：

- 数据规模固定：50 类、100 物体、1000 轨迹，抽样 seed 和清单冻结可复现
- 指标是物理成功率，不是几何误差（反复验证过：关键点误差小的轨迹不一定抓得住）
- 测试集与调参集分离（调参只在 calibration/开发集，正式 1000 只在方法冻结后碰）

## 2. 三只目标手的选择与结构

### 2.1 选择标准与决策

| 候选 | 主动自由度 | 决策 | 理由 |
|---|---|---|---|
| Linker O6 | 6 | ✅ | 低自由度、强欠驱动代表——研究"信息极限压缩下能否保留抓取功能" |
| XHand | 12 | ✅ | 参考仓库唯一完整基线——先跑通全链路的中等难度基准 |
| Wuji | 20 | ✅ | 高自由度、形态接近 Shadow——研究"高保真重定向上限" |
| Allegro | 4 指 | ❌ | 不符合"五指灵巧手"字面要求 |
| Revo3 | 21 | ❌ | 超过 20 主动自由度上限 |

三只手构成**自由度梯度实验**（6→12→20），核心研究问题之一："自由度从 20 降到 6，重定向误差和物理成功率如何变化？"

### 2.2 各手结构细节

**Shadow Hand（源手）**——22 个手指关节：

- 食指/中指/无名指：各 4 关节 = 侧摆(±0.349 rad) + 屈曲×3(0–1.571)
- 小指：5 关节 = 侧摆×2 + 屈曲×3
- 拇指：5 关节 = 旋转(±1.047) + MCP 屈曲(0–1.222) + 侧摆(±0.209) + 侧摆(±0.524) + 远端屈曲(-1.571–0，**负号方向**)
- 轨迹格式 (T,28) = [平移3, 欧拉3, 关节22]

**Linker O6**——6 电机带 11 手指关节：

- URDF 中 5 个 mimic 联动（如某关节 = 另一关节 × 1.86 / × 0.89 倍率）
- Isaac 中实际 17 DOF = 虚拟手腕 6 + 手指 11
- 优化只做 6 个主动量，mimic 在模型内部展开（`expand_linker_frame`）
- 保存格式 (T,12) = [腕6, 主动6]

**XHand**——12 关节：

- 拇指 3：bend [0,1.832] + rota1 [-0.698,1.570] + rota2 [0,1.570]
- 食指 3：bend(侧摆) [-0.174,0.174] + joint1/joint2 [0,1.919]
- 中指/无名指/小指：各 2 个屈曲 [0,1.919]
- 保存格式 (T,18) = [腕6, 关节12]

**Wuji**——20 关节 = 5 指 × 4：

- 普通指布局：[MCP 屈曲 0–1.636, 侧摆 ±0.495, PIP 0–1.627, DIP 0–1.627]
- 拇指 4 关节：屈曲 [−0.045,1.651] / [−0.166,0.934] / [−0.493,1.627] / [−0.493,1.627]
- 保存格式 (T,26) = [腕6, 关节20]

## 3. 数据来源（逐字段）

### 3.1 源轨迹 GraspM3

每物体一个 npy：

```
{
  "grasp_seqs": (20, 70, 28),  # 20轨迹×70帧×28维；70帧@20Hz=3.5秒
  "obj_rotmat": (20, 3, 3),    # 物体世界旋转（整条轨迹固定）
  "obj_scale":  (20,),         # 物体缩放
}
```

`.npy` 是 NumPy 保存数组的二进制格式。这里它保存的是一个 Python 字典，而不是一张图片或一个模型。若 `grasp_seqs.shape=(20,70,28)`，含义是该物体有 20 种抓取演示，每种演示有 70 帧，每帧是 28 个控制量。

第 `i` 条轨迹必须与 `obj_rotmat[i]` 和 `obj_scale[i]` 一起使用：前者告诉程序物体在世界中朝向哪里，后者告诉程序网格应放大或缩小多少。它们不是每帧变化的状态，而是该条抓取的固定初始化条件。

坐标约定：源平移以 `[0,0,1]` 为参考点，程序统一做 z 方向对齐。不同手和不同参考代码可能使用不同基座原点，所以“数值相同”不一定代表世界中的实际位置相同；这也是必须校准手腕变换的原因。

### 3.2 物体模型 COACD

物体网格切成凸块（`coacd/decomposed.obj`），物理引擎用凸块算碰撞。开发集物体在 `reference/.../object_41/`，正式集在 `external_data/meshdata/`。

可以把视觉网格理解为“物体看起来是什么形状”，把碰撞网格理解为“物理引擎认为哪里能撞到”。复杂凹形网格直接做碰撞很慢，COACD 把它近似拆成若干凸块，既保留主要形状，又让 PhysX 更容易计算接触。

### 3.3 URDF 与正向运动学

所有方法的第 0 步：解析 URDF → 拿到关节名/边界/mimic → 建立 FK 模型（给定关节角算任意 link 上点的世界坐标）。XHand/Wuji 用 `get_penetraion_keypoints`（每手预定义的关键点集合），Linker 用 `linker_world_points`（link 局部坐标 + 全局手腕变换 + scale）。

**URDF** 是机器人的结构说明书，记录“哪个 link 连接哪个 joint、关节绕什么轴转、能转多少、mesh 放在哪里”。**link** 是一段刚性骨骼，**joint** 是两段骨骼之间的运动连接。

**正向运动学（FK）**的输入是所有关节角，输出是手掌、指尖等位置。它回答“已知怎么弯，手指在哪里”。重定向需要反过来寻找关节角，因此本质上是一个带约束的逆运动学问题；代码没有直接求解析逆解，而是让 SLSQP 反复调用 FK，并根据位置误差迭代调整关节。

关键点不是模型自动理解的“食指尖”语义，而是我们事先在每只手的某个 link 局部坐标中标出的点。例如 Shadow 食指尖必须对应 XHand 食指尖，不能误对应到中指或关节中心。关键点可视化校准就是为了在大批运行前发现这种语义错误。

#### 3.3.1 关键点究竟是 link，还是一个点

最终计算误差的始终是三维点，不是两个 link。一个关键点由两项信息唯一确定：

```text
点固定在哪个 link 上 + 它在该 link 局部坐标系中的 [x,y,z]
```

FK 先算出 link 的世界旋转 `R_link` 和平移 `t_link`，再得到点的世界坐标：

```text
p_world = R_link @ p_local + t_link
```

因此，link 名只回答“这个点跟随哪节指骨运动”，局部坐标才回答“它在这节指骨上的具体位置”。XHand/Wuji 的局部点已经写在各自的 penetration-points 资产里，`get_penetraion_keypoints` 返回 FK 变换后的世界点；Linker 则在 `linker_o6_keypoint_map.json` 中直接写明 `linker_link + linker_local_xyz`。

#### 3.3.2 15 个语义位置如何确定

统一语义集合为：掌心 1 个；食指、中指、无名指、小指各取近端/中段/指尖 3 个；拇指取中段/指尖 2 个，共 `1+4×3+2=15`。“对应”表示相同的解剖或功能位置，不表示 link 编号、关节数量或指节长度必须相同。

| 语义 | Shadow 索引 | XHand 索引 | Wuji 索引 | Linker 点序号 |
|---|---:|---:|---:|---:|
| 掌心 | 0 | 0 | 0 | 0 |
| 食指近端/中段/指尖 | 1/3/4 | 10/12/14 | 6/8/10 | 1/2/3 |
| 中指近端/中段/指尖 | 5/7/8 | 15/17/19 | 11/13/15 | 4/5/6 |
| 无名指近端/中段/指尖 | 9/11/12 | 20/22/24 | 16/18/20 | 7/8/9 |
| 小指近端/中段/指尖 | 13/15/16 | 25/27/29 | 21/23/25 | 10/11/12 |
| 拇指中段/指尖 | 19/20 | 6/8 | 3/5 | 13/14 |

- XHand 的对应表从官方参考脚本中的 `SHADOW_MATCH_ALL/XHAND_MATCH_ALL` 逐项翻译，不是我们重新猜的。
- Wuji 先从 URDF 确认 `finger1=拇指，finger2..5=食指..小指`，再从它的 26 个预定义点中选取相同语义位置。
- Linker 没有可直接沿用的 15 点官方表：掌心是基座 link 原点，近端点由 URDF 关节/骨段末端确定，指尖由 distal mesh 末端确定，四指中段点放在 distal 起点到指尖的 37% 处。这 37% 是为了使位置约对应 Shadow 中间指节末端，它只是几何观测点，不会给 Linker 新增自由度。

完整表在 `configs/xhand_keypoint_map.json`、`configs/wuji_keypoint_map.json` 和 `configs/linker_o6_keypoint_map.json`。

#### 3.3.3 这些点在指骨 link 上的精确局部坐标

坐标单位都是米，下表的坐标只能在它所属 link 的局部系中解读，不能把不同 link 的 XYZ 直接相减当成指节长度。

| 手 | 局部点的实际定义 |
|---|---|
| XHand | 掌心 `[0.005,-0.006,0.020]`；食指 link1/link2/tip 分别为 `[-.0015,.0005,.0213] / [-.0015,.0005,.0115] / [-.0015,.0005,-.006748]`；中指 `[-.0015,.001,.0248] / [-.0015,.001,.015] / [-.0015,.001,-.003248]`；无名指 `[-.0015,-.001,.0238] / [-.0015,-.001,.014] / [-.0015,-.001,-.004248]`；小指 `[-.0015,-.004,.0198] / [-.0015,-.004,.0100] / [-.0015,-.004,-.008248]`；拇指中段和指尖都用各自 link 原点 `[0,0,0]` |
| Wuji | 掌心 `[-.01,0,.005]`；四根普通指的 link1 近端点均为 `[0,0,0]`，link3 中段点均为 `[0,0,.002]`，tip 点均为 `[0,0,-.0045]`；拇指 link3 中段为 `[0,0,0]`，tip 为 `[0,0,-.004]` |
| Linker | 掌心 `[0,0,0]`；四指 proximal 点均为 `[-.0052516,0,.036625]`，distal 上的中段点均为 `[.005424345,.000002528,.013901684]`，指尖点均为 `[.014660391,.000006831,.037572119]`；拇指中段 `[.0037776,0,.045368]`，拇指尖 `[-.006392941,0,.048517395]` |

XHand/Wuji 原始坐标分别保存在 `reference/HandRetargetTask2026/scripts/assets/xhand_right/urdf/penetration_xhand.json` 和 `.../wujihand_urdf/urdf/penetration_wuji_right.json`；Linker 坐标保存在本项目的 `configs/linker_o6_keypoint_map.json`。

### 3.4 冻结清单 manifest

- **50 集筛选集**：train50（SHA-256 排序选 50 类各 1 条，不读物理标签）/ confirm_e
- **正式 1000 集**：formal_50c_100o_1000t_seed20260808.json（每类 2 物体 × 10 轨迹）
- 筛选纪律：任何新方法先 50 集评估，绝不直接上 1000（贵）；正式集只在方法冻结后评测

manifest 是一份 JSON 清单，明确指定物体名、源文件、轨迹索引、资产路径和文件哈希。它本身不存轨迹，只是回答“这次实验到底评哪些样本”。冻结 manifest 的意义类似考试前封卷：方法不能因为已经看到某些轨迹成功或失败，就悄悄换成更有利的样本。

本项目虽然基础方法没有神经网络参数，也仍然需要开发集和测试集分离。因为关键点、loss 权重、接触阈值、夹紧量等人工选择，本质上也会对开发样本过拟合。

## 4. Pipeline 五步（每步输入输出）

```
①读数据      ②重定向（核心）        ③物理重放           ④评测              ⑤配对比较
.npy+URDF → 逐帧优化出候选轨迹  →  Isaac真执行   →  成功/失败+失败分类 →  逐条对比统计
```

- **②重定向**：输入源轨迹 (70,28) + 目标手 URDF → 输出候选 (70,D)（D=12/18/26）。逐帧求解（见 §6）
- **③物理重放**：CPU PhysX、dt=1/60、substeps=2、每源帧执行 3 步（20Hz 动作）、PD 控制（刚度 120 阻尼 5）驱手指、落稳 30 步后开始、末动作保持 30 步
- **④评测**：见 §5
- **⑤配对比较**：同一批轨迹上新旧方法逐条对比 → "新增 N/丢失 M/净变化 + McNemar exact p"

**核心原则：候选 ≠ 专家**。数学优化出来的轨迹只是"候选"；物理重放成功抬起的才是"专家轨迹"。

配对比较不是只看两个总成功率。例如旧方法 10/50、新方法也 10/50，可能是同样 10 条成功，也可能新方法新增 10 条同时丢掉旧方法 10 条，稳定性含义完全不同。程序把每条轨迹分成“共同成功、共同失败、新增、丢失”四类；McNemar 检验主要比较新增与丢失是否明显不对称。

以 Linker 的一条轨迹为例，程序中的对象依次变化为：

```text
源数组 grasp_seqs[i]          (70, 28)
        ↓ retarget_linker_*.py
目标候选 grasp_seqs[j]        (70, 12)
        ↓ expand_linker_frame
Isaac实际位置目标              (每步17维：腕6+真实手指11)
        ↓ PhysX接触仿真
物体位置/接触/实际关节曲线     (240步)
        ↓ compute_success_metrics
一条JSON报告：success、最高高度、最终高度、接触等
```

这里的 PD 控制器可以直观理解为“带阻尼的弹簧”：位置误差越大，关节越用力追向目标；阻尼抑制高速震荡。我们输出的是目标位置，真正的接触力由控制器和 PhysX 根据质量、摩擦、碰撞共同产生。

**Isaac Gym 不等于一定使用 GPU。**基础重放代码通过 `create_cpu_sim` 明确使用 CPU PhysX，以保证三手评测口径一致；是否使用 GPU 是仿真参数，而不是由“导入了 Isaac Gym”自动决定。进阶 PPO 的神经网络在用户的 RTX 4060 上用 CUDA 训练，但当前通用环境的接触仿真仍走 CPU PhysX。网络设备和物理设备是两件不同的事。

## 5. 最终评测协议（reference + stable 15 cm v3，逐条）

最终结果同时报告两层指标，不能混为一个数字：

1. **参考 Isaac 成功**：参考目标在台面上方 0.30 m，目标半径为 0.15 m；物体中心曾进入目标球即锁存成功。这是与题目参考实现对齐的主报告指标。
2. **稳定运输成功**：物体在**最后 30 个物理步（0.5 秒）**持续满足下表，并进一步限制物体相对手掌的滑移；它用于判断轨迹能否成为进阶任务的训练示范。

| 门 | 阈值 | 作用 |
|---|---|---|
| 高度保持 | 末 30 步全部 ≥ 0.15 m | 排除"瞬间过线" |
| 回落 | 峰值→末段 ≤ 0.03 m | 排除"抓起又掉" |
| 波动 | 末段高度震荡 ≤ 0.01 m | 物体在手里要稳 |
| 接触 | 末段接触比例 = 100% | 排除"抛起来" |
| XY 漂移 | ≤ 0.25 m | 排除"撞飞" |

**运输质量门**（进阶任务的入场券）：末段物体相对手掌的滑移/转动要小——通过它才是"可用专家"。
**手型解剖门**：四普通指远端关节不得反向弯折（视频审查+关节审计触发，详见 §8）。

稳定门中的“抬升 15 cm”不是看物体世界坐标是否大于 0.15，而是计算：

```text
当前物体z − 落稳后初始物体z ≥ 0.15 m
```

这样大物体和小物体即使初始中心高度不同，也使用同一抬升距离。`success` 是整条 episode 的布尔结果，不是“成功帧占多少”。只要末段稳定门有一项失败，这条轨迹就计为失败。

“参考成功”“稳定成功”和“运输质量”是逐步变严格的三层筛选。以 Linker 为例，439 条进入参考目标区，394 条满足末段稳定，350 条再通过掌物相对滑移与训练质量门；进阶训练只使用最后这 350 条。XHand 对应 695/655/630，Wuji 对应 718/682/662。

**口径教训**：早期把 30 cm 误当成题目硬指标，甚至有些源专家本身也达不到该高度，导致把合理抓取误判为失败。最终协议同时保留参考可比性和严格质量筛选，并写入冻结 JSON，后续不再随实验改口径。

## 6. 绝对关键点法（所有方法的公共基线，实现细节）

**变量**（每帧）：[目标手关节 D-6, 手腕平移3, 手腕欧拉3]（内部布局；保存时转回 [腕6, 关节]）。

**目标函数**（每帧）：15 对语义点的三维平方距离取平均再乘 1000（掌心、四指近端/中段/指尖、拇指中段/指尖）。

**求解**：nlopt LD_SLSQP，逐帧独立求解、上一帧热启动、首帧零位或对齐初值；maxeval 50–100，xtol_rel 1e-6，ftol_rel 1e-8。

**边界**：关节 URDF 上下界（Wuji 再叠加解剖配置），平移 ±2 m，欧拉 ±π。

**为什么逐帧而不是整段**：70 帧联合优化变量达 `70×D`，求解成本和调参难度都大幅上升；逐帧+上一帧热启动，必要时再加显式时序正则，成本更可控。官方 XHand 最终基线只有热启动，没有显式时序 loss。

把公式翻译成直白语言：假设这一帧有 15 对对应点。对每一对，程序计算目标手点与 Shadow 点之间的三维距离，距离越大，loss 越大。SLSQP 尝试改变目标手关节和手腕，让总 loss 下降，同时不允许关节超过 URDF 边界。

一次迭代的内部过程是：

```text
给出一组目标手关节角
    ↓ FK
算出15个目标手世界关键点
    ↓ 与Shadow关键点作差
得到loss和梯度
    ↓ SLSQP更新变量并执行边界约束
重复，直到收敛或达到maxeval
```

“上一帧热启动”只表示第 `t` 帧从 `q_(t-1)` 附近开始找，不表示直接复制上一帧。这样既利用动作通常连续的事实，也避免每帧从完全张开的随机位置重新搜索。

`maxeval=50` 表示单帧最多评估目标函数约 50 次，不是训练 50 个 epoch。`xtol_rel` 和 `ftol_rel` 分别控制变量变化与 loss 变化多小时认为收敛。

**关键发现（后续所有方法的出发点）**：绝对点法的"低 loss"不保证"抓得住"——Wuji v2 关键点误差 1.965 mm 却比 11.821 mm 的 v1 成功率低（12/20 vs 16/20）。**几何相似 ≠ 抓取功能**。

### 6.1 单帧的实际输入、未知量和输出

对源轨迹第 `t` 帧 `s_t=[shadow腕6, shadow关节22]`，先用 Shadow FK 计算 21 个世界关键点，再按映射表取出 15 个目标点 `P^S_t`。优化器不会改 Shadow 数据，它改的是目标手参数：

```text
x_t = [joints, tx,ty,tz, roll,pitch,yaw]
```

| 手 | `joints` 维数 | 优化变量总维数 | 保存时顺序 |
|---|---:|---:|---|
| Linker O6 | 6 个主动电机量 | 12 | `[腕6,主动6]` |
| XHand | 12 | 18 | `[腕6,关节12]` |
| Wuji | 20 | 26 | `[腕6,关节20]` |

代码内部先放 joints，是为了方便拼接 URDF 关节边界；保存前再换回和数据集一致的“腕在前”格式。Linker 保存的 6 个手指量在物理重放前经 `expand_linker_frame` 展开成 11 个真实手指关节；5 个 mimic 值由主动关节乘 URDF 中的 multiplier 得到，不是优化器另外求出的。

### 6.2 从欧拉角到 FK，以及梯度如何回到 SLSQP

`roll,pitch,yaw` 是手腕绕 X/Y/Z 轴的三个旋转角，代码用 `robust_compute_orth6d_from_eulerXYZ` 转成 6D rotation，再与平移、手指角拼成手模型接受的 `q`。每次 SLSQP 给出一个候选 `x_t`，程序执行：

```text
NumPy候选x_t
  → Torch tensor(requires_grad=True)
  → 欧拉角转6D rotation
  → FK得到目标手关键点
  → 组合loss
  → loss.backward()
  → 把Torch梯度写入NLopt的gradient缓冲区
  → SLSQP在边界内更新x_t
```

因此这里确实使用梯度下降方向，但它不是训练一个 MLP：没有跨轨迹保存的可学习权重，每帧都在直接求该帧的关节角。上一帧的解只作为下一帧初值和时序正则参考；换一条新轨迹仍需重新优化。

### 6.3 绝对点 loss 的准确定义，为什么乘 1000

绝对点基线的几何项是：

```text
L_point = mean_i( ||P_target_i(x_t) - P_shadow_i(t)||_2^2 ) * 1000
```

点的单位是米；1 cm 误差平方后只有 `0.0001`。乘 1000 后变成 `0.1`，可以与数值量级约为 `0.001~1` 的关节/时序正则共存，也让优化器更容易看到几何梯度。它只是数值尺度：如果整个 loss 所有项同时乘相同常数，理论最优解不变；但只放大位置项会改变它与正则项的相对重要性。

官方 XHand 基线的 `enlarge_scale` 默认是 `1.0`：它直接拟合 Shadow 世界点，**没有按每根手指自动缩放**。这与 §7 的向量方法必须区分。

### 6.4 边界到底限制了什么

- **关节边界**：来自 URDF 的 lower/upper，保证关节不超过模型允许角度。
- **手腕平移边界**：`tx,ty,tz ∈ [-translation_bound,+translation_bound]`，本项目通常为 ±2 m。平移就是整只手在世界中沿 x/y/z 移动，不改变手型。
- **手腕欧拉角边界**：`roll,pitch,yaw ∈ [-π,+π]`，描述整只手的姿态，不是手指关节。
- **Wuji 解剖配置**：这不是一组新关节，而是在 URDF 边界内再收紧范围。`wuji_anatomy_coupled_v1.json` 把四指 PIP/DIP 下界从约 -28° 收到 -5°，并加 `0.02*(DIP-2/3*PIP)^2`软协调项。“硬边界”绝不允许超过；“软协调”只是倾向，其他 loss 足够大时可偏离。

### 6.5 上一帧热启动和时序正则不是一回事

**热启动**是“从哪里开始搜”：第 `t` 帧以第 `t-1` 帧的解作为 SLSQP 初值。**时序正则**是“搜到哪里会受罚”：

```text
L_temporal = wq * mean((q_t-q_{t-1})^2)
           + wt * mean((trans_t-trans_{t-1})^2)
           + wr * mean((rotation6D_t-rotation6D_{t-1})^2)
```

只热启动时，优化器仍可以从上一帧跳很远；加时序项才会显式惩罚跳变。Linker 最终向量法的 `previous_joint_weight=0.01`，只约束 6 个手指主动量；掌心通过每帧掌心锚定跟随源轨迹。

### 6.6 求解终止和失败回退

每帧新建 `nlopt.LD_SLSQP`，设置上下界、`maxeval=50~100`、`xtol_rel=1e-6`、`ftol_rel=1e-8`。终止条件分别是达到最大评估数、变量相对变化已很小、loss 相对变化已很小。如果 NLopt 报 `RoundoffLimited/RuntimeError`，当帧保留启动值；这能让批处理继续，但不意味着该帧已经优化成功。所以候选文件会保存逐帧 loss，最终还必须经物理重放过滤。

## 6.5 冻结的最终物理优化方法（实现级主线）

第 6 节及后续三手几何方法现在承担的是**产生初始轨迹**，不是最终结果。最终提升来自在 Isaac Gym 中直接评价完整抓取的两层 CEM：

```text
Shadow 轨迹
  → 三手各自的运动学初始轨迹
  → Global CEM（Linker 再做一层 Global2）
  → 从成功轨迹拟合 Rank-5 关节协同基
  → Rank-5 分阶段协同 CEM
  → 候选和基线各独立重放两次确认
  → 不稳定则恢复基线
  → 正式 1000 条独立重放与审计
```

### Global CEM 优化什么

它不训练一个跨物体共享的网络，而是针对**当前一条轨迹**搜索少数全局残差参数：腕部平移、腕部旋转以及成组手指的闭合偏置。参数经过平滑时间窗施加到原 70 帧动作，避免某一帧突然跳变。一次候选的分数不是几何点误差，而是把整条候选送入 Isaac Gym 后，由目标区到达、末端高度、接触、回落和稳定性共同给出。Linker 的 6 个主动关节压缩最严重，所以在第一层后再运行一次 Global2。

### Rank-5 协同从哪里来、优化什么

先把已有高质量轨迹中每帧相对初始手型的关节变化堆成矩阵，再用 SVD/PCA 取方差最大的前 5 个方向，得到 `B∈R^(J×5)`。这里 `J` 是该手的主动关节数。CEM 不再独立搜索 `J` 个关节，而只搜索 5 个系数 `c`，关节残差为：

```text
Δq = Bc
```

系数还分别乘接近、闭合、抬升阶段的平滑权重，因此可改变“何时、以怎样的整手协同关系收紧”，而不会随意扭动单个关节。正式参数为 population=8、elite=2、iterations=2、rank=5；这是小开发集筛选后冻结的计算量与收益折中。

### 为什么必须再确认

CEM 候选在一次物理重放中获胜，可能只是接触求解的随机波动。最终程序让“上一阶段基线”和“新候选”各自再独立重放 2 次；只有候选重复成功且平均分超过选择余量才接受，否则恢复基线。最终确认只接受 Linker 37/244、XHand 25/184、Wuji 43/154 条曾被修改的候选，说明单次最优确实偏乐观。

### 三只手哪里相同、哪里不同

三只手共享同一主线：物理 Global CEM、Rank-5 协同 CEM、重复确认和同一评测协议。差异只在必须尊重的机械结构上：三手运动学初值定义不同，Linker 额外有一层 Global2。没有为每只手事后挑选不同成功判据，也没有把多种候选的成功并集当最终结果。

## 7. 三手运动学初始方法（实现级细节，历史探索也保留）

阅读下面三手方法前，先区分四种约束各自在管什么：

| 约束 | 直观问题 | 它直接保证什么 |
|---|---|---|
| 绝对关键点 | “目标手的点是否到了源手点的位置？” | 几何位置接近 |
| 功能向量 | “掌心到指尖的方向和相对长度是否相似？” | 跨手形态关系 |
| 接触锚 | “应该接触物体的指尖是否真的靠近接触区域？” | 接触位置 |
| 抓握偏置 | “低自由度手是否弯得足够紧？” | 闭合程度 |

它们都不能单独保证抓取成功，因为物理成功还取决于摩擦、接触法向、力矩、碰撞和执行时机。它们最终只负责按各手机械结构生成运动学初值；随后三手统一进入 §6.5 的物理CEM与重复确认。

### 7.1 XHand：官方参考运动学初值与历史向量挑战者

**官方运动学初值**：参考仓库实现（SLSQP + 15 点匹配），视频审查通过。旧v2协议曾统计为516稳定/501运输；按当前v3同协议重审计，物理CEM前的参考成功/运输为565/513，最终提升到695/630。

官方基线逐帧执行的代码级过程如下：

1. `get_shadow_keypoints()` 把源帧28维 `[平移3,欧拉3,关节22]` 转成模型输入 `[平移3,rotation6D,关节22]`，经 Shadow FK 得到 `(21,3)` 点；按 `SHADOW_MATCH_ALL=[0,1,3,4,5,7,8,9,11,12,13,15,16,19,20]` 取15点。
2. XHand 待求变量为 `[关节12,平移3,欧拉3]`。FK 得到30点后按 `XHAND_MATCH_ALL=[0,10,12,14,15,17,19,20,22,24,25,27,29,6,8]` 取15点。
3. 目标函数只有 `mean(sum((xhand_points-shadow_points)^2, xyz))*1000`；官方最终设置没有逐指 scale、接触 loss 或显式时序 loss。
4. 第0帧关节全0，平移复制 Shadow 腕部平移；由于两套资产基坐标轴不同，旋转初值使用固定对齐矩阵 `[[0,1,0],[-1,0,0],[0,0,1]] @ R_shadow`。这相当于绕世界 z 轴做一次坐标约定对齐，不是从数据中学习出来的旋转。
5. 后续帧以上一帧18维解热启动。关节使用 XHand URDF 边界，平移±`trans_bound`，欧拉±π；SLSQP 每帧最多 `iter_num` 次评估。
6. 求出的内部 `[关节12,腕6]` 在保存时整理成 `(70,18)` 的 `[腕6,关节12]`，同时原样带上对应的 `obj_rotmat/obj_scale` 供 Isaac 重放。

`enlarge_scale` 若不为1，会把全部 Shadow 关键点和物体 scale 一起做全局放缩；正式基线使用1.0。它不是按食指、拇指分别补偿长度，因此不能与下面的 `scale_i` 混称为同一种缩放。

**向量+α 方法（探索中最好，50 集 32/50 vs 官方 27/50）**：

向量法不要求两只不同形态的手把中间骨节硬放在相同世界坐标，而是比较“从掌心指向指尖”的箭头。箭头方向描述手指朝向，长度比描述展开程度；零姿态尺度比先吸收两只手天生的长度差异。

目标函数逐项（归一化单位下）：

```
L = Σ_i w_i·Huber(‖v_i(q) - scale_i·v̂_i‖, 0.02)  位置向量 ×1000
  + Σ_j w_j·Huber(‖û_j(q) - û̂_j‖, 0.5)            方向向量
  + 1000·‖palm(q) - palm̂‖²                        掌心锚定
  + 0.0025·‖joints‖²                               中性正则
  + 0.01·‖joints - joints_prev‖²                   时序
```

- **位置向量 10 条**：掌心→指尖×5（权重 1.0）+ 掌心→中段×5（权重 0.5）
- **方向向量 5 条**：中段→指尖单位方向（权重 5.0）——专门防止"指尖位置对但指向错"的退化
- **scale_i**：对每一条位置向量分别用两手零姿态长度比计算：`scale_i=||v_target_zero||/||v_shadow_zero||`。随后每帧目标为 `v_ref_i(t)=scale_i*v_shadow_i(t)`。方向向量会归一化成单位方向，因此不使用长度 scale。这里缩放的是 loss 里的参考箭头，**不是修改 URDF、mesh 或真实指骨长度**；目标手始终通过自身 FK 产生合法刚体手型。如果多条参考箭头无法同时满足，优化器只能留下残差折中，不会把实体手掌拉变形
- **α 捏合混合**（AnyDex 核心思想）：每帧量源手拇指尖到各指尖距离 d，

  ```
  α = clip((0.05 - d)/(0.05 - 0.025), 0, 1)
  指尖权重 ×= α + (1-α)×0.6；方向权重 ×= α + (1-α)×0.5
  ```

  d ≤ 2.5cm（捏合中）→ 该指尖权重拉满；d ≥ 5cm（张开）→ 放松跟踪。**闭式计算，不加优化维度**。
- **Huber(δ=0.02)**：误差超 2cm 后线性计——防止个别不可达目标霸占梯度

**为什么 50 集赢、正式集输（461 vs 516）**：50 集每类只 1 条轨迹，正式集每物体 10 条（轨迹索引 0–9 抓取风格差异大）；α 权重对源手拇指-指尖距离敏感，在未见轨迹风格上不稳定。**教训：开发集赢家必须经过正式集验证**——这是本项目第二个"开发集赢家未泛化"案例。

### 7.2 Linker：历史功能向量初值与组件消融

6 DOF 单技巧不够，四组件叠加（每个都有消融增量）：

#### 7.2.1 Linker 的 15 个世界点从哪里来

`retarget_linker_vectors.py` 先加载 `linker_o6_keypoint_map.json`。映射表的数组顺序本身就是 Linker 点编号：`0=掌心`，`1/2/3=食指近端/中段/指尖`，`4/5/6=中指`，`7/8/9=无名指`，`10/11/12=小指`，`13/14=拇指中段/指尖`。

`linker_points()` 接收 `[主动关节6, 平移3, 欧拉3]`，先把欧拉转为 6D rotation，再调用 `model.update_kinematics(q)`。`linker_world_points()` 对映射表中的每个 `linker_local_xyz` 执行对应 link 的 FK 变换，返回 `(15,3)` 世界点。这里没有把一个 link 当成一个点；每个点都有明确 link 和局部 XYZ。

6 个主动量进入 FK 后，模型按 URDF mimic 关系同步计算另外 5 个从动关节，所以优化器看到的是一个只有 6 维可控空间、但外观上有 11 个关节运动的手。这也是 Linker 比 XHand/Wuji 更容易出现“多个目标互相冲突”的根本原因。

**组件 1：分指段向量 + α**（配置 `linker_anydex_vectors_v2.json`）
Linker 手指两段式：位置向量 = 掌心→指尖×5（w1.0）+ 掌心→近端×4（w0.5）+ 掌心→拇指中段（w0.5）；方向 = 近端→指尖×5（w5.0）；α 混合同 XHand。

配置中的 15 条 `pairs` 不是 15 对绝对点，而是 15 条箭头：

| 类型 | Shadow 端点 | Linker 端点 | 数量 | 作用 |
|---|---|---|---:|---|
| 指尖可达位置 | 掌心0→4/8/12/16/20 | 掌心0→3/6/9/12/14 | 5 | 决定五个指尖相对掌心在哪里 |
| 手指形状位置 | 掌心0→四指近端1/5/9/13、拇指中段19 | 掌心0→1/4/7/10/13 | 5 | 防止只对准指尖而骨段姿态任意 |
| 末段方向 | 四指近端→指尖、拇指中段19→指尖20 | 1→3、4→6、7→9、10→12、13→14 | 5 | 防止“端点接近但手指朝向相反” |

程序启动时把 Shadow 和 Linker 都置零位，通过 `zero_pose_points()` 得到两组零姿态点，再由 `zero_pose_scales()` 对全部 pair 计算长度比。实际只有前 10 条 position pair 使用 scale；后 5 条 direction pair 在每帧被除以自身模长成为单位向量。每一帧具体构造：

```text
target_position_vectors =
    (shadow_points[task] - shadow_points[origin]) * position_scales

target_directions = normalize(
    shadow_points[task] - shadow_points[origin]
)
```

同理，目标手当前 position vector 直接由 Linker 世界点相减；当前 direction vector 相减后再归一化。最终基础向量 loss 与代码完全对应：

```text
L_pos  = mean(weight_pos * HuberNorm(v_linker-v_ref, delta=0.02)) * 1000
L_dir  = mean(weight_dir * HuberNorm(unit(v_linker)-unit(v_shadow), delta=0.5))
L_palm = 1000 * ||p_linker_palm-p_shadow_palm||^2
L_neutral = 0.0025 * mean(joints^2)
L_prev    = 0.01 * mean((joints-joints_prev)^2)       # 首帧没有
```

`HuberNorm` 先求三维残差的模长 `d`：`d<=δ` 时为 `0.5*d²`，超过 δ 时改为 `δ*(d-0.5δ)`。这样不可达的某一根手指误差很大时，loss 只线性增长，不会完全压过其他手指。

#### 7.2.2 α 是怎样逐指、逐帧改变权重的

对每帧分别计算 Shadow 拇指尖20到五个指尖 `[4,8,12,16,20]` 的距离。拇指对自身距离恒为0，所以对应 α 为1；其余四指：

```text
alpha_f = clip((0.05-d_f)/(0.05-0.025), 0, 1)
```

- `d_f<=2.5 cm`：α=1，说明正在捏合，该指尖 position 和 direction 保持原权重。
- `d_f>=5 cm`：α=0，说明仍张开，指尖 position 权重乘0.6，direction权重乘0.5。
- 中间距离线性插值。

α 不改变目标向量，也不是待优化变量；它只是根据当前 Shadow 帧闭式计算的权重开关。代码只调整 position 数组的前5项（掌心→五指尖）和全部5项 direction，不调整近端/中段形状向量。

**组件 2：接触锚（+2 条）**
诊断：向量法忠实复现源手屈曲量，但 Linker 手指更长，源手"轻捏"能拿住的物体 Linker 必须"用力包住"——失败轨迹接触步数只有成功者的 1/3。
实现：**闭合阶段**，把源手**正在接触物体的指尖世界位置**（接触掩膜：指尖到物体表面 < 0.02 m）当钉子：

```
L += 3.0 × mean(‖tip_target(q) - anchor_source‖²) × 1000
```

只对掩膜内的指尖生效。规律：**只在 6 DOF 有效**——12/20 DOF 手的向量解本来就能把指尖放对，硬钉反而过约束（XHand 8/50、Wuji 28/50 判负）。

“锚”不是物体上的实体钉子，而是 loss 中的目标位置。程序从 Shadow 轨迹判断哪些指尖已经进入物体表面 2 cm 邻域，再要求目标手对应指尖靠近这些位置。它提供接触线索，但不直接计算抓力，所以仍需 PhysX 验证。

代码级流程是：先把物体 mesh 按该轨迹的 `obj_scale` 和 `obj_rotmat` 变换到世界坐标，KD-tree 查询每个 Shadow 指尖到最近物体顶点的距离；首次至少2个指尖距离不超过 `contact_threshold=0.02m` 的帧为 `close_start`。在 close 阶段，`frame_contact_anchors()` 取当前确实通过2cm掩膜的 Shadow 指尖世界坐标，并映射到 Linker 指尖索引 `[3,6,9,12,14]`：

```text
L_contact = contact_weight(3) *
            mean(||linker_tip-source_tip_anchor||^2) * 1000
```

当前实现的 contact anchor 只在 `phase==close` 且掩膜非空时加入；进入 lift 后不继续使用这个绝对世界锚，否则会把指尖钉在物体初始位置而阻碍抬升。`contact_fallback=nearest` 只负责在整条轨迹找不到“双指进入2cm”时选择第二近指尖距离最小的帧来建立阶段，不能伪造一个不存在的接触掩膜。

**组件 3：抓握偏置（+2 条）**
单侧屈曲拉力，目标剖面取自夹紧法的闭合姿态 `[1.16, 0.58, 0.94, 1.26, 1.46, 1.54]`：

```
L += 8.0 × mean(relu(target_joint - joint)²)
```

**relu 的含义**：只惩罚"弯得不够"，绝不惩罚"弯得更多"。权重扫描：5 太弱（11/50）、8 最优（13/50）、接触 5 回落（12/50）。只在闭合+抬升阶段激活。

例如目标屈曲角是 1.2 rad：当前只有 0.7 rad 时产生惩罚，当前已经是 1.3 rad 时惩罚为 0。它不是把所有轨迹变成同一个固定手型，而是给欠闭合解设置一个单侧“最低抓紧倾向”。

最终 v2 的六维下限倾向是 `[1.16,0.58,0.94,1.26,1.46,1.54] rad`。`grip_active` 只在 `close` 和 `lift` 阶段为真；approach 阶段完全不加，避免手还没到物体附近就提前夹死。这里的“目标”也不是硬边界：它是权重8的软惩罚，若几何项更强，关节仍可小于该值。

**组件 4：热启动**：首帧从夹紧法已生成的闭合姿态开始，避开"手指半开"局部最优。

更准确地说，正式入口通过 `--warm-start-dir linker_o6_optimized_v2` 找到同物体、同源轨迹索引的旧候选；只取旧候选第0帧，将保存顺序 `[腕6,关节6]` 换成内部顺序 `[关节6,腕6]`，作为新向量轨迹第0帧的 SLSQP 初值。第1帧以后都以上一帧新解热启动。旧轨迹不是监督标签，也没有逐帧混入最终结果。

**阶段检测**（自动，源数据驱动）：指尖距物体表面 < 0.02 m 且 ≥2 指 → 闭合帧；手腕升高 0.03 m → 抬升帧；接触检测无解时 `fallback=nearest`（用最近接近帧回退，7/50 条轨迹依赖）。

这里的阶段不是人工固定为“第 30 帧闭合”。不同轨迹的接近速度不同，所以程序从该轨迹自身的指尖—物体距离和腕部高度推断。`fallback=nearest` 只在整条源轨迹没有满足严格双指阈值时启用，选择最接近接触的帧，并写入元数据以便审计。

阶段的完整判定是：从 `close_start` 之后的腕部最低 z 作为基准；首次腕部 z 比该基准高 `lift_delta=0.03m` 的帧为 `lift_start`。所以阶段依次为 `[0,close_start)=approach`、`[close_start,lift_start)=close`、`[lift_start,T)=lift`。这个3cm只是“检测何时开始抬升”的时序阈值，不是最终成功高度。

#### 7.2.3 Linker 一帧从输入到输出的完整伪代码

```text
输入：Shadow第t帧21点、上一帧Linker解、物体mesh、固定vector scales

1. 从Shadow点构造10条缩放后位置目标、5条单位方向目标
2. 由拇指—各指尖距离算5个alpha，并修改对应权重
3. 根据phase决定是否建立contact anchors、是否打开grip偏置
4. 选择初值：首帧用旧夹紧候选首帧，否则用上一帧新解
5. SLSQP反复尝试 [6主动关节+3平移+3欧拉]
6. 每次尝试：mimic展开 → FK得15点 → 计算
   L_pos+L_dir+L_palm+L_neutral+L_prev+L_contact+L_grip
7. autograd返回12维梯度；SLSQP在URDF/位姿边界内更新
8. 达到maxeval=50或收敛后，保存该帧并成为下一帧热启动

输出：70×12候选轨迹 + 70个逐帧loss + scales/配置哈希/阈值等元数据
```

对应实现入口为 `retargeting/run/retarget_linker_vectors.py`，方法配置为 `retargeting/configs/linker_anydex_vectors_v2.json`，批量参数冻结在 `retargeting/run/run_linker_formal_1000.sh`。正式参数是 `maxeval=50, contact_weight=3, grip_flexion_weight=8, contact_threshold=0.02m, lift_delta=0.03m, fallback=nearest`。

### 7.3 Wuji：历史无约束点法与三处手型修复记录

**历史运动学选择**（当时以成功率优先）：15 点逐帧 SLSQP，无解剖约束。它曾出现四指反向弯折和拇指过早弯成90°，因此后来又完成拇指零空间等修复。当前最终Wuji轨迹还要经过Global/Rank-5 CEM和重复确认，不能再把这段旧SLSQP单独称为最终方法。

“无解剖约束”不代表没有任何关节边界，而是仍使用原 URDF 边界；问题在于 URDF 允许普通指远端关节向反方向转约 28°。数学优化把这个合法数值范围当作可用动作，但它视觉上不像自然人手，也可能不适合真机长期执行。

#### 7.3.1 Wuji 旧点法究竟怎样实现

`retarget_wuji_keypoints.py` 读取 `wuji_keypoint_map.json`：Shadow 的15点映射到 Wuji 26个预定义点中的 `[0,6,8,10,11,13,15,16,18,20,21,23,25,3,5]`。每帧未知量是 `[关节20,平移3,欧拉3]`，目标函数的基础项为：

```text
L_keypoint = mean_i(||P_wuji_i(q)-P_shadow_i||^2) * 1000
```

最终旧无约束版本没有传 anatomy profile，所以关节上下界就是原始 URDF 上下界；也没有启用接触、参考姿态或 PIP/DIP 协调项。第0帧与 XHand 一样使用固定坐标对齐矩阵初始化腕部、20个手指角置0，之后逐帧热启动。内部 `[关节20,腕6]` 保存为 `[腕6,关节20]`，得到 `(70,26)`。

该文件中的 `WujiFrameObjective` 还预留了三组可选项，但“代码存在”不代表最终旧版本启用：

```text
joint/translation/rotation temporal  # 对上一帧连续性
reference joint/translation/rotation # 对另一候选轨迹的贴近
contact anchors                      # 指尖目标点
flexion couplings                    # PIP/DIP协调
```

必须以输出 `.npy` 元数据中的权重和 `anatomy_config` 判断实际实验用了什么，不能看到类中有该分支就宣称实验已经使用。

**我们做的三处修复（作为消融保留）**：

1. **解剖约束 coupled_flexion**：PIP/DIP 下界 -28°→-5°，软约束 DIP≈2/3×PIP（w0.02），并同步写入物理仿真。结果：手型合规但 581→431（反弯抓取的"非法收益"约 150 条被去掉）
2. **拇指零空间修复（n005）**：删掉错的"拇指中段绝对对应"，只保指尖位置一条硬信息，在 4 个拇指关节的零空间里用中性正则(0.05)+时序(0.01)选自然解。90° 帧 96.1%→2.6%，指尖偏移 <3mm，train50 34/50（+5）
3. **phase_open 分阶段闭合**：接近阶段拇指保持展开、接触前 8 帧开始闭合（lead=8/settle=4）。手型完美（90° 帧 0%）但成功率 13→12→30/50 一路小降——**早闭合其实是"承力提前量"**。按成功率优先放弃，作消融

拇指零空间修复不是重新优化整只手：它读取点法候选，固定手腕和其余16个关节，只重新求4个拇指关节。loss 保持原候选的拇指指尖位置（`tip_weight=1`），同时用 `neutral_weight=0.05` 选择较自然的冗余解、用 `temporal_weight=0.01` 保持逐帧连续。所谓“零空间”就是多组拇指关节角能把指尖放到几乎同一点时，利用不明显移动指尖的剩余自由度改善手型。正式消融求解预算为 `maxeval=80`。

## 8. 基础任务的关键困难与诊断过程（答辩故事）

1. **Linker 接触全无**：初版手和物体放在不同 collision group → 逐项排查发现 → 统一 group
2. **10cm→30cm 口径修正**：用户指出题目要求 → trace 重算全部数字（Linker 234→169 稳定、XHand 574→516、Wuji 638→581）
3. **Wuji 反弯**：视频审查发现"断指" → 关节审计 92.5% 越界 → 解剖约束。但反弯正是高分来源——**视觉标准 vs 仿真成功率的冲突**成为贯穿项目的研究主题
4. **拇指 90°**：用户看视频发现 → 96.1% 帧统计确认 → 零空间修复
5. **批处理死代码 bug**：接触/抓握变体结果与纯向量"完全相同" → 逐层排查发现 `return [` 把参数追加变成死代码 → 全部相关实验重跑。**教训：批量入口的参数传递要有元数据回证**
6. **方法天花板**：Linker/XHand 关键点法长期无进展 → 用户要求跳出 → 7 方法队列全量探索

## 9. 方法队列探索的最终规律（7 方法 × 三手，50 集口径）

| 方法 | XHand | Linker | Wuji | 规律 |
|---|---:|---:|---:|---|
| 关节归一化 | 19 | 2 | 10 | 源关节模式不迁移，全判负作下限 |
| AnyDex向量+α | **32** | 4 | 30 | XHand/Linker 大幅受益；Wuji 接近 |
| 抓握偏置 | 28 | **13** | 32 | 只对欠闭合手有益，权重按手调 |
| 接触锚 | 8 | +2 | 28 | **只在 6 DOF 有效** |
| 轨迹平滑 | 31 | 10 | 31 | 时序正则已够，后处理只伤接触时机 |
| 学习式映射 | 4/10 | 3/10 | 7/10 | 蒸馏只能逼近不能超越，独立判负 |
| eigengrasp/其它 | — | — | — | 未单独胜出 |

**总规律**：逐帧 SLSQP + 功能向量表征是本任务最强的运动学框架；α 混合是最大单一贡献（XHand +2、Linker +7）。

## 10. 最终正式 1000 结果与分析

| 手 | 运动学初值参考成功 | 最终参考成功 | 末段稳定 | 稳定运输/可训练 |
|---|---:|---:|---:|---:|
| Linker | 222（22.2%） | **439（43.9%）** | 394（39.4%） | **350（35.0%）** |
| XHand | 565（56.5%） | **695（69.5%）** | 655（65.5%） | **630（63.0%）** |
| Wuji | 476（47.6%） | **718（71.8%）** | 682（68.2%） | **662（66.2%）** |

1000 条由 50 类 × 每类 2 个物体 × 每物体 10 条轨迹组成，因此每类、每物体样本数平衡。在这个特定清单上，轨迹微平均、物体宏平均和类别宏平均数值一致：

- **轨迹微平均**：所有成功条数直接除以 1000；
- **物体宏平均**：先算每个物体成功率，再对 100 个物体等权平均；
- **类别宏平均**：先算每类成功率，再对 50 类等权平均。

如果数据不平衡，微平均会更受样本多的类别影响，宏平均则保证每个物体或类别权重相同。

- **自由度梯度仍然明显**：20 DOF Wuji 与 12 DOF XHand 接近 70%，6 DOF Linker 仍最困难。
- **物理反馈对 Linker 的相对收益最大**：参考成功从 22.2% 提升至 43.9%，说明低自由度手仅靠几何误差很难找到有效接触。
- **最终结果不是三种方法取并集**：每条轨迹只有一个经重复确认后的确定输出；候选不稳定时恢复上一阶段基线。
---

# 第二部分：进阶任务

> **当前主线说明（2026-09-01）**：本部分保留了大量旧BC、Residual PPO、检索和DAgger的失败诊断，便于复盘，但不再代表当前最终策略。当前合法主线位于`advanced_policy_v2/`：只使用episode初始状态、在线观测、策略自身历史和初始腕部坐标系中的128点物体表面；禁止类别ID、测试专家动作、专家手腕和训练轨迹检索。候选是Geometry-Phase单步与Geometry-Chunk8，物理valid结果冻结后再决定是否训练三帧Transformer。最终模型确定后，本部分会只保留主线说明，把下面旧路线移入历史附录。

## 0. 从“轨迹重放”到“策略控制”（概念与历史路线）

基础任务中，控制器提前拿到了整条 70 帧目标手轨迹，只需按时间顺序播放。它不会观察物体滑没滑，也不会因为实际关节落后而改变下一步动作，因此叫**开环重放**。

进阶任务训练的是策略 `π`：每一个物理步都读取当前观测 `o_t`，再输出动作 `a_t`。执行后环境变成新状态，策略继续观察并决策：

```text
观察当前状态 o_t → 策略输出 a_t → PhysX执行 → 得到 o_(t+1) → 再决策
```

这叫**闭环控制**。它理论上能在物体滑动或关节没有跟上时修正动作，但也会出现新问题：策略自己的一个小错误会改变下一步输入，错误可能逐步累积。

### 0.1 监督学习和强化学习在本项目里的区别

监督学习把专家动作当标准答案：

```text
输入：专家执行时的观测
标签：专家下一动作
loss：预测动作与专家动作的差
```

BC、Temporal3、PhaseResidual 和 Diffusion 都属于这一大类。训练数据已经告诉模型“正确动作是什么”，网络通过反向传播降低 MSE 或扩散噪声预测误差。

强化学习不要求每一步都有专家标签，而是让策略在环境里执行，根据结果得到奖励：抬得更高、有接触、最终成功会得到正奖励，动作残差过大会扣分。PPO 根据这些经验增加高奖励动作的概率。最终 Residual RL 同时利用两者：专家轨迹提供基础动作，RL 学习其上的闭环修正。

### 0.2 本项目的策略不是相机输入

观测来自仿真真值，包括关节位置/速度、物体三维位姿和速度、接触数及网格形状描述。这类观测通常称为**特权状态**：非常适合研究控制算法，但真实部署时不能凭普通 RGB 相机直接得到，必须由视觉姿态估计、触觉或状态估计器替代。

所以当前结果证明的是“在准确仿真状态下的目标手控制能力”，不是完整的视觉到动作系统，也不是 sim-to-real 已完成。

## 1. 任务定义

基础任务筛选出的专家轨迹，训练**闭环操作策略**：策略每物理步读状态、出动作，从"手指全张开"的初态开始，自己完成接近—闭合—抬升。比较三类模型：单帧 MLP（BC）、三帧历史（Temporal3）、Diffusion Policy；并探索 PhaseResidual（增量+相位）与 Residual RL（PPO 残差）。

**与基础任务的关键区别**：基础任务"重放"专家轨迹（开环）；进阶任务策略**闭环**执行——每一步的动作依赖当前状态，误差会累积。

最终 Residual RL 还必须写清一个边界：它不是只输入物体状态就从零规划抓取，而是每个测试任务仍提供该轨迹的重定向基线命令，并采用专家手腕轨迹；策略只学习手指残差。因此它更准确地叫“参考轨迹条件下的闭环修正策略”。

## 2. 数据链路（专家池 → trace → split）

**专家池**（重放成功 + 运输质量达标）：

| 池 | 运输质量轨迹数 |
|---|---:|
| xhand_official | 501 |
| linker（新方法） | 153 |
| wuji_old（旧无约束） | 557 |
| wuji_n005（修复版） | 421 |

**Wuji 两池并存的实证设计**（本项目最重要的方法学设计之一）：旧无约束轨迹被"手型门"（人工视觉标准）隔离为 0 条可用专家。用户质疑：**这是视觉定义，没有训练实证**。于是两套池分别训练、分别评测——用策略成功率回答"反弯轨迹到底能不能当专家"。

**Trace**：每条轨迹重放时保存逐物理步（240 步 = 70帧×3 + 30 保持）的"**执行前状态 → 下一条命令**"对。修复过一次**未来泄漏 bug**：初版在 PhysX 推进后记录状态却与刚执行的命令配对（动作后状态配过去动作），离线 loss 虚低但闭环语义错误；修正为每步先读状态再写命令，并加元数据标签 `trace_alignment=pre_action_state_to_command_v1` 防混用。

一条监督样本不是整段视频，而是一对向量。例如第 100 个物理步：输入记录“命令执行前手在哪里、物体在哪里、有没有接触”，标签记录“接下来应发什么关节位置目标”。240 步轨迹就产生约 240 对样本。

**对象级划分**（无泄漏）：每类 2 个物体——一个物体只进 train/valid（8+2 条），另一个物体 10 条全部进 test。策略从未见过 test 物体。归一化统计只从 train 算。

不能把同一物体的不同帧随机分到 train 和 test。那样模型虽然没见过第 101 帧，却已经见过同一个网格、同一抓取方向的前后帧，测试会虚高。对象级划分要求测试物体实例整体不可见，才是在测同类别新物体泛化。

**类别 ID 的准确含义**：50 个语义类别各有一个 ID（ball=0，…，winebottle=49），不是 100 个物体各有一个 ID。同类别的训练物体和测试物体共用 ID，因此不会直接告诉网络“这是哪个测试实例”，但测试时必须已知物体类别，也不能直接处理第 51 个新类别。成功专家筛选后 Linker 有 14 类、XHand/Wuji 各有 2 类没有成功 train 轨迹，这些类别的专属 embedding 实际没有受过训练。为此新增同参数量的有/无 ID 配对消融：无 ID 组仍保留 50×16 embedding 表，但所有样本统一读取第 0 行，排除网络容量差异。

**30 cm 一致性修正**：物理成功判据早已改为稳定抬升 0.30 m，但旧离线观测中的 `remaining_lift` 仍按 0.10 m 计算，造成训练与运行时语义不一致。新数据规格 `hand_data_specs_v5.json` 和 `formal_final_30cm` 已统一为 `max(0.30-relative_z, 0)`；旧实验结果保留为历史结果，不覆盖重写。

## 3. 第一代：绝对动作回归（BC / Temporal3 / Diffusion）——为什么全灭

三个模型都输出**绝对关节角度**。12 个模型（3 模型 × 4 池）闭环评测**全部 0/500**。

**BC（Behavior Cloning）**就是把控制问题改写成普通回归：给网络当前观测，让它预测专家下一条绝对命令，使用 MSE 训练。Temporal3 在此基础上多看当前和前两步状态；Diffusion 不直接回归一个均值，而是从噪声逐步生成未来动作片段，希望表达“同一状态可能有多种合理动作”。

它们离线 loss 低只能说明“在专家访问过的状态上，输出接近专家”。真正闭环时模型访问的是自己造成的状态。假设第一步手指角度只差 0.02 rad，下一步观测就已不完全属于训练分布；模型再犯一个小错，偏差继续扩大。这就是**distribution shift（分布偏移）**和**compounding error（复合误差）**。

**诊断过程**（这是本部分最重要的方法论示范）：
1. 单条 rollout 看指标：策略相邻命令跳变 L2 均值 **1.51**，专家 **0.049**——**31 倍**
2. 手在动但物体不动：平均接触仅 0.49 步——手在物体上方乱舞，从未碰到
3. 根因：**闭环分布偏移下的绝对动作失稳**。训练时模型见到的都是专家状态；闭环里第一步的微小误差改变状态 → 模型输出跳变到另一个"动作模式" → 状态更偏 → 恶性循环

**尝试过的缓解都失败**：观测噪声增强（v5，腕部变成单调下降）、动作限速（允许步长仍是模型跳变的量级）。**结论：绝对动作回归在这个任务上不是调参能救的**。

## 4. 第二代：PhaseResidual（增量动作 + 相位）

**架构**（模型、数据集、运行时三处配套）：

- **预测目标**：增量 δ = command_t − command_{t−1}（归一化：(δ − δ_mean)/δ_std，统计从 train 算）
- **输入**：当前观测 + **上一条实际命令**（归一化）+ **相位** phase = t/209（0→1，210 个运动步）+ 类别 ID
- **执行**：command_t = command_{t−1} + δ（积分）；首步上一命令 = 张开姿态
- 模型：MLP（观测 68/66 维 → 增量 D−6 维），150 epoch 早停

为什么预测增量更稳定？绝对回归每一步都要重新回答“关节应该位于多少度”，相邻输入稍变就可能跳到另一个绝对姿态；增量回归只回答“在上一条真实命令上再动多少”。专家在 60 Hz 下相邻变化本来就很小，因此学习目标集中在零附近，动作自然连续。

相位 `phase∈[0,1]` 是归一化任务进度。0 表示刚开始，接近 1 表示运动结束。单看某一帧状态，策略可能分不清“正在接近”还是“已经抬升”；相位提供一个确定的时序坐标。它不是从传感器推断出来的隐状态，而是由当前物理步编号直接计算。

**效果**：跳变 1.51 → 0.026（比专家还平滑）、接触 0.49 → 25–170 步。但抬不起物体——引出下面的修复链。

## 5. 修复链：五个补丁（每个都有归因和消融数字）

| # | 问题（诊断证据） | 修复 | 效果 |
|---|---|---|---|
| 1 | 手指在接近阶段提前闭合（第 20 步手指已 1.1 rad，专家保持 0） | **相位感知增量钳位**：10 个相位 bin，每 bin 每维取专家增量 P95 作上限；接近段手指限幅 ≈0.0001，闭合段自动放开到 0.26–0.48 | 手指时序对齐专家 |
| 2 | 闭环抬腕消失（离线给专家状态时模型正确预测 +0.0024/步，闭环里只有 +0.0001） | **DAgger**（见 §6） | 抬腕恢复 |
| 3 | DAgger 第一版无效 | **关键 bug：在线样本的"上一条命令"从教师标签推导**，而不是学生真实执行命令——模型学不到"从自己的状态纠偏"。数据集支持独立 previous_commands 字段 | DAgger 真正生效 |
| 4 | 腕部横向漂移（专家 y 恒 0.127，模型漂到 -0.028，差 15 cm） | **层级分解**：腕部走专家开环（相位对齐的专家帧），策略只学手指 | 漂移消除 |
| 5 | 评测口径错误（调试的轨迹专家自己只抬 1.3 cm） | **专家成功子集**：只在专家重放成功的轨迹上计复现率 | 口径公平 |

**每个补丁的教训都通用**：①时序结构可以"查表"强加，不必让网络学；②复合误差要用 DAgger 而非数据增强；③管道 bug 靠"结果与预期完全一致"这种反常信号发现；④确定性成分不该进学习目标；⑤先检查任务本身是否可能。

## 6. DAgger（实现细节）

**流程**（每轮）：
1. 学生（当前模型）在 50 条 train 轨迹上闭环 rollout（每类 1 条），记录每一步的（观测、学生执行命令）
2. **教师标注**：用**相位对齐的专家命令**（完美 oracle，确定性）：phase p 的专家标签 = 专家轨迹第 round(p×69) 帧
3. 合并：离线样本 + 在线样本（各池 +12000 步），在线样本的 previous_commands = 学生真实执行命令、监督 actions = 教师（专家）命令
4. 重训 → 下一轮

**为什么用相位专家而不是学习式教师**：旧项目的类别教师在闭环里自身振荡（历史诊断），不能当 oracle；相位专家零失稳且与我们的相位设计天然对齐——比标准 DAgger 的"学习式教师"更强。

DAgger 与普通 BC 的区别不在于 loss，而在于**去哪里采输入状态**。普通 BC 只在专家状态上训练；DAgger 让学生真的跑起来，哪怕学生跑偏，也在这些跑偏状态上查询教师答案。模型因此学到“已经偏了以后怎样拉回来”。本项目的教师不是实时求解新抓法，而是根据当前相位从同一条专家轨迹取对应命令，所以它是 DAgger-like 的确定性 oracle。

**轮次结果**：第 1 轮大幅提升（Linker 3.7%→12.3% 等）；第 2 轮 Linker 继续涨（12.3%）但 XHand 回落（19.1%→8.6%）——**DAgger 轮次振荡**（第二轮学生状态聚在第一轮行为附近，重训过拟合采集分布）。处理：每池保留最优轮次。

## 7. 已撤销的参考轨迹Residual RL实验（仅作错误路线记录）

> **勘误（2026-08-21）**：以下控制器在测试时逐步读取测试轨迹的专家基线和手腕动作，不符合自主模仿学习评测。相关45%–54%只作为“正式1000条重定向轨迹池上的参考轨迹条件控制”辅助结果保留，禁止继续称为“最终自主策略”或“自主泛化成功率”。

**为什么 RL 能赢 BC**：BC 家族无论怎么增强，都在"专家分布"上训练、"自己的分布"上执行；RL 在自己的探索分布上训练，天然对闭环偏移鲁棒。

### 7.1 一步内部到底发生什么

以 XHand 为例，某个物理步先从专家候选取出 18 维基线命令。PPO 不重新输出完整 18 维，而只输出 12 个手指关节残差：

```text
expert_command = [腕6, 手指12]
policy_residual = [手指残差12]
final_command = [expert腕6, expert手指12 + residual12]
```

Linker 输出 6 维手指残差，Wuji 输出 20 维。残差经过 `tanh` 限制在 `[-1,1]`，再与基线手指命令相加并裁剪到 URDF 关节边界。训练初始标准差为 0.05，因此一开始主要在专家动作附近探索，而不是随机乱动整只手。

评测时不再随机采样，而使用高斯策略的均值并经过 `tanh`，所以同一观测得到确定性残差。

**环境**（`train_residual_ppo_general.py`，三手通用）：
- 20 env 并行（每条轨迹一个 env），240 步/回合
- 基线 = 专家轨迹（含腕部）；策略输出**手指残差** f（动作维 = 手指数 6/12/20），command = 专家帧 + 残差，经 mapper 映射到物理 DOF 布局
- **观测（逐字段）**：基线命令(D) + 实际关节位置(dof) + 关节速度(dof) + 物体相对位移(3) + 四元数(4) + 线速度(3) + 角速度(3) + [log1p(接触数), 相位, 相对z](3) + 上一步残差(f) + 物体形状描述(14 维：轴向尺寸3+顶点协方差6+表面积+体积+径向分位数3)
- **奖励**：200×max(抬升增量, −0.002) + 0.02×[有接触] + 末步成功 +10 − 0.002×‖残差‖²

观测维度不是三只手完全相同，因为 `command_dim`、物理 DOF 数和残差数不同。代码按以下公式自动计算：

```text
obs_dim = 基线命令D
        + 实际关节位置dof
        + 实际关节速度dof
        + 物体位姿与速度13
        + 接触/相位/相对高度3
        + 上一步手指残差f
        + 形状描述14
```

奖励中的“抬升增量”是相邻物理步物体高度的变化，而不是当前总高度。这样每一步向上移动都能立即获得反馈，不必等到最后才知道好坏；最终严格成功再奖励 10，保证策略仍以完整任务成功为目标。残差惩罚让策略倾向于只在必要时偏离专家。

### 7.2 Actor、Critic 和 PPO 分别做什么

Residual RL 使用的是 Actor-Critic：

- **Actor**读取观测，输出每个手指残差的高斯分布均值；训练时从分布采样以探索。
- **Critic**读取同一观测，估计“从现在开始预计还能获得多少累计奖励”。
- **Advantage**比较实际回报与 Critic 预期，判断这次动作比预期更好还是更坏。

如果 advantage 为正，PPO 会提高该动作在相似状态下的概率；为负则降低。PPO 不允许新策略一次改变过猛，而是把新旧动作概率比裁剪到 `1±0.2` 附近。这就是“Proximal（近端）”的含义：每轮只走相对保守的一步，减少强化学习突然崩溃。

GAE 用 `γ=0.99` 把未来奖励折算回当前，并用 `λ=0.95` 在高方差的真实回报和有偏的价值估计之间折中。每轮 20 个并行环境各执行 240 步，得到约 4800 个 transition，再做 4 个 epoch、每 epoch 4 个 minibatch 的网络更新。

### 7.3 CPU、GPU和并行环境的真实关系

`train_residual_ppo_general.py` 中 Actor、Critic、rollout batch 和 PPO 反向传播通过 `--device cuda` 放在 RTX 4060 上。20 个 Isaac Gym 环境也在同一个 simulator 中并行推进，但当前 `create_cpu_sim` 明确采用 CPU PhysX；Isaac Gym 是框架名称，并不自动意味着 GPU PhysX。

这里还有一个提交前必须主动披露的覆盖限制：现有正式 checkpoint 的参数是 `num_envs=20`，加载逻辑按 split 顺序取前 20 个不同训练物体，所以实际只覆盖 ball 到 horse 这 20 类，并非 50 类各一个。完整 500 条 test 评测没有泄漏，但训练覆盖不足。若重做正式 PPO，应改为 `--num-envs 50`；现有数字只能称为“20 类训练、50 类测试”的结果。

本项目实际测得 20 env 规模约 2.8 秒/轮，网络很小、CPU 接触仿真占主要时间，所以仅把网络放 GPU 与 CPU 总耗时接近。若要进一步利用 4060 加速物理，需要把 PhysX 切到 GPU pipeline，并把逐环境 `get_actor_*`/接触读取改成 tensor API；这属于性能工程，不改变 PPO 方法本身。

### 7.4 最终结果能说明什么、不能说明什么

54.1% 或 54.3% 表示：在**专家自身成功的测试轨迹**中，Residual RL 有约一半能够在未见物体实例上复现稳定抓取。它证明参考轨迹附近的物理闭环修正比纯 BC 更有效。

它不能证明策略已经学会：

- 只给一个目标物体就自主选择抓取方向；
- 不依赖测试轨迹的专家基线命令；
- 自主规划手腕路径；
- 从相机图像感知物体；
- 直接迁移到真实机器手。

这些边界不削弱当前结论，但必须在报告和答辩中主动说明，避免把“参考轨迹跟踪与修正”夸大成“端到端通用抓取”。

**PPO**：tanh 压缩高斯（σ=0.05）、GAE(γ=0.99, λ=0.95)、4 epoch × 4 minibatch、clip 0.2、target_kl 0.5、lr 3e-4、grad clip 1.0、300 轮。

**学习回路 bug（重要答辩素材）**：初版 KL 早停检查在"下一批更新前"——第一个 minibatch 更新后，后续每个 minibatch 的更新前 KL 都超阈值被拒，**每轮实际只做 1/16 次更新**，且报告的 clip_fraction 恒 0、approx_kl≈0、value_loss 卡在 7–9——"训练在空转但日志看起来像在学"。修复：KL 检查移到更新后测量。修复后 value_loss 6.2→1.0 收敛、clip_fraction 0.38 健康。

## 8. 评测口径（三层，报告必须严格区分）

1. **全量 test（500 条）**：含专家自己失败的轨迹——策略在这些任务上必败。RL 全量：XHand 144/500、Linker 60/500、Wuji旧 151/500、Wuji修复 83/500
2. **专家成功子集**：只在专家重放成功的轨迹上计"策略复现率"——策略学习能力的纯净度量（分母：XHand 257、Linker 81、Wuji旧 295、Wuji修复 190）
3. **训练集 vs 测试集**：RL 训练集 40–45% 曾被误读为"远超旧项目 26%"——**训练集是背答案**；测试集（36–54%）才是泛化。答辩被问"你的 40% 和之前的 26% 怎么比"，答案就在这层区分

例如 XHand 全量 test 是 500 条，其中只有 257 条对应的专家基线自身成功。Residual RL 在全量上成功 144 条，即 28.8%；在专家成功子集上约为 54.1%。前者回答“随机测试任务总体能完成多少”，后者回答“在参考轨迹本身可行时，策略能复现多少”。两个指标都真实，但回答的是不同问题，不能只挑较大的一个报告。

## 9. 已撤销结果的历史分析（不得用于报告）

**统一测试集口径（专家成功子集复现率）**：

| 专家池 | BC 式最优 | Residual RL | 提升 |
|---|---:|---:|---:|
| XHand | 19.1% | **54.1%** | 2.8× |
| Linker | 12.3% | **54.3%** | 4.4× |
| Wuji 旧方法 | 12.9% | **45.4%** | 3.5× |
| Wuji 修复版 | 11.6% | **36.3%** | 3.1× |

**分析**：

1. **RL 全面碾压 BC**（3.1–4.4×）：闭环分布鲁棒性是进阶任务的第一性原理
2. **Linker 受益最大**（4.4×，54.3% 追平 XHand）：6 DOF 欠驱动手最依赖闭环纠错——基础任务里它最弱（193），进阶任务里 RL 几乎抹平差距
3. **Wuji 两池：旧方法 45.4% > 修复版 36.3%**，且三种口径（RL 训练集、RL 测试集、BC 测试集）一致——"反弯轨迹当专家可用"被训练实证支持。**人工视觉标准对仿真成功率没有预测力**，这是本项目最重要的方法学结论
4. **Temporal3 消融**（用户的疑问）：在 PhaseResidual 上加三帧历史，XHand 13.2%（无历史 19.1%）、Linker 6.2%（3.7%）、Wuji 两池基本持平——**相位输入已经提供了"第几步"的时序信息，历史冗余**；只有 6 DOF Linker（观测信息最少）受益。结合旧项目"历史只在最后的 Temporal3 阶段有效"：**历史记忆只在策略基本正确、需要平滑抗噪时有效**
5. **与旧项目 26% 的对比**（诚实口径）：RL 的 XHand 全量 28.8% 与旧项目 Final8 26.15% 同量级，但本设定更有利（专家腕部开环 + 专家基线输入）——报告必须写明这个边界

## 10. 进阶任务的困难清单（答辩故事线）

1. 12 模型全 0 → 31× 跳变诊断 → 增量+相位架构
2. 五个补丁链（§5）——每个都有归因和数字
3. PPO 学习回路 bug——"指标正常但没在学"的经典陷阱
4. 训练集数字误读风险——口径纪律
5. Diffusion+专家腕部仅 1.2%——绝对动作的振荡问题在开环片段下依然存在（Diffusion 输出的是绝对动作片段）
---

# 第三部分：答辩问答预备

> 按"被问概率 × 答不上来的尴尬程度"排序的常见问题与回答要点。

**Q1：为什么用向量而不是绝对关键点？**
A：绝对点匹配假设两只手的中间骨节一一对应，但手的长短/关节数不同，硬凑位置会折出不自然的形状；且"位置对"不保证"功能对"（Wuji v2 误差 2mm 反而抓不住）。向量只匹配"掌心到指尖的方向+长度关系"（按零姿态长度比缩放），把"手长得不一样"这个事实吸收进尺度里。α 混合进一步让"捏合时指尖精确、张开时姿态自然"自动切换。

**Q2：α 混合的 α 是怎么算的？为什么不直接学？**
A：闭式计算：α = clip((0.05 − d)/(0.05 − 0.025), 0, 1)，d 是源手拇指尖到该指尖的距离（单位米）。d ≤ 2.5cm 是捏合、≥ 5cm 是张开。不学的原因：它从输入直接可算，学反而增加参数且不稳定。这个设计来自 AnyDexRetarget 的捏合自适应权重。

**Q3：接触锚为什么只在 6 DOF 有效？**
A：自由度够的手（12/20）向量解本来就能把指尖放在正确位置，强制拉到源接触点反而过约束（XHand 14→8、Wuji 32→28）。6 DOF 的 Linker 指尖放不准，接触锚提供额外的闭合信息（+2）。这是一个"信息补足"与"过度约束"的权衡，由数据决定边界。

**Q4：抓握偏置为什么用 relu 单侧？**
A：relu(target − joint) 只惩罚"弯得不够"，不惩罚"弯得更多"——已经弯够的解不受影响，只把欠闭合的拉向专家闭合剖面。双侧平方会把自然弯得更多的解也拉回来。

**Q5：相位钳位的 bin 怎么定的？会不会限制策略能力？**
A：10 个 bin，每 bin 每维取专家增量 P95 作上限（下限 1e-4）。它限制的是"每步最多动多少"，不是"总共动多少"——总位移由积分决定，不受限。它消灭的是"该静止的阶段乱动"（接近段手指增量≈0）。副作用：它假设专家时序是对的——这正是相位设计的含义。

**Q6：DAgger 里为什么用相位专家当 oracle，而不是学习式教师？**
A：旧项目的历史诊断发现类别教师闭环自身振荡，不能当 oracle。相位专家（专家轨迹按相位对齐）零失稳、确定性，且与我们的相位设计天然一致。这比标准 DAgger 更强（oracle 更可靠），也更容易实现。

**Q7：你们 DAgger 的"上一条命令"bug 到底怎么回事？**
A：在线样本的训练格式需要 (obs, prev_command, phase → delta)。第一版把教师标注的命令当作"上一帧命令"推导 prev（即 prev = 教师命令平移）——这等于假设学生上一帧执行的就是教师命令，**丢了"学生真实执行了什么"这个纠偏信号**，DAgger 退化成普通重放。修复：数据集中独立存学生真实执行命令作为 previous_commands。

**Q8：RL 的 40% 和旧项目的 26% 怎么比？**
A：不可直接比。40% 是训练集（背答案）；26% 是旧项目 Final8 未见物体宏平均。可比的口径：RL 统一测试集全量 28.8%（XHand）——同量级，但我们的设定更有利（专家腕部开环+专家基线输入）。报告里三层口径全部列出，不混用。

**Q9：PPO 学习回路 bug 是什么？怎么发现的？**
A：KL 早停检查放在"更新前"——第一个 minibatch 更新后，后续 minibatch 的更新前 KL 超阈值被跳过，每轮只做 1/16 次更新；且报告指标采自更新前（clip_fraction 恒 0、approx_kl≈0 的假象）。发现途径：value_loss 卡在 7–9 不收敛 + clip_fraction 恒 0 两个"不可能"的指标。修复：KL 检查移到更新后。

**Q10：Wuji为什么曾经保留旧无约束点法？现在最终方法是什么？**
A：旧阶段按当时30 cm口径比较时，无约束点法高于解剖修复版，因此被当作运动学起点；这只说明某些视觉不自然关节姿态在仿真里可能有承力收益。现在最终方法是统一的“运动学初值→Global CEM→Rank-5 CEM→重复确认”，Wuji最终参考成功718/1000、稳定运输662/1000，不能再用581/431解释最终选择。

**Q11：为什么评测要分"专家成功子集"？**
A：test 集里有一部分轨迹专家重定向本身就失败（比如某 ball 轨迹专家只抬 1.3cm）——策略在这些任务上必败。混在一起会低估策略的学习能力。专家成功子集是"策略复现率"的公平分母；全量数字也报告（含"不可能任务"）。

**Q12：层级分解（专家腕部开环）会不会被质疑"不是端到端"？**
A：会，但这是有意的工程决策：腕部轨迹由源轨迹确定，是确定性成分，不该进学习目标（漂移 15cm 的实验证明了学习它的代价）。策略学的是真正需要适应性的部分（手指抓形）。这是"把确定性成分开环、把适应性成分闭环"的层级控制思想，报告中明确标注边界。

**Q13：手型门（解剖约束）到底该不该要？**
A：这是本项目最有意思的开放问题。仿真里"掰手指"是有效策略（+150 条），真机上可能不可行。我们两边都做了（约束版 431、无约束版 581），并用训练实证（Wuji 两池对比）给了一个答案：**在当前仿真评测口径下，无约束更好**。如果评估关注真机迁移，约束版是更安全的选择——报告把两边都摆出来。

**Q14：Temporal3 为什么最后阶段加历史才有效？**
A：历史的作用是"平滑和抗单帧噪声"——前提是策略已经基本正确（旧项目里 Temporal3 在 Online-R1 之后）。早期策略本身是错的，给历史也没用。我们的 PhaseResidual 用相位输入提供了"第几步"的时序锚点，历史与之冗余——所以我们的消融里历史基本无增益（除 6 DOF Linker）。

---

# 附录

## A. 结果速查

**基础任务（最终冻结正式 1000）**：

| 手 | 运动学初值参考成功 | 最终参考成功 | 稳定运输/可训练 |
|---|---:|---:|---:|
| Linker | 222 | **439** | **350** |
| XHand | 565 | **695** | **630** |
| Wuji | 476 | **718** | **662** |

最终目标、评测报告与哈希锁位于：

```text
outputs/reboot_synergy_rank5_formal1000_v1/postconfirmed_rank5_v1/
retargeting/configs/final_retargeting_release_v1.json
```

**已撤销的参考轨迹跟踪结果（不得作为进阶任务结果）**：

| 池 | BC 式 | RL | RL 全量 |
|---|---:|---:|---:|
| XHand | 19.1% | 54.1% | 144 |
| Linker | 12.3% | 54.3% | 60 |
| Wuji 旧 | 12.9% | 45.4% | 151 |
| Wuji 修复 | 11.6% | 36.3% | 83 |

## B. 文件索引

| 文件 | 内容 |
|---|---|
| `METHODS_EXPLAINED.md` | （本文件已合并其全部内容） |
| `HANDOFF_METHODS_PLAN.md` | 方法探索的交接文档 |
| `WORK_LOG.md` | 每日实验记录（最权威的细节来源） |
| `retargeting/MODULE_GUIDE.md` | 基础重定向各模块的输入、输出和作用 |
| `retargeting/run/retarget_*` | 单物体/单方法的核心重定向计算 |
| `retargeting/run/run_*_manifest.py` | 按冻结清单批量生成候选 |
| `retargeting/evaluate/isaac_replay_common.py` | 三手共享的物理初始化、重放与30 cm成功判据 |
| `retargeting/evaluate/replay_*_isaac.py` | 每只手的动作映射和单轨迹物理重放 |
| `retargeting/evaluate/evaluate_hand_manifest.py` | 批量物理评测与微/宏平均汇总 |
| `advanced_policy/observations.py` | 策略一帧能看到的状态和14维形状描述 |
| `advanced_policy/prepare/prepare_policy_dataset.py` | trace筛选、归一化和监督数据生成 |
| `advanced_policy/dataset.py` | 单帧、历史、增量和在线数据如何组成batch |
| `advanced_policy/models.py` | BC、Temporal3、PhaseResidual和Diffusion结构 |
| `advanced_policy/train.py` | 监督学习loss、反向传播、验证与checkpoint |
| `advanced_policy/runtime.py` | 训练后的策略怎样维护历史并输出动作 |
| `advanced_policy/collect_online_phase_residual.py` | 学生闭环访问状态和相位专家标注 |
| `advanced_policy/residual_ppo.py` | Actor-Critic、GAE、PPO裁剪更新 |
| `advanced_policy/train_residual_ppo_general.py` | 三手Residual RL环境、奖励和训练循环 |
| `advanced_policy/evaluate_policy_isaac.py` | 单轨迹闭环策略与Residual RL评测 |
| `advanced_policy/evaluate_policy_manifest.py` | 按valid/test split批量评测策略 |
| `advanced_policy/runs/tensorboard/` | 全部训练曲线（tensorboard） |
| `advanced_policy/runs/policy_eval_summary.json` | 26 组闭环评测汇总 |
| `retargeting/` | 重定向实现（prepare/run/evaluate/test） |
| `advanced_policy/` | 策略实现（数据/训练/运行时/评测） |
| `FINAL_SUBMISSION/` | 旧项目（Shadow 手 BC 管线，26.15%）的提交材料 |

### 推荐代码阅读顺序

如果完全从零开始，不建议先钻进最长的优化脚本。按下面顺序，每一步只回答一个问题：

1. `retargeting/evaluate/linker_replay_utils.py`：12 维 Linker 命令怎样展开成 17 个物理 DOF。
2. `retargeting/evaluate/isaac_replay_common.py`：一条已经生成的轨迹如何执行和判成功。
3. `retargeting/evaluate/replay_linker_isaac.py`：把前两步组合成一个完整场景。
4. `retargeting/run/retarget_linker_vectors.py`：候选轨迹本身如何逐帧求出来。
5. `retargeting/run/run_linker_vector_manifest.py`：一条命令如何扩展到整份 manifest。
6. 再平行阅读 XHand/Wuji 的 `retarget_*` 与 `replay_*`，理解三手差异。
7. `advanced_policy/observations.py` → `dataset.py` → `models.py` → `train.py`：理解监督策略。
8. `advanced_policy/evaluate_policy_isaac.py`：理解训练模型怎样进入真正闭环。
9. 最后读 `advanced_policy/residual_ppo.py` 和 `train_residual_ppo_general.py`：理解最终 RL 方法。

在阅读任何函数时，都用同一个检查表：输入的数组 shape 是什么、输出 shape 是什么、单位是什么、这一步会不会读取未来专家信息、结果是否经过物理验证。

## C. 名词表

| 名词 | 含义 |
|---|---|
| DOF / 自由度 | 能够独立控制的运动变量数量 |
| URDF | 描述机器人link、joint、轴、边界和mesh的结构文件 |
| link | 机器人的一段刚性骨骼 |
| joint | 两个link之间允许运动的连接 |
| FK / 正向运动学 | 已知关节角，计算掌心/指尖等世界位置 |
| 逆运动学 | 已知希望到达的位置，反求关节角 |
| 世界坐标 | 整个仿真场景共享的三维坐标系 |
| 局部坐标 | 相对某个link自身原点定义的坐标 |
| Euler / 欧拉角 | 用roll、pitch、yaw三个角表示旋转 |
| quaternion / 四元数 | 更适合连续计算的四维旋转表示 |
| 重定向 | 把源手动作翻译成目标手动作 |
| 候选轨迹 | 数学优化生成的轨迹（未过物理验证） |
| 专家轨迹 | 物理重放成功抬起的轨迹 |
| episode | 从初始化到结束的一整次抓取尝试 |
| timestep / 物理步 | 仿真器推进一次的离散时间单位，本项目为1/60秒 |
| action / 动作 | 发送给控制器的目标位置命令，不是直接力矩 |
| observation / 观测 | 策略做当前决策时能够读取的状态向量 |
| 开环 | 后续动作只按预定时间播放，不根据执行结果调整 |
| 闭环 | 每一步重新观察环境，再决定下一动作 |
| PD控制 | 根据位置误差和速度产生控制力的带阻尼位置控制器 |
| PhysX | Isaac Gym使用的刚体、碰撞和接触物理引擎 |
| 运输质量 | 末段物体相对手掌不滑移的门 |
| 手型门 | 禁止手指反向弯折的解剖约束 |
| 相位 | 0→1 执行进度 |
| 相位钳位 | 增量限制在专家该阶段最大步长内 |
| DAgger | 自滚状态+专家标注+混合重训 |
| 残差 RL | 专家轨迹上加可学习小修正，PPO 优化成功率 |
| 专家成功子集 | 专家重放成功的轨迹集（复现率的公平分母） |
| mimic | 一电机带多关节的联动 |
| 复合误差 | 闭环误差逐步累积导致分布漂移 |
| McNemar 检验 | 配对二分类显著性检验 |
| SLSQP | 带边界约束的梯度优化器 |
| Huber 损失 | 小误差平方、大误差线性——抗不可达目标 |
| PPO | 近端策略优化（RL 算法） |
| GAE | 广义优势估计 |
| Actor | 根据观测输出动作分布的策略网络 |
| Critic | 估计当前状态未来累计奖励的价值网络 |
| reward / 奖励 | 强化学习对一步行为好坏给出的标量反馈 |
| rollout | 让当前策略在环境里完整执行并收集经验 |
| checkpoint | 保存模型权重、配置和训练状态的文件 |
| CUDA | NVIDIA GPU上的通用并行计算平台，本项目用于神经网络训练 |
| 特权状态 | 仿真中可直接读取、真机上需要额外估计的完整状态 |

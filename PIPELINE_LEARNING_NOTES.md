# 当前方法与实验探索学习笔记

> 这份文档用于理解项目，不是提交版实验报告。它记录当前主线到底使用了什么数据、有哪些网络、每个网络的输入输出、各阶段训练了什么能力，以及一路上做过哪些验证。

## 1. 先用一句话说明当前方案

当前最终模型是一个 **带四类 Task ID 和短时历史的统一行为克隆策略**：

- 当前帧用 DexRep 表示手与物体的空间关系；
- Task ID 告诉网络当前物体属于 Bottle、Mug、Bowl、Camera 中哪一类；
- 前两步的本体状态和动作帮助网络判断当前处于接近、闭合还是抬升阶段；
- 网络最终输出 Shadow Hand 的 28 维动作。

它从 Online-R1 checkpoint 继续训练，但推理时**只运行一个 Temporal3 网络**。Soup、四个类别教师、离线学生和 Online-R1 都是训练过程中的前序节点，不会在最终运行时依次调用。

## 2. 数据到底是怎样划分的

### 2.1 四类物体

本实验只研究四个已经见过的类别：

| 类别 | 训练物体 | 训练轨迹 | 同物体验证轨迹 |
| --- | ---: | ---: | ---: |
| Bottle | 20 | 536 | 137 |
| Mug | 20 | 412 | 103 |
| Bowl | 20 | 341 | 85 |
| Camera | 20 | 437 | 109 |
| 合计 | 80 | 1726 | 434 |

每条预处理轨迹包含70帧观测和动作。原始示范轨迹本身是成功抓取轨迹，但“示范成功”不代表 BC 闭环执行也一定成功：只要网络在某一帧产生小误差，下一帧状态就可能偏离示范，之后误差会继续累积。

### 2.2 三种评测数据

| 数据 | 内容 | 用途 |
| --- | --- | --- |
| 80物体训练轨迹 | 1726条 | 训练类别教师和统一学生 |
| 80物体独立验证轨迹 | 434条 | 检查同一物体上的闭环复现能力；不参与梯度更新 |
| 20个未见物体 | 每类5个 | 都属于上述四类，但具体几何实例没有用于训练 |

20个未见物体又在训练前固定分为：

- 12个开发物体、313条轨迹：可以反复用于选 checkpoint 和做消融；
- 8个最终留出物体、216条轨迹：模型锁定后才做最终三 seed 评测。

这里的“未见”是**未见过的物体实例**，不是未见过的新类别。

验证集并不是“只能使用一次”。开发验证集本来就可以用于反复选模；真正不能继续反馈选模的是已经声明为最终测试集的8个物体。最终集现在已经打开，因此若以后继续研究，应重新划分新的留出物体，而不能根据这8个物体的结果继续调模型再把它们称为最终测试。

## 3. 一条完整的训练主线

### 3.1 训练时的 checkpoint 继承关系

```text
官方 BC
   │
   ├─ 加观测噪声训练多个 BC ── 权重平均 ── BC Soup
   │                                      │
   │                                      ├─ Bottle 类别教师
   │                                      ├─ Mug 类别教师
   │                                      ├─ Bowl 类别教师
   │                                      └─ Camera 类别教师
   │                                                │
   │                         在离线示范状态上生成教师动作标签
   │                                                │
   └──────────────────────────────────────→ 离线 Task-ID 统一学生
                                                    │
                                     学生自己在仿真中运行并查询类别教师
                                                    │
                                               Online-R1
                                                    │
                         R1/R2在线状态 + 离线状态 + 两步状态/动作历史
                                                    │
                                               Temporal3
```

这是一条**训练血缘关系**，不是最终运行时的串联计算图。

### 3.2 最终推理时的真实闭环

```text
仿真当前观测 + 物体类别 + 前两步状态/动作
                    │
                    ▼
             一个 Temporal3 网络
                    │
                    ▼
                28维动作
                    │
                    ▼
               Isaac Gym执行
                    │
                    └────产生下一时刻观测────┐
                                             └─回到网络
```

最终不再调用 Soup、类别教师、离线学生或 Online-R1。

## 4. 所有 BC 网络共用的观测编码器

### 4.1 原始观测为什么是2582维

原始观测由机器人本体信息和 DexRep 几何信息组成。数据加载时按官方规则移除部分速度、力和冗余字段，得到2460维有效输入：

| 输入部分 | 维度 | 直观含义 |
| --- | ---: | --- |
| `prop` | 100 | 手腕、手指关节等机器人本体状态 |
| `dexrep_sensor` | 1080 | 手与物体之间的空间/距离感知特征 |
| `dexrep_pnl` | 1280 | 物体点云相关的局部几何特征 |
| 合计 | 2460 | 当前帧送入编码器的观测 |

### 4.2 2460维如何变成384维

三部分并不是直接送进大 MLP，而是分别经过一个线性编码层：

```text
100维本体状态 ───────── Linear ─→ 128维
1080维DexRep sensor ── Linear ─→ 128维并归一化
1280维DexRep PNL ───── BN+Linear → 128维并归一化
                                  │
                                  └─拼接为384维当前状态表示
```

因此这里的“特征提取”不是 CNN、Transformer 或一个神秘的预训练大模型，而是官方 DexRep 策略中的三个线性投影与归一化。三个128维向量拼接得到384维。

## 5. 各个网络分别是什么

### 5.1 BC Soup

Soup 不是新结构，也不是推理时同时运行多个网络。它把两个相同结构、不同训练 seed 的噪声 BC 参数按2:1做逐参数平均：

```text
θ_soup = 2/3 θ_seed2025 + 1/3 θ_seed2026
```

这样做的目的是降低单个 seed 在接触动力学中的偶然偏差。Soup 仍然是一个普通单帧 BC 网络：

```text
384 → 1024 → 1024 → 512 → 512 → 28
```

它主要作为后续类别教师和统一学生的初始化。2:1不是理论最优比例，而是早期小规模筛选得到的工程选择，因此 Soup 的结果应理解为稳健初始化，而不是方法创新的核心。

### 5.2 四个类别教师

四个类别教师结构完全相同，但每个网络只用一个类别的20个训练物体继续训练：

```text
当前2460维观测 → 384维编码 → MLP → 28维教师动作
```

它们的作用是：

- Bottle 教师提供 Bottle 状态下的动作标签；
- Mug、Bowl、Camera 同理；
- 教师只在训练学生时使用。

当前固定的标准教师来自20物体普通采样训练，checkpoint 分别为 Bottle 第30轮、Mug 第10轮、Bowl/Camera 第40轮。这里确实做过开发集 checkpoint 选择，但没有把后来只对某一类有利的“物体均衡专家”事后拼进教师池，也没有按具体物体选择不同教师。

需要区分两种“路由”：

- **生成标签时的硬路由**：程序看到 Mug，就调用 Mug 教师；
- **最终学生推理**：没有硬路由到四个网络，只有一个共享学生。

### 5.3 离线 Task-ID 统一学生

离线学生在384维特征后增加4维 one-hot：

```text
Bottle = [1,0,0,0]
Mug    = [0,1,0,0]
Bowl   = [0,0,1,0]
Camera = [0,0,0,1]

[384维当前特征; 4维Task ID]
             │
             ▼
388 → 1024 → 1024 → 512 → 512 → 28
```

Task ID 不会自动“选中某个子网络”。它只是额外输入，网络在训练中学习类似下面的规律：

```text
相似几何状态 + 不同Task ID → 可以输出不同动作
```

也就是说，这是网络学出来的条件响应，不是 if/else 开关。为保持从 Soup 初始化时动作完全不变，新增加的4列输入权重初始为0，之后通过训练变成非零。

离线学生在原示范状态上学习四个教师的动作。我们比较过：

- 100%教师动作；
- 70%教师动作 + 30%原示范动作。

开发集最终选择100%教师、第15轮。离线学生不是用来“生成专家没有覆盖的新数据”，它首先完成的是**把四个教师压进一个统一网络**；之后它还会作为第一轮在线数据采集的执行者。

### 5.4 Online-R1

Online-R1 与离线 Task-ID 学生的网络结构完全相同，仍然是388维输入的单帧网络。变化发生在训练数据，而不是网络结构。

第一轮在线模仿过程如下：

1. 离线学生控制手在80个训练物体上运行；
2. 每个物体使用4条训练轨迹，共320个 rollout；
3. 每一步保存学生实际访问到的状态和学生执行动作；
4. 根据物体类别调用相应教师，在这个状态上产生教师动作；
5. 得到 `22080 = 80 × 4 × 69` 个在线监督样本；
6. 用这些样本继续训练学生，得到 Online-R1。

训练批次的期望组成是：

```text
75% 原离线状态 + 25% 学生在线状态
```

同时四个类别各占约25%。这里的25%是通过采样器控制的**在线样本占比**，不是“离线学生只起25%的作用”，也不是教师/示范损失比例。

Online-R1 的核心能力是：不仅在“正确示范轨迹上的状态”输出动作，还学习在“自己已经走偏的状态”上如何接近教师。这正是缓解 BC 闭环分布偏移的 DAgger 式思想。

### 5.5 Online-R2

Online-R1 又在每个训练物体的另外4条、不与R1重叠的轨迹上执行，产生第二批22080个在线状态。R1与R2合计44160个在线样本。

直接训练得到的 Online-R2 checkpoint 没有超过 Online-R1，所以没有成为主线模型。但 R2 数据仍提供了“更强学生在另一批轨迹上访问的状态”，因此 Temporal3 训练时使用了R1+R2聚合数据。

这意味着：

- Online-R2 **模型**被淘汰；
- Online-R2 **采集的数据**被保留；
- Temporal3 仍从更好的 Online-R1 checkpoint 初始化。

### 5.6 最终 Temporal3

Temporal3 在 Online-R1 基础上加入短时历史。名字中的“三帧”具体指：

```text
当前帧 t + 前两帧 t-1、t-2
```

当前帧仍使用完整的2460维观测并编码为384维；历史只保留前两步的100维本体状态和28维动作：

```text
当前状态编码                         384维
Task ID                                4维
前两步本体状态            2 × 100 = 200维
前两步已执行动作           2 × 28  =  56维
-------------------------------------------
最终送入动作MLP                       644维
```

网络为：

```text
644 → 1024 → 1024 → 512 → 512 → 28
```

历史是直接拼接进 MLP 的，没有额外 GRU、LSTM 或 Transformer。训练时：

- 离线轨迹的历史动作来自示范动作；
- 在线轨迹的历史动作来自当时学生实际执行的动作；
- 推理时历史动作来自 Temporal3 自己前两步输出并执行的动作；
- episode 开始时，历史本体状态用首帧填充，历史动作置零。

它能培养的能力不是长期规划，而是短时间内根据状态变化和刚执行的动作判断运动趋势。例如，同一当前姿态可能既是“正在闭合”也可能是“已经闭合准备抬升”，单帧不容易区分，短历史可以提供这个阶段线索。

为什么最终选3帧，不是凭经验固定的：单帧 Online-R1、连续3帧和连续7帧做过同协议比较，3帧明显优于单帧，7帧反而下降。更长或更稀疏历史也没有稳定改善，因此3帧是当前数据和训练条件下的实验选择，并不代表所有灵巧手任务的最优历史长度。

## 6. 最终模型的训练目标与超参数

### 6.1 28维动作损失

动作由6维手腕控制和22维手指关节控制组成。基础动作损失直接继承官方 BC：

```text
L_action =
    2 × MSE(手腕位置3维)
    + MSE(手腕姿态3维)
    + MSE(手指22维)
    + L1(全部28维)
```

手腕位置 MSE 权重为2是官方代码原有设置，不是本项目后来修改的。我们自定义的是教师动作与示范动作之间的外层混合：

```text
L = α L_teacher + (1-α) L_demo
```

最终 Online-R1 和 Temporal3 使用 `α=1.0`，即100%教师目标；70/30、50/50、30/70和纯示范都作为消融测试过。

### 6.2 最终关键设置

| 设置 | 最终值 | 来源 |
| --- | ---: | --- |
| 当前观测 | 2460维 | 官方 DexRep 处理 |
| 当前编码 | 384维 | 官方3×128编码 |
| Task ID | 4维one-hot | 本项目增加 |
| 历史 | 当前+前2步 | 本项目消融选择 |
| Temporal3动作输入 | 644维 | 384+4+2×(100+28) |
| 隐藏层 | 1024-1024-512-512 | 官方 BC 结构沿用 |
| 输出 | 28维动作 | 官方动作空间 |
| batch size | 128 | 官方配置沿用 |
| 学习率 | `2e-5` | 在线/时序阶段的稳定微调值 |
| 训练轮数 | 4 | 第4轮最佳，继续到第8轮未提升 |
| 本体观测噪声 | 均匀噪声±0.05 | 早期鲁棒 BC 搜索 |
| 在线样本比例 | 25% | 自然占比与25%对照后选择 |
| 类别采样 | 四类期望等概率 | 防止轨迹多的类别主导 |
| 教师/示范目标 | 100% / 0% | 时序目标消融后保留 |

最终 checkpoint 内约有506万参数，但其中包含官方 `ActorCritic` 类遗留、BC 推理不用的 critic 分支。真正产生动作的“编码器+actor”约283万参数。因此不能简单把506万都理解成推理动作网络的有效容量。

网络宽度1024-1024-512-512是沿用官方基线，并没有完成系统性的宽度搜索；所以不能声称网络规模已经全局最优。现有证据只能说明：在当前容量下，加入正确的在线状态和短历史，比继续堆复杂时序模块更有效。

## 7. 最终模型是怎样一步步训练出来的

| 节点 | 初始化 | 训练输入 | 监督目标 | 主要学到的能力 |
| --- | --- | --- | --- | --- |
| 类别教师 | BC Soup | 本类别示范状态 | 成功示范动作 | 类别专门动作规律 |
| 离线Task-ID | BC Soup | 四类示范状态+Task ID | 对应类别教师动作 | 把四个教师压入一个网络 |
| Online-R1 | 离线Task-ID | 75%离线状态+25%学生访问状态 | 对应类别教师动作 | 处理自己造成的偏离状态 |
| Temporal3 | Online-R1 | 离线状态+R1/R2在线状态+短历史 | 对应类别教师动作 | 利用短时运动趋势判断阶段 |

一个重要限制是：当前类别教师仍是单帧 BC。Temporal3 虽然有历史，但监督标签仍由单帧教师给出，所以学生能利用历史“决定在什么状态下如何映射”，却没有真正的时序专家提供更强恢复动作。这可能是当前成功率上限的重要原因。

## 8. 评价指标到底表示什么

### 8.1 官方成功率

我们没有改官方成功标志。每条轨迹只要在 episode 任意时刻触发官方 `successes`，就记为一次峰值成功。官方代码检查物体是否进入目标附近或达到目标高度，并排除明显飞出工作区的情况；目标在初始物体上方约30 cm。

因此更准确的说法是“物体曾到达官方目标区域/高度”，而不是代码显式检查了“手指保持抓住并稳定30 cm”。正常情况下完成目标通常需要抓取抬升，但成功标志本身没有单独判断接触是否稳定。

### 8.2 总体成功率与物体宏平均

- 总体成功率：所有成功轨迹数 ÷ 所有轨迹数；
- 物体宏平均：先算每个物体成功率，再对物体平均。

由于不同物体的轨迹数不同，主要指标采用物体宏平均，避免轨迹多的物体占更大权重。

### 8.3 辅助指标

- 平均最大抬升：每条轨迹相对初始高度的最大值；
- failure rate：稠密诊断中出现失败条件的轨迹比例；
- reward：只用于残差 PPO 训练诊断。

最终主模型是模仿学习，训练目标是动作损失，不直接优化 reward。报告里的 reward 曲线来自失败的残差 PPO 实验，不能把它当成 Temporal3 的训练 reward。

## 9. 为什么最初成功率那么低

最关键的诊断是：

| 状态来源 | BC动作MAE |
| --- | ---: |
| 专家示范状态 | 0.0072 |
| BC自己闭环访问的状态 | 0.2648 |

闭环状态误差约为专家状态的36.8倍。按阶段看，误差放大倍数约为：

- 接近：13.4倍；
- 预抓取：46.0倍；
- 闭合：68.1倍；
- 抬升：71.3倍。

这说明原始示范数据并不是失败轨迹，问题也不只是“训练轮数太少”。网络在示范状态上拟合得很好，但一旦自己执行产生偏差，后续状态就离开训练分布。灵巧手接触对毫米级位置和手指闭合时机很敏感，因此离线动作误差会被物理接触快速放大。

这也解释了为什么：

- 离线 validation loss 最低的 checkpoint 不一定闭环成功率最高；
- 单纯继续训练更多 epoch 可能让 loss 更低，却不提高成功率；
- 在线模仿比单纯扩大网络或继续训练更有效。

## 10. 主线每个节点实际贡献了多少

在固定16个训练物体、495条轨迹上，使用三个仿真 seed 对整条训练血缘进行串行审计：

| 节点 | 物体宏成功率 | 相对前一节点 |
| --- | ---: | ---: |
| BC Soup | 26.10% | — |
| 四类教师池 | 24.94% | -1.16个百分点 |
| 离线Task-ID学生 | 22.81% | -2.14个百分点 |
| Online-R1 | 29.56% | **+6.75个百分点** |
| Temporal3 | 31.12% | **+1.56个百分点** |

这个结果说明：

1. 类别专门训练和统一蒸馏并没有自动提高闭环成功率，反而发生了能力损失；
2. 离线统一学生仍然必要，因为它提供单网络初始化和在线采集执行者；
3. 最大的有效改进来自学生访问状态上的在线模仿；
4. Temporal3 在此基础上提供进一步、较小但稳定的提升。

所以不能把当前成果概括成“类别专家比共享 BC 更强”。更准确的结论是：**类别教师提供了可查询的动作监督，但真正提高统一学生闭环表现的是在线状态覆盖和短时历史。**

## 11. 一路上做过的主要探索

这一节不只列“成功的模型”，而是按实际时间顺序复盘问题是怎样一步步被定位的。失败实验同样重要，因为它们排除了“只是训练轮数不够”、“只是reward没调好”或“只是网络不够复杂”等直觉解释。

### 11.1 先区分几套评测协议

项目中间更换过数据规模，所以不能把不同日期的百分比直接排序。这些结果分别回答不同问题：

| 阶段协议 | 大致规模 | 主要用途 |
| --- | --- | --- |
| 初期固定轨迹 | 16物体×10条=160条 | 快速对比Warm-start、Scratch、BC和残差PPO |
| 初期独立留出 | 16物体共100条 | 噪声BC、Soup、早期类别教师和PPO对比 |
| 三个专项物体 | 93条训练+24条同物体留出 | 检查单物体专家、检索和参数插值是否过拟合 |
| 扩展开发集 | 12个未见实例、313条 | 最终Task-ID、Online-R1/R2、Temporal3和时序消融选模 |
| 主线训练集审计 | 16个训练物体、495条、3 seed | 定量比较Soup→教师→离线学生→Online-R1→Temporal3 |
| 最终留出 | 8个未见实例、216条、3 seed | 模型锁定后的最终泛化报告 |

因此，例如早期的“16/100”和后来的“37.42%”不在同一组物体上，不能理解为单纯从16%涨到37%。真正可直接比较的数字，都会在文中标明共用对照和协议。

### 11.2 阶段A：把官方Demo、数据和指标链路跑通

#### 为什么先不急着训练

刚开始最大的风险不是模型效果，而是“我们到底在评什么”。因此先后验证了：

1. Isaac Gym、PyTorch、CUDA和官方checkpoint能正常加载；
2. 轨迹、mesh、物体尺度和DexRep特征能对齐；
3. 训练、验证、checkpoint恢复、曲线导出和渲染链路都能独立工作；
4. 自定义稠密reward与官方success分开记录，不用reward替换挑战指标；
5. 自定义代码放在`custom_tools/`，官方文件恢复为Git基线。

官方BC在当时小规模Demo上只有约2.5%～7.5%成功。这说明环境已经跑通，但官方权重并不会自动适配我们选择的多物体数据。

#### “预处理后只保留12/20”的误解

早期一个新Bottle预处理后只保留12/20，一度看起来像“源轨迹只有60%成功”。后来逐轨迹回放发现：

- 20/20都曾经达到官方成功并抬升约30 cm；
- 8条在后续时刻越界，导致末帧`successes`被清零；
- 因此12/20是“末帧保留率”，不是轨迹的峰值成功率。

这一诊断也是后来统一使用“episode内曾经成功”峰值指标的原因。我们没有改官方成功判定，只是避免用末帧状态误解整条轨迹。

#### 8 GB显存下的预处理与评测稳定性

扩展数据时遇到的OOM并不主要是BC网络太大。出错时PyTorch只占几十MiB，大量显存在PhysX和Isaac Gym中。最终的处理逐步演变为：

```text
同进程多物体
  → 每10条轨迹分块
  → 每物体新CUDA子进程
  → 每分块新进程
  → 出错时按8→4→2→1条轨迹递归降级
```

已完成块采用原子保存，因此某个Camera网格触发非法显存访问时，前面结果不会丢失。评测同样改为逐物体独立进程、可断点和可重试。这些是工程准备结论，不是算法改进。

### 11.3 阶段B：Warm-start、Scratch和第一版残差PPO

#### Warm-start与Scratch回答了什么

两组使用相同的395条训练轨迹、seed和21600次更新：

| 初始化 | 最终训练loss | 最终验证loss | 160条闭环成功 |
| --- | ---: | ---: | ---: |
| 官方权重Warm-start | 0.0088 | 0.0300 | 12/160=7.5% |
| 随机Scratch | 0.0100 | 0.0310 | 12/160=7.5% |

总成功数相同，但Warm-start四类都有成功，Scratch的Camera为0。所以官方权重有助于初始类别覆盖，却不足以解决闭环问题。另一个重要现象是：两者离线loss很接近，闭环类别表现却不同，说明离线MSE无法反映接触结果。

#### 为什么最初选“BC+残差PPO”

直接用PPO从头探索高28维灵巧手动作很难，因此冻结BC，只让PPO学一个有界残差：

```text
实际动作 = BC动作 + 小范围残差
```

并加入特权critic、分类reward、KL早停和类别内advantage归一化。这些数学与工程检查都通过，零残差也与纯BC严格对齐，所以后来的失败不是因为评测器悄悄改了BC动作。

第一次1/3/5帧残差PPO消融曾经出现reward坍塌。检查后发现是局部重置没有等物体落稳，造成“隔步假掉落”。那批结果作废，加4个中性稳定控制步后重跑，1/3/5帧分别为0/28、3/28、0/28。这个“3帧PPO历史”是早期RL观测设计，不等于后来最终的Temporal3 BC。

#### Reward是怎样被排查的

在相同课程上做了多个单变量实验：

| 修改 | 想验证的假设 | 结果 |
| --- | --- | --- |
| 正式训练300轮 | 短测3/28是否会随训练累积 | 最佳第150轮1/28，最终0/28；reward上升但success不上升 |
| 探索噪声0.25→0.15 | 大随机残差是否破坏精细抓取 | 三次验证仍0/28，排除它是主因 |
| 接近权重10→5 | 策略是否只学会靠近 | 最佳2/28，折中方向略有改善 |
| 抬升权重40→80 | 高度奖励是否太弱 | 只1/28，负高度变化也被放大，恢复40 |
| 只奖励历史最高度进步 | 避免上下抖动刷reward | 仍1/28，且接近项重新主导 |

在16物体扩展PPO中，独立100条上BC从2/100变为PPO 8/100，但另一组严格固定160条上反而由13/160降到8/160，尤其丢失Bowl。这意味着PPO确实学会了某些新成功，同时也破坏了原BC技能，存在明显负迁移。

### 11.4 阶段C：低成功率的根因诊断

#### 先排除“源示范是失败的”

在PPO实际使用的32条训练起点上：

- 32条源示范按官方口径全部成功；
- Warm-start BC只成功1/32，平均最大抬升2.36 cm；
- 残差PPO第150轮为0/32、1.28 cm，还丢失了BC唯一的成功。

因此低成功率不能归因于“选到了失败示范”。

#### 教师状态与学生状态对照

用同一BC分别在两类状态上计算动作误差：

| 输入给BC的状态 | 动作MAE |
| --- | ---: |
| 成功示范轨迹中的专家状态 | 0.0072 |
| BC自己控制后访问的闭环状态 | 0.2648 |

后者是前者的36.8倍。阶段放大倍数为接近13.4、预抓取46.0、闭合68.1、抬升71.3，而手指误差在预抓取阶段已开始明显增大。

这给出了项目中最重要的诊断：

```text
不是BC不会拟合示范状态
        而是
BC的小误差改变了接触 → 下一帧进入未见状态
      → 开环示范不再提供正确恢复经验 → 误差累积
```

所以后续主线才从“继续调reward”转向“对观测加噪声”、“学生状态上查询教师”和“增加短时历史”。

### 11.5 阶段D：噪声BC与Model Soup

#### 为什么观测噪声有效

只对100维本体状态加均匀噪声，等价于告诉网络：“即使手腕或关节位置与示范有小偏差，也应输出接近的纠正动作。”它不会真正生成学生闭环状态，但是最低成本的局部状态扩增。

±0.02噪声与原BC使用相同初始化、数据、seed和100轮训练：

- 160条固定轨迹由13/160提高到25/160；
- 100条独立验证由2/100提高到16/100；
- 四个类别都出现成功；
- 闭环动作MAE下降7.9%，其中手腕位置误差约降31%；
- 干净验证loss反而更差，说明鲁棒性和离线拟合存在权衡。

#### 噪声幅度和训练seed

后来扫描±0.01/0.02/0.03/0.05和多个epoch。严格逐物体重复评测中：

| 候选 | 三轮成功数 | 平均宏成功 |
| --- | --- | ---: |
| ±0.05 epoch40 | 19/18/20 | 21.01% |
| ±0.03 epoch100 | 平均16.67/100 | 次佳 |
| ±0.02 epoch100 | 平均14.33/100 | 16.89% |

但更换训练seed后，±0.05只在3个seed中的2个胜过±0.02。不同seed的离线loss很接近，闭环结果却差异很大。因此可以说强噪声是有价值的鲁棒化方向，但不能声称±0.05对所有seed都最优。

#### Soup解决的是什么

为减小seed方差，尝试了等权双模型、三模型和偏向最佳源模型的2:1权重平均。等权和三模型平均明显较差，说明过度平均会把灵巧手精细动作抹平。2:1 Soup的三轮结果为25/24/23，平均24/100、宏成功22.44%，高于最佳源模型平均19/100。

Soup对Bottle、Mug、Camera改善，Bowl下降；平均最大抬升也略降。因此它是一个更稳定的初始化，不是每类都更强的绝对冠军。

### 11.6 阶段E：为什么多版残差PPO都没有进入最终主线

发现残差PPO会丢失BC已有成功后，不是立即放弃RL，而是逐步增加保护机制。

#### 门控残差

门控网络学习每类动作应该使用多大残差。与无门对照相比：

- 无门只保留3条BC成功，新增8条，丢失13条；
- 门控保留12条，新增4条，丢失4条；
- 门控第50轮为16/100，与纯噪声BC成功数持平，但抬升更低、失败率更高。

结论是门控确实减少了技能破坏，但没有创造净增益。

#### 成功锚点约束

从Soup在训练轨迹上真实成功的起点中建立64轨迹课程，其31条为成功锚点。对锚点加入“残差应该小”和“门应该小”的损失，希望只在失败轨迹上学纠错。

锚定第50轮单次达24/100，但与同期Soup重复三轮后：

| 模型 | 平均成功 | 宏成功 |
| --- | ---: | ---: |
| Soup | 24.00/100 | 22.49% |
| 锚定PPO | 23.00/100 | 22.27% |
| 无锚定PPO | 22.33/100 | 22.22% |

锚点把失败率降到约5.30%，却仍未提高官方成功率。这表明“保护原策略”和“学会新的有效纠错”是两件不同的事。

#### 阶段门控reward

训练后期接近reward一度占绝对值约86%，所以尝试“形成接触后关掉接近项”。两接触阈值太严，只在前几轮触发；改成一接触后，前4轮有12次训练成功，但之后触发率和成功再次归零，评测只22/100。因此不再继续调reward阈值。

综合这些实验，RL分支的问题不是“PPO没有跑通”，而是当前reward和有限成功课程无法产生可泛化的纠错策略。

### 11.7 阶段F：第一代“类别教师→统一学生→在线模仿”

这是扩展到最终80个训练物体之前的第一代验证，数据仍是早期16物体。它的价值是先验证整个想法可行，后来被更大、更规范的Task-ID主线取代。

#### 类别教师

从Soup低学习率微调四个类别BC。当时最佳固定教师池为Bottle/Bowl/Camera专家+Mug Soup，两次重复为37/100和34/100，均值35.5/100，明显超过Soup 24/100。

但这个“Mug使用Soup”是看到类别结果后的人工组合，方法学上不够整洁。所以后来扩展主线明确禁止这种事后混用，四类都使用同一训练规则的标准类别教师。

#### 第一代统一学生和在线聚合

| 节点 | 代表结果 | 学到的经验 |
| --- | ---: | --- |
| 离线统一学生 | 30/100，宏32.24% | 多教师蒸馏到单网络有能力损失 |
| 在线R1 | 三轮31/32/32，宏34.57% | 学生状态上的教师标签能稳定改善统一学生 |
| 在线R2 | 均值29.67，低于R1 | 师生MAE降低不代表成功率必然提高 |

R1共采集27255个有效学生状态，师生MAE从接近阶段0.0056增到抬升阶段0.0145，与前面的闭环诊断一致。R2后MAE再降约15%，但成功率反而下降，提醒我们：教师也不是完美的闭环oracle，“更像教师”不总等于“抓得更好”。

#### 噪声累加bug和有限超参搜索

当时发现Dataset对NumPy视图原地加噪声，有放回采样时同一样本可能被重复累加。修复为每次先复制干净观测再加独立噪声后，成功数基本不变，但抬升和波动改善，说明修复是正确的，也没有伪造一个新方法增益。

后续搜索了教师比例、学习率、在线比例和噪声。85%教师在seed2025提高约4.33/100，但seed2026增益只有1/100。再做L16正交多seed搜索时，验证集冠军在新几何物体上反而不如更稳定的第2组；但第2组也没超过旧学生和教师池。这排除了“只需要更大网格搜索就会大幅提升”的期望。

### 11.8 阶段G：单物体专家、阶段检索和参数插值

当时未见集约20%的结果明显不足，因此暂停报告，专门检查“对少数训练物体能否做到高成功”。选了Bottle、Mug、Camera三个数据充足物体：

| 物体 | 专家动作回放 | 现有策略 |
| --- | ---: | ---: |
| Bottle | 31/32 | 14/32 |
| Mug | 31/31 | 8/31 |
| Camera | 28/30 | 14/30 |

这证明物理初始状态和轨迹本身具有很高可实现上限，问题在策略学习。

#### 单物体神经BC

专项微调后，Bottle/Mug/Camera训练轨迹分别提高到21/32、13/31、15/30，但同物体留出轨迹分别由4/8→1/8、5/8→4/8、4/8→4/8。即使物体几何不变，换一批抓取方向也会明显退化。

又做了真正冻结DexRep编码器的对照。冻结能略微减缓遗忘，但仍不超过微调初始策略，说明过拟合不只是编码器漂移，动作头本身也在小数据上过拟合。

#### 阶段对齐检索

这一分支的动机是：MSE BC可能对多种合理抓取动作取平均，而近邻检索可直接找一个当前阶段最接近的示范动作。逐帧`k=1`检索在训练轨迹上达到：

```text
Bottle 32/32，Mug 31/31，Camera 29/30，合计98.9%
```

但留出只有3/8、4/8、6/8，合计54.2%。Camera超过BC，Bottle和Mug反而下降。固定整条参考轨迹也没更好，因为一开始选错参考后会整段被带偏。

所以检索的98.9%说明示范库在训练起点上很强，不能说明它已经学到泛化抓取。而且按物体选择“Bottle/Mug用BC、Camera用检索”是看过留出结果后的人工路由，不能作为统一方法的无偏证据。

#### Checkpoint参数插值

把单物体专家按25%/50%/75%插值回微调前策略，希望保留泛化能力。一次评测中的按物体最佳组合看似将训练成功由29/93提高到44/93，留出保持13/24；但三轮复测后留出均值降到11/24。这是典型的“单次Pareto改进被复测否定”，因此整体放弃。

三个专项实验最终的作用是：证明普通单帧BC可以记住某些物体和起点，但对新抓取方向的泛化很差；人工检索和插值可以得到局部高分，却不是可靠的统一策略。

### 11.9 阶段H：扩展到每类20个物体

之前的类别教师只看到每类4个左右的物体，因此“教师不够强”可能只是数据不足。为此审计完整GraspM3，四类共找到803个原始物体，建立每类4/10/20的嵌套训练集、每类5个未见物体和备选库。

预处理后对有效轨迹过少的物体，只按同类几何最近邻选替换候选，不使用任何策略成功率信息。最终得到：

- 每类20个训练物体，共80个；
- 1726条训练轨迹和434条同物体验证轨迹；
- 12个开发物体和8个最终物体在训练前固定。

#### 数据量曲线给出了什么结论

在12物体开发集、多epoch、3 seed的156组评测中：

| 候选 | 宏成功 |
| --- | ---: |
| Soup初始策略 | 17.05% |
| 统一规模最佳：每类20物体、第10轮 | 20.76% |
| 按类别事后挑规模/epoch的理论组合 | 25.74% |

Mug和Bowl随数据增加明显改善，Camera仍只约14.33%；Bottle甚至仍是小规模专家更好。所以增加数据有帮助，却不是单调必然改善。“25.74%”只是分析上的按类选择，不能直接当成一个事先固定方法。

#### 物体均衡采样为什么没保留

同类不同物体有10～32条训练轨迹，普通帧等权会让轨迹多的物体占更大梯度。实现物体等概率采样后，只有Mug改善，Bottle/Bowl/Camera都下降。这说明原始轨迹数也可能表示某些有用的抓取多样性，不能一概抹平。

最终教师池因此统一使用“20物体、普通采样”的四个类别专家，不把只对Mug有利的物体均衡模型事后混入。

### 11.10 阶段I：显式Task ID的统一学生

审计旧统一学生后发现，它并没有显式输入Task ID，与原本想模仿的“任务条件统一学生”不完全一致。因此在384维DexRep编码后加4维one-hot，并将新列权重初始为0，确保加载Soup时初始动作严格不变。

四个标准20物体教师在1726条示范状态上生成120820帧标签。两个离线学生都训练20轮：

| 监督 | 开发集最佳checkpoint | 宏成功 |
| --- | --- | ---: |
| 100%教师 | 第15轮 | 21.92%；三seed 22.06±0.22% |
| 70%教师+30%示范 | 第15轮 | 21.21% |

70/30的离线validation loss全程更低，闭环宏成功却不如100%教师。而loss在第4～5轮最低，闭环最佳在第15轮。这再次证明：既不能只用离线loss选checkpoint，也不能默认加入原成功示范就一定提高闭环能力。

三seed对照中，固定教师池约23.48±0.69%，离线学生22.06±0.22%，蒸馏损失约1.43个百分点。Task ID让一个网络有了区分四类任务的条件，但它不会自动消除蒸馏容量损失。

### 11.11 阶段J：Online-R1为什么是最大的有效提升

离线Task-ID学生在80个训练物体上每体执行4条训练轨迹，每条69个有效控制步，得到22080个学生访问状态。四类各5520个，无缺失、重复或非有限值。

采集数据中：

- 前10帧师生动作MAE约0.0062；
- 末9帧增至约0.0322；
- Camera和Bowl的误差最高。

这次在线采集直接把前面的闭环诊断变成了训练数据：学生在哪里走偏，就在那些状态上查询教师。

在线数据的自然帧数占比只有15.5%，因此对比自然采样和25%在线重采样：

| 训练方式 | 开发集最佳宏成功 |
| --- | ---: |
| 在线样本自然占比 | 27.89% |
| 在线样本固定25% | 33.29% |
| 离线Task-ID学生 | 21.92% |

25%第2轮为95/313、平均抬升16.06 cm，12个物体中9升1平2降。三seed复测为32.80±0.86%，成功数95/95/91，稳定超过离线学生和教师池。

这个提升的意义大于“某个超参更好”：它在因果上对应了之前的36.8倍闭环误差。数据分布被修正，比继续降低示范状态loss更有用。

#### 为什么Online-R2没有继续提升

R2用Online-R1在每个物体的另4条训练轨迹上采集，与R1的320对“物体—轨迹”零重叠，又得到22080状态。师生总MAE由0.02042降至0.01591，后19帧由0.03145降至0.02394，看起来学生更像教师。

但R2最佳第1轮宏成功31.67%，低于R1的33.29%，抬升也由16.06降至14.61 cm。因此停止第三轮。关键结论是：

```text
师生动作MAE更小 ≠ 闭环成功率一定更高
```

可能原因是单帧类别教师自身也只在示范附近可靠，对学生的复杂偏离只会给出局部小修正。R2 checkpoint被淘汰，但R2状态覆盖了另一批起点，所以后来作为Temporal3的训练数据保留。

### 11.12 阶段K：Temporal3为什么进入最终主线

单帧观测可能无法区分“刚开始闭合”、“已经接触”和“应该抬升”。Temporal3从Online-R1初始化，仅加入前两步本体状态和实际动作，不改教师、当前DexRep和动作空间。

第4轮在同一开发集达107/313、宏37.42%、抬升17.64 cm，相对Online-R1的33.29%明显提高。Bowl由20.71%升到40.76%，Camera也提高，但Bottle由51.92%降至42.76%，说明历史不是对每类都有相同作用。

三seed复测中：

| 模型 | 宏成功 | 成功数（3次合计） | 平均抬升 |
| --- | ---: | ---: | ---: |
| Online-R1 | 32.80±0.86% | 281/939 | 15.93 cm |
| Temporal3 | **37.63±1.02%** | **321/939** | **17.80 cm** |

提升跨seed存在，且不只是某一次PhysX波动，所以按预设的四类宏平均指标锁定Temporal3。

### 11.13 阶段L：Temporal3之后的结构与训练消融

下表中“开发对照”主要是12物体313条协议；部分昂贵候选用4→8→12物体逐级淘汰，因此只能与同轮配对对照比，不能把39.38%与37.42%当成不同全集的绝对差。

#### A. 历史长度与采样位置

| 候选 | 结果 | 为什么没替换Temporal3 |
| --- | --- | --- |
| 连续7帧 | 35.00% vs 37.42% | 额外输入增加冗余，Bowl降至29.22% |
| 稀疏3帧 `[t-6,t-3,t]` | 37.428% vs 37.422% | 总体只差0.006点，Bowl由40.76%降至28.50% |
| 多尺度5帧 `[t-6,t-3,t-2,t-1,t]` | 35.52% | 长短时域拼接没有自动学会有效融合 |
| 继续训练第5～8轮 | 最佳第7轮36.74% | 排除“第4轮还没训够”，后续有退化 |

三帧并不是凭经验拍板，而是在相同输入方式下比较后，在短时运动信息和学习难度之间取得的折中。

#### B. 更复杂的时序网络

| 候选 | 控制变量 | 结果 |
| --- | --- | --- |
| 轻量注意力残差 | 冻结Temporal3的5061561参数，只训2层4头128维attention和零初始动作修正头 | 最佳37.12%，对照37.42%；抬升降0.58 cm、失败率升约3点 |
| 历史完整DexRep+GRU | 共享原DexRep编码器，GRU隐层128，新动作残差头零初始 | 第2轮8物体30.78%，配对继续Temporal3为38.59%；Mug由46.49%降至19.15% |
| GRU第1轮补审 | 排除第2轮已过拟合 | 4物体19.75%，仍低于同轮Temporal3 24.78% |

注意力分支是一次轻量Transformer式尝试，GRU是完整历史观测尝试。它们说明“时序网络更复杂”不等于“更容易学到接触阶段”。当标签仍来自单帧教师时，新时序支路甚至可能破坏原有策略。

我们没有完成一个与Temporal3公平、可报告的完整Diffusion Policy实验，因此不能声称Diffusion Policy已被证明无效。

#### C. 动作监督与阶段信息

| 候选 | 结果 | 解释 |
| --- | --- | --- |
| 未来3步动作辅助 | 33.71% vs 37.42% | 辅助项约占总目标37%，未来接触后动作多解，干扰当前动作 |
| 显式归一化时间/阶段标量 | 8物体29.33%，锁定对照39.38% | 同一时刻的真实接触阶段可能不同，固定轨迹进度是弱信号 |
| 70%教师+30%示范 | 完整开发集38.60%，对照37.42% | 只高1.18点，未达事先+2点复测门槛，Bowl/Camera各降约8点 |
| 50/50、30/70和100%示范 | 8物体35.60%、34.03%和首轮22.76% | 示范比例超过30%时，学生偏离状态的教师纠正被稀释 |

70/30一次全开发集略高不代表它应立即替换主模型。它未达预先定义的复测门槛，且类别收益严重不均；后续更广目标比例实验也没显示“更多示范”存在稳定趋势。

#### D. Temporal3自身状态的在线模仿

这是很重要的一个负结果。原Temporal3使用的在线状态由早期单帧学生采集，所以合理假设是：“换成Temporal3自己访问的状态再做DAgger，应该更好。”

配对实验保持1726离线轨迹、100%教师目标、25%在线比例和优化器状态不变，只替换22080个在线状态。还用旧在线数据做等时长继续训练对照。

- Temporal3自身状态最佳epoch6：36.52%；
- 锁定Temporal3：39.38%；
- 旧数据等时长继续训练：39.35%；
- Bowl由49.38%降至32.50%。

同时，Temporal3与单帧教师的MAE更小，但在配对69步采集中成功数由42/320降至35/320。这说明问题不是没有采集当前学生状态，而是单帧教师在这些时序偏离状态上并不能给出更好的恢复标签。

#### E. 动作融合、类别时序专家与残差PPO

| 候选 | 结果 | 结论 |
| --- | --- | --- |
| `0.75 Temporal3 + 0.25 Online-R1` | 108/313、37.96%，比Temporal3多1条、+0.54点 | 抬升低0.80 cm、失败率高0.90点、Camera低6.97点，未达+2点门槛 |
| 四个Temporal3类别专家 | 8物体24.73%，统一Temporal3为39.38% | 只用本类成功示范微调导致快速能力遗忘，不再蒸馏 |
| Temporal3门控残差PPO | 第10/25/50轮为33.63%/33.25%/35.64%，零残差36.24% | 学习率、KL、门和动作饱和都已校准，但仍未学到可泛化纠错 |

类别Temporal3专家的结果也回答了“是否应立即做类别时序专家再统一蒸馏”：教师池自身已远低于统一Temporal3，此时继续蒸馏只会把更弱教师压进学生，所以在教师层就停止是合理的。

### 11.14 阶段M：最终主线审计、留出评测和渲染复核

#### 为什么还要把主线所有checkpoint重新拿出来

单次开发集搜索会混入checkpoint选择和PhysX波动。因此最后在固定16个训练物体、495条轨迹上，用2025/2026/2027三个seed对五个主线节点做了一次串行审计：

| 节点 | 宏成功 | 与前一节点差值 |
| --- | ---: | ---: |
| BC Soup | 26.10% | — |
| 类别教师池 | 24.94% | -1.16点 |
| 离线Task-ID学生 | 22.81% | -2.14点 |
| Online-R1 | 29.56% | **+6.75点** |
| Temporal3 | 31.12% | **+1.56点** |

Temporal3三次成功数为157/155/159，宏成功标准差只有0.21个百分点，且失败率在五个节点中最低。这个审计明确显示：类别专业化和离线蒸馏本身都有损失，最大的正收益是Online-R1，Temporal3进一步小幅提升。

#### 最终未见实例

在打开最终8物体前，锁定Online-R1和Temporal3的checkpoint路径、SHA256、评测seed和物体清单。最终Temporal3在3个seed均胜过Online-R1，宏成功由17.46±0.72%提高到26.15±0.33%。这才是“短历史可以泛化到同类新物体”的主要证据。

但全80个训练实例的独立轨迹宏成功只有26.05%，与最终未见实例26.15%几乎一样。因此不能把低成功率主要归因于新几何：即使是训练见过的物体，闭环复现仍只有约四分之一成功。

#### 渲染结果为什么与并行评测不完全一致

最终8个预选案例做单环境视觉重放时，实际是2成功6失败。预先在并行评测中稳定成功的Bowl和Camera，在单环境渲染中变成失败；早期训练案例也出现过Bottle预选成功变失败、Mug预选失败变成功。

这不表示可以任意重放直到成功。相反，报告案例严格按当次渲染YAML标注，并把这个现象作为接触仿真对并行布局和数值扰动敏感的限制。统计结果因此使用固定协议的大批量三seed结果，而不是用几个渲染视频代替成功率。

### 11.15 从所有探索中得到的方法论

1. **先验证指标和数据对齐，再调模型。** 末帧成功、峰值成功、示范回放和学生闭环是四个不同概念。
2. **闭环指标必须决定checkpoint。** 离线loss、师生MAE和PPO reward多次与官方成功率不一致。
3. **先做严格配对，再解释单次提升。** Soup、锚定PPO、参数插值和动作融合都出现过“单次好看，重复后不成立”。
4. **人工分类挑冠军可以做上限分析，不能冒充统一算法。** 最终教师池不事后混用物体均衡专家，最终推理也不按物体挑模型。
5. **增加复杂度前先检查教师和数据是否提供了新信息。** 7帧、GRU、attention、未来动作和阶段标量都没有解决单帧教师的恢复能力不足。
6. **当前最有效的改进是改变训练状态分布。** 在线R1直接覆盖学生偏离状态，为串行主线贡献最大增益；Temporal3再提供短时阶段信息。
7. **负结果不等于方法在所有条件下无效。** 我们只能说当前reward、数据、教师和算力预算下的具体PPO/GRU/attention实现未超过Temporal3，不能推广为这些方法理论上无效。

## 12. 最终结果应怎样理解

### 12.1 全部80个见过物体的独立轨迹

| 指标 | Temporal3 |
| --- | ---: |
| 成功数 | 119/434 |
| 总体成功率 | 27.42% |
| 物体宏成功率 | 26.05% |
| 平均最大抬升 | 12.92 cm |
| Bottle / Mug | 35.27% / 33.49% |
| Bowl / Camera | 14.55% / 20.88% |

80个物体中有19个零成功，其中10个是Bowl。说明Bowl和Camera仍是主要短板。

### 12.2 最终8个未见实例

| 模型 | 总体成功率 | 物体宏成功率 | 平均最大抬升 |
| --- | ---: | ---: | ---: |
| Online-R1 | 19.91±0.65% | 17.46±0.72% | 9.37 cm |
| Temporal3 | **27.47±0.58%** | **26.15±0.33%** | **11.83 cm** |

Temporal3 在三个仿真 seed 都超过 Online-R1，宏成功率提高8.69个百分点。Mug、Bowl、Camera分别提高16.03、14.17、4.88个百分点，Bottle基本持平。

但26.15%仍然是较低的绝对成功率，不能包装成“任务已经解决”。它与80个见过物体的26.05%几乎相同，因此当前主要瓶颈不是新物体几何泛化，而是：

1. 在见过物体上也无法稳定复现成功示范；
2. 单帧教师缺乏偏离后的真正恢复能力；
3. Temporal3只有短时本体和动作历史，没有历史接触/几何记忆；
4. Bowl等类别在共享训练中仍发生能力损失；
5. Isaac Gym接触结果存在一定数值敏感性。

单环境渲染中8个预选案例实际只有2成功、6失败；并行评测中成功的Bowl/Camera在单环境重放变成失败，也说明临界接触轨迹会受仿真布局和数值扰动影响。

## 13. 当前最可信的结论

1. **原始低成功率不是因为示范本身失败。** 核心是 BC 的闭环分布偏移。
2. **离线动作loss不能代表闭环抓取成功率。** checkpoint 必须在仿真中按官方指标选择。
3. **在线模仿是主线最大有效改进。** 它直接覆盖学生自己造成的偏离状态。
4. **短历史有效，但复杂时序结构没有自动提升。** 数据标签质量比模型复杂度更可能是当前瓶颈。
5. **类别教师和蒸馏本身存在能力损失。** 当前它们是形成统一在线学生的训练手段，不是性能最高的节点。
6. **当前结果尚不够高。** Temporal3 是现有证据下最可靠的单网络方案，而不是理论或工程上已经到达上限。

## 14. 如果以后继续做，优先研究什么

按现有证据，优先级应是：

1. 建立能在学生偏离状态上给出可靠纠正的闭环教师，而不是继续查询单帧 BC；
2. 使用接触状态、指尖力或历史 DexRep 设计“接近—包络—抬升—恢复”状态表示；
3. 针对Bowl/Camera研究类别适配器，保留统一主干但减少共享训练的能力冲突；
4. 重新冻结一批未见物体，再进行新一轮模型选择；
5. 在统一数据和评测协议下再比较 Diffusion Policy、时序 Transformer 或分阶段策略。

不应优先做的是：只根据现有最终8物体挑类别模型、继续无上限搜索 seed、或只追求更低的离线 validation loss。

## 15. 关键文件位置

| 内容 | 文件 |
| --- | --- |
| 最终Temporal3配置 | `custom_tools/configs/unified_student_taskid_temporal3_v1.yaml` |
| 最终模型锁定信息 | `custom_tools/configs/taskid_final_model_lock_v1.yaml` |
| Task-ID/Temporal3结构 | `custom_tools/task_conditioning.py` |
| 数据、在线采样和训练损失 | `custom_tools/train_bc.py` |
| 数据集加载与2460维处理 | `custom_tools/graspm3_dexrep_dataset.py` |
| 官方DexRep编码器 | `ActionDiffusion/bc/model/policy/lqt_policy.py` |
| 官方动作损失 | `ActionDiffusion/bc/model/policy/lhm_policy.py` |
| 三seed主线审计 | `custom_tools/results/training_mainline_three_seed_v1/summary.yaml` |
| 最终8物体结果 | `custom_tools/results/taskid_locked_final_holdout_v1/summary.yaml` |
| 80物体验证结果 | `custom_tools/results/taskid_locked_seen80_validation_v1/summary.yaml` |
| 简洁过程日志 | `PROJECT_LOG.md` |
| 提交版实验报告 | `EXPERIMENT_REPORT.md` |

当前锁定主模型：

```text
custom_tools/runs/bc/
unified_student_taskid_temporal3_seed2025_e4_v1/
epoch=003-step=5152.ckpt
```

SHA256：

```text
aecf421b76f8e2a8e669cdd01117ae64b8c275b658ea4766fdf9c6bb705d8b04
```

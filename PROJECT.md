# 灵巧手抓取项目一周重启记录

> 周期：2026-08-23 至 2026-08-30  
> 状态：ShadowHand阶段已冻结；基础重定向重新进入G0–G2审计
> 本文是本轮唯一核心工作文档；契约、决策、实验和复现入口均在这里更新。

## 1. 一句话目标

在一周内重审并改进三条现有工作流，尽可能提高未见物体上的自主、稳定、可恢复抓取成功率；第六天冻结方法，第七天只做封闭测试、复算和收尾。

三条工作流：

1. GraspM3四类物体上的Shadow Hand自主策略；
2. Shadow轨迹到Linker O6、XHand、Wuji的基础重定向；
3. 基于合格重定向轨迹训练三只目标手的自主策略。

## 2. 已确认的项目契约

### 2.1 信息边界

- Shadow和三只目标手的策略在测试时只读取初始状态及执行中的当前/历史观测。
- 禁止读取未来专家动作、未来重定向轨迹、专家手腕或轨迹检索结果。
- 未来专家信息可以在训练阶段作为监督或在线模仿教师，但必须明确教师如何对学生访问状态给出有效纠正动作。
- 基础重定向可以读取完整Shadow源轨迹，因为轨迹转换本身就是该任务的输入。

### 2.2 数据与外部资源

- Shadow策略保留原四类、80个训练物体范围，不在本周扩展到50类。
- 重定向和目标手策略保留50类、100物体、1000条协议。
- 可以复用论文、开源算法、代码、URDF和通用依赖。
- 模型只使用GraspM3及其重定向产物训练；不引入其他抓取数据集或外部抓取策略权重。
- 可以使用官方DexRep和从GraspM3物体网格、手部运动学中自行计算的几何特征。

### 2.3 指标优先级

1. 质量主指标：稳定抓取成功率——相对落稳初始高度抬升至少0.15 m，末段0.5 s持续保持，且峰值到末值没有明显滑落。
2. 可比性指标：参考仓库Isaac累计成功——物体进入桌面上方0.30 m目标点的0.15 m邻域，或达到目标点高度；成功一旦出现便锁存。
3. 严格诊断：稳定抬升30 cm；它用于比较高抬升能力，不冒充题面规定。
4. 诊断指标：最终高度、峰值到终点回落、掌物相对滑移、接触、动作跳变和穿透。
5. 小型封闭集补充质量扰动：质量、摩擦或初始位姿发生变化后的重复成功率。
6. 主指标相同时，依次比较物体/类别宏平均、鲁棒性和滑移。

### 2.4 双层评测

- 旧Final8、原1000条和原test500已经被查看，改称开发基准。
- 方法只在旧开发基准和训练/验证数据上选择。
- 另建立一批未参与选择的封闭样本，第六天冻结，第七天只评测一次。
- 基础重定向仍报告考核要求的原1000条配对结果，同时用新样本检查是否只适配旧池。

### 2.5 时间盒

| 日期 | 工作 |
|---|---|
| 第1天 | G0–G4：任务、源码、数据、指标审计；冻结候选与最小实验 |
| 第2天 | Shadow策略实现与小规模比较 |
| 第3天 | Shadow冻结；基础重定向实现与筛选 |
| 第4天 | 三手重定向冻结；生成正确专家池 |
| 第5–6天 | 三只目标手自主策略实现、开发评测；第六天冻结 |
| 第7天 | 封闭测试、复算、图表、视频、复现和Git收尾 |

某方向到时没有提升也必须冻结，不能无限侵占下一阶段。

## 3. G0：任务标准审计

### 3.1 GraspM3/Shadow策略

官方仓库提供单物体训练/测试示例，基线是DexRep表示上的MLP；GraspM3每帧28维由6维全局手腕和22维Shadow关节组成。正式挑战测试物体不可见，并随机化初始手姿，要求生成抓取序列并抬升物体。

来源：

- [DexGraspMotionChallenge2025官方仓库](https://github.com/DexGraspMotionChallenge/DexGraspMotionChallenge2025)
- [HANDS 2025挑战说明](https://hands-workshop.org/challenge2025.html)

旧项目扩展出的四类多物体协议不是官方单物体示例本身，而是我们的研究协议。旧最终主指标是官方“曾达到目标”的峰值成功，本周新增稳定质量指标。

### 3.2 基础重定向

考核原文要求从GraspM3随机选50类、100物体、1000条轨迹，重定向到三种6–20自由度五指手并提高重放成功率。原文没有规定30 cm、末段保持或扰动；这些是本轮为抓取质量增加的统一研究标准，报告时必须与题面要求分开表述。

### 3.3 进阶策略

考核原文要求“基于重定向后的轨迹训练操作策略，提高测试集物体抓取成功率”，没有明确未来参考轨迹是否允许。本轮选择更严格且更符合自主策略的解释：测试时禁止未来专家信息。

## 4. G1：现有实现与可信基线

### 4.1 Shadow旧主线

训练血缘：

```text
噪声BC → BC Soup → 四个类别教师 → Task-ID统一学生
→ Online-R1/R2聚合 → Temporal3
```

Temporal3读取当前DexRep、当前及前两步100维本体状态、前两步实际动作和四维Task-ID，以MLP输出28维绝对动作。旧Final8在3个仿真seed上的物体宏平均为26.15%，同数据官方单帧BC为8.77%。

确定限制：

- 旧选模指标是官方峰值成功，不是末段稳定抓取；
- DAgger类别教师仍是单帧BC，不是能够从严重偏离状态恢复的真实oracle；
- Temporal3只预测单步绝对动作，没有动作片段建模；
- 最终8个物体已经打开，不能继续作为无偏测试；
- 仓库已有更完整的`ActionDiffusion`实现，但旧项目没有完成与Temporal3同协议的正式比较。

可信旧结果来源：`FINAL_SUBMISSION/EXPERIMENT_REPORT.md`及其冻结结果文件。

### 4.2 基础重定向旧结果

旧最终方法文件 `retarget_research/configs/final_method_decision_v3.json` 记录：

- Linker：object-centric 3 mm方法，旧成功234/1000；
- XHand：官方参考方法，旧成功574/1000；
- Wuji：旧无时序映射，旧成功638/1000。

按稳定运输协议重新审计的另一组轨迹池为：

| 手 | 末段稳定30 cm | 运输不滑移 | train可用 | valid可用 | test演示上限 |
|---|---:|---:|---:|---:|---:|
| Linker | 193/1000 | 153/1000 | 70/400 | 21/100 | 62/500 |
| XHand | 516/1000 | 501/1000 | 225/400 | 52/100 | 224/500 |
| Wuji旧版 | 581/1000 | 557/1000 | 244/400 | 62/100 | 251/500 |

来源：`retarget_research/outputs/formal_1000/final_pools_audit/three_hand_stable_audit_summary.json`。

这里存在两个必须先解决的不一致：

1. 稳定审计中的Linker来源是`linker_vector_v2alpha_c3g8_v1`，不是旧最终决策中的`linker_object_centric_3mm_v1`。
2. Wuji旧版四根普通指远端关节大量利用负下界反向弯曲；虽然旧配置后来把解剖门改成pass，但该轨迹池不能直接视为形态合格专家。合规的`wuji_anatomy_coupled_v1`尚未完成正式1000条替换。

因此，上表目前是旧数据事实，不是本轮已经认可的最终专家池。

### 4.3 目标手自主策略旧结果

旧冻结结果：

| 手 | 旧方法 | valid100 | 旧test500 |
|---|---|---:|---:|
| Linker | Initial-State + Phase，MSE | 10% | 7.0% |
| XHand | Initial-State + Phase，Huber | 14% | 8.4% |
| Wuji | 初态名义策略 + 三帧反馈/旧DAgger | 19% | 6.0% |

这些模型测试时没有读取未来专家轨迹，但存在更基础的数据与表示问题：

- `prepare_policy_dataset.py`用旧评测摘要的`result["success"]`筛train/valid，没有读取稳定审计的`training_eligible`；
- `hand_data_specs_v5.json`中的Linker轨迹池与旧最终方法决策不一致；
- Wuji训练轨迹仍指向存在反向远端弯曲的旧池；
- Linker/XHand最终InitialPhase策略在episode开始后只依赖初态和时间，相当于开环动作生成，没有使用当前手物偏差；
- 当前观测只有关节、物体状态、总接触数和14维静态形状描述，没有类似DexRep的显式手—物表面几何关系；
- 旧Diffusion是简化的MLP-DDPM、绝对动作和无phase版本，0成功不能代表正式Diffusion Policy无效。

## 5. G1方法调研的本周候选

以下只是候选，尚未经过G3选择：

### Shadow策略

- 正确复用仓库现有ActionDiffusion/DP3，使用DexRep、短观测历史、动作片段和滚动重规划；
- 或先做更低风险的动作分块策略：一次预测短动作片段，并用重叠预测平滑执行；
- 只有在训练oracle能够对学生偏离状态给出有效恢复动作时，才追加一轮DAgger。

动作分块针对单步预测的误差累积；ACT论文表明action chunking与temporal ensembling是关键组件。[ACT论文](https://arxiv.org/abs/2304.13705) Diffusion Policy强调多模态动作、动作序列和receding-horizon控制。[Diffusion Policy论文](https://arxiv.org/abs/2303.04137)

### 基础重定向

- 不再继续单独调一个局部权重；优先比较已有方法与“完整任务空间目标”的最小实现：全局拇指/腕姿态、腕到指尖、指尖方向、拇指—各指pinch、关节自然先验和速度平滑共同优化；
- XHand官方方法作为强基线，主要资源用于Linker与Wuji；
- Wuji必须把解剖合规性作为硬边界，而不是事后视频说明；
- 只有开发小集出现稳定提升，才进入正式轨迹生成。

近期系统实验指出，腕到指尖形状、指尖方向、pinch、关节自然先验和速度正则对不同任务作用不同，不能只以关键点MSE判断。[Retargeting Objectives论文](https://arxiv.org/abs/2506.09384)

### 目标手自主策略

- 第一优先级是用真正稳定、形态合格的专家重新建数据；
- 加入目标手FK计算出的掌心/指尖—物体相对几何，形成轻量目标手DexRep，而不是只给网络原始关节角和静态包围盒；
- 使用当前闭环状态、phase与短动作片段生成，避免InitialPhase完全开环；
- 若做DAgger，教师必须根据当前学生状态进行状态对齐或闭环纠正，不能直接复制同一时间帧专家动作；
- Diffusion只作为动作分块基线通过后才晋级的高成本候选。

DexRep的核心正是显式编码动态物体形状和手物空间关系。[DexRepNet论文](https://arxiv.org/abs/2303.09806) DAgger要求在策略实际访问状态上查询专家，而不只是取时间对齐标签。[DAgger论文](https://arxiv.org/abs/1011.0686)

## 6. G3重新展开：不把旧BC流水线当作默认答案

上一版“小修数据、加几何、做动作分块”的候选矩阵已撤回。那些组件可以作为实现细节或回退方案，但不能构成本轮主体创新。重新调研后，把方法分成以下四个真正不同的体系。

### 6.1 体系一：目标抓姿生成 + 目标条件强化学习

这类方法不直接从初态回归70帧动作。它先回答“这件物体应该以什么手腕姿态和手指形状抓”，再让闭环控制器回答“当前状态下怎样到达并稳定保持这个抓姿”。

训练时，GraspM3或合格重定向轨迹的末段稳定手姿提供抓姿样本；抓姿生成器在物体坐标系内学习物体几何到手腕6D姿态和关节角的多模态分布。随后用Isaac Gym并行PPO训练goal-conditioned executor，观测当前手物状态和选定目标抓姿，输出当前控制动作。测试时目标抓姿由生成器根据新物体几何产生，不读取测试专家轨迹。

这是UniDexGrasp的核心分解；其执行策略每一步读取点云和本体状态。UniDexGrasp++进一步说明，物体/几何课程对RL很重要：论文中普通训练约31%，加入课程后训练集约74%，完整方法报告未见物体78.2%。这些数字不能直接移植到本项目，但说明70%量级并非纯BC小修得到，而是任务分解、RL和大规模课程共同作用。[UniDexGrasp论文](https://openaccess.thecvf.com/content/CVPR2023/papers/Xu_UniDexGrasp_Universal_Robotic_Dexterous_Grasping_via_Learning_Diverse_Proposal_Generation_CVPR_2023_paper.pdf) [UniDexGrasp++代码](https://github.com/PKU-EPIC/UniDexGrasp2)

### 6.2 体系二：示范初始化的完整PPO，而不是冻结BC上的小残差

旧Residual PPO把低成功BC冻结，只允许很小残差，因此策略的搜索空间从一开始就被错误轨迹限制。本轮的新候选是完整actor：用成功示范做BC预训练或辅助loss初始化，但PPO可以更新全部策略参数、产生完整动作，并直接根据接近、对向接触、抬升、末段保持、滑移和掉落奖励探索。

训练使用课程：先固定少数容易物体和较近初态学会接近/闭合，再扩大到全部训练物体、随机初态、摩擦与质量扰动。actor只看可用观测，critic可在训练时读取接触力、精确物体状态等特权信息。DexPoint和UniDexGrasp++都使用物体几何、多物体并行RL与课程来获得未见物体泛化。[DexPoint论文](https://proceedings.mlr.press/v205/qin23a/qin23a.pdf) [UniDexGrasp++论文](https://openaccess.thecvf.com/content/ICCV2023/papers/Wan_UniDexGrasp_Improving_Dexterous_Grasping_Policy_Learning_via_Geometry-Aware_Curriculum_and_ICCV_2023_paper.pdf)

### 6.3 体系三：物理驱动重定向（SPIDER式），而不是逐帧关键点拟合

旧方法只问“目标手关键点是否像Shadow”，并不直接问“物体在这只手里是否受力稳定”。新候选保留旧运动学结果作为初始轨迹，但随后在仿真中用采样优化整段运动。

具体把高维70帧轨迹压成少量可修改参数：手腕位姿偏移、各指闭合偏移、接近/闭合/抬升三段时间、抬升方向和幅度。每轮并行采样多组参数，在Isaac中真实重放，并按接触保持、对向接触、30 cm末段稳定、滑移、穿透和动作平滑评分，再用CEM更新采样分布。源Shadow接触关系作为“虚拟接触引导”，防止采样时丢失原抓取意图。

这与SPIDER“运动学结果给任务结构，物理采样恢复动态可执行性”的思想一致；其论文报告相对标准采样平均提高18%，并明确采用采样而非依赖接触梯度。[SPIDER论文](https://arxiv.org/abs/2511.09484) [SPIDER代码](https://github.com/facebookresearch/spider)

### 6.4 体系四：学习式几何重定向

GeoRT用大量源手姿自监督训练一个神经映射，目标不是逐帧重新跑SLSQP，而是在目标手关节空间中保持源手运动结构、可达范围、平直姿态和对捏关系。官方代码支持导入自定义URDF，训练本体通常只需分钟级。[GeoRT代码](https://github.com/facebookresearch/GeoRT)

它是真正不同的重定向方法，适合检验手工语义映射是否是Linker/Wuji的瓶颈；但它本身不保证抓取物理成功，因此只作为快速候选，输出仍要经过物理回放，必要时再接体系三修复。

## 7. 新的G4候选矩阵

### 7.1 Shadow：先比较两条新主线

| 编号 | 新体系 | 与旧Temporal3的本质差别 | 首轮判定 |
|---|---|---|---|
| S1 | 完整PPO + 示范初始化 + 几何课程 | 不再拟合固定动作；用物理奖励更新完整策略 | 先在4个训练物体上学到明显非零稳定成功 |
| S2 | 抓姿生成器 + goal-conditioned PPO | 把“选抓法”和“执行抓法”拆开 | 给真实/预测目标抓姿时均完成20-case闭环测试 |
| S3 | 正式DP3 | 直接学习多模态动作序列 | 只在S1/S2工程阻塞或离线对照需要时启动 |

旧Temporal3只作为B0，不再投入主体优化。优先S2，因为GraspM3天然提供多条末段抓姿；若抓姿生成来不及稳定，则S1是仍可独立成立的新主线。

### 7.2 基础重定向：两种新方法，而不是继续搜旧权重

| 编号 | 新体系 | 运行范围 | 判定 |
|---|---|---|---|
| R1 | GeoRT式学习几何映射 | 三只手各10条→均衡50条 | 几何、解剖和物理成功同时与旧方法比较 |
| R2 | SPIDER式物理采样+CEM+虚拟接触引导 | 先修复旧失败/滑移case，再跑50条 | 稳定成功必须净增，不能用穿透换峰值 |

R2是主候选；R1训练快，先用于判断Linker/Wuji的手工映射是否根本有误。XHand也参加小集对比，不能因为旧基线强就预先排除新方法。只有胜者运行原1000条。

### 7.3 三只目标手：统一成同一套自主RL体系

基础轨迹只在训练阶段提供抓姿分布和初始化，不在测试时播放。三只手均采用：

```text
物体几何 → 候选目标抓姿 → goal-conditioned PPO闭环执行
                      ↑
          当前手物状态、接触和历史反馈
```

先在XHand验证整套闭环，因为它的合格专家最多；成功后替换动作维度、URDF/FK和关节边界迁移到Linker、Wuji。策略不是冻结BC残差，PPO能输出完整手腕与手指动作。若目标抓姿生成器暂时不可靠，训练时先用训练集真值抓姿验证executor上限，再切到预测抓姿；两者必须分别报告，不能把真值目标结果冒充自主测试。

### 7.4 质量指标同步升级

“末段稳定15 cm且不掉落”是本轮主任务成功；30 cm只作严格高抬升诊断。抓取质量还需要同时记录：

1. **任务结果：** 抬升高度、末段保持、滑移、掉落；
2. **接触质量：** 接触指数量、接触持续时间、拇指与其他手指是否形成对向接触；
3. **力学质量：** 根据接触点、法向和摩擦锥近似计算是否force closure，以及Ferrari–Canny epsilon近似；
4. **鲁棒质量：** 改变质量、摩擦、初始位姿或施加小扰动力后是否仍稳定；
5. **代价与安全：** 穿透、关节越界、动作跳变和控制能耗。

主结果仍以未见物体稳定成功率排序，但该组指标能解释两个同为成功的抓法为什么质量不同。

### 7.5 一周内的止损顺序

1. 先做S1/S2和R1/R2各自最小闭环，不先承诺某篇论文的完整复现；
2. 新体系必须在10–20个case中产生非零且可视化真实的物理成功，才扩到50/100；
3. 每个体系只允许一次结构修复和一次参数修复；仍不工作就停止；
4. 第四天若新重定向仍未超过旧基线，才回退旧方法并只修专家质量；
5. 第六天若完整RL仍未超过旧自主策略，才启用动作分块/DP3等离线回退方案。

## 8. G0–G8状态

| 阶段 | 状态 | 当前出口条件 |
|---|---|---|
| G0 任务审计 | 完成 | 题面未规定高度；参考Isaac采用30 cm目标点+15 cm邻域累计成功；另报告稳定15 cm质量与稳定30 cm诊断 |
| G1 领域/基线审计 | 完成 | 三条数据流、旧结果和关键实现错误已核对 |
| G2 Grill-me | 完成 | 目标、期限、边界、指标、交付物已确认 |
| G3 方案反审 | 完成 | 完成一次正反论证并压缩为最小候选矩阵 |
| G4 项目契约 | 完成 | 用户确认新体系候选和停止条件 |
| G5 最小闭环 | 进行中 | XHand物理CEM已出现失败转成功；Shadow完整动作PPO尚未通过确定性闭环 |
| G6 小规模迭代 | 未开始 | 按硬时间盒比较候选 |
| G7 正式冻结 | 未开始 | 第六天冻结配置、代码、数据和模型 |
| G8 封闭测试/交付 | 未开始 | 第七天一次评测并收尾 |

## 9. 变更记录

- 2026-08-23：重启项目；旧测试改为开发基准；统一采用自主信息边界和稳定抓取质量。
- 2026-08-23：确认一周期限、允许新体系但禁止外部抓取训练数据、采用硬时间盒。
- 2026-08-23：完成G3反审；主线确定为先修数据和闭环表示，再比较动作分块与正式DP3；DAgger改为有oracle门槛的条件候选。
- 2026-08-23：用户否决“小修旧BC”的范围；撤回原候选矩阵，改为目标抓姿生成+goal-conditioned PPO、完整PPO、SPIDER式物理重定向和GeoRT式学习重定向。旧方法仅保留为回退基线。
- 2026-08-23：G4确认；新增`goal_conditioned_env.py`、`goal_conditioned_ppo.py`和训练入口，完整actor由示范初始化后接受PPO更新；新增`physics_cem_refine.py`，按真实物理稳定性搜索整段轨迹低维修改。CPU单元测试通过，等待Isaac G5短测。
- 2026-08-23：首次G5短测：Shadow的BC loss从0.198降至0.133，PPO出现官方峰值事件但稳定成功仍为0；诊断为BC初始化不足、保持奖励缺失以及28维PPO单次更新KL过大，已增加保持/回落奖励、30轮预训练并把PPO学习率降至1e-5。XHand CEM在WineBottle-10得到35.77 cm稳定轨迹，但该旧基线本身成功，因此只证明搜索可保持成功，不能算提升；下一轮改用稳定审计明确失败的FileCabinet-36与Pillow-10。
- 2026-08-24：第二次G5短测：Shadow预训练loss降至0.0024，但确定性闭环仍为0/8，PPO只在随机探索中零星产生稳定事件，因此不能把低训练loss或随机事件当成自主策略成功，已补充最终checkpoint的确定性评测入口。物理CEM在同一物理runner中把FileCabinet-36从末段掉落17.04 cm修到末段稳定抬升35.28 cm，把Pillow-10从末段掉落19.59 cm修到稳定抬升37.15 cm；这是2/2明确失败case转成功，但仍须经独立统一重放、视频和穿透检查后才能进入50-case。当前实现是12维低维物理CEM，尚未实现源接触虚拟引导，故将后续schema更正为`physics_cem_lowdim_v1`，不夸大为完整SPIDER复现。
- 2026-08-24：独立复核确认FileCabinet-36末段35.31 cm、掉落0.017 cm、末段接触率1.0，Pillow-10末段37.00 cm、掉落0.014 cm、末段接触率1.0；两条视频均能看到真实接近、包覆与抬升，故R2通过XHand最小闭环，下一步跨Linker/Wuji检查。Shadow第5/10/15/20轮确定性评测均为0/8，平均最大抬升依次约0.00/0.15/1.18/1.76 cm；该体系尚未通过G5，先用“仅初态+末端目标的解析插值”诊断执行器分解是否成立，再决定唯一一次结构修复，禁止用随机rollout事件冒充成功率。
- 2026-08-24：R2跨手短测中，Wuji Headphones-33由峰值30.95 cm后掉落29.06 cm修为末段稳定40.22 cm，初步通过；Linker Canister-19由峰值17.39 cm/末段-7.12 cm改善为峰值15.66 cm/末段0.06 cm，但仍失败。代码审计发现通用5指分组把Linker的`thumb_yaw`和`thumb_pitch`错误绑定为同一个偏移；已改为6个主动量分别搜索（拇指yaw、拇指pitch及四指屈曲），这是结构修复而非继续盲搜权重。Wuji仍须独立统一重放；Shadow解析插值结果文件缺失，不能记作已完成。
- 2026-08-24：修正Linker参数化后，Canister-19由峰值17.52 cm、末段-7.26 cm修为末段稳定32.28 cm、掉落0.12 cm，证明6自由度耦合手仍可由物理CEM修复；Wuji独立重放保持末段40.25 cm、掉落0.08 cm，视频可见真实包覆与抬升。至此R2已在三只手上各出现至少一个明确失败转成功案例，但尚未证明小集平均提升。Shadow仅初态到末端目标的插值为0/8且平均峰值仅0.004 cm，说明单一末端28维动作不是充分运动计划；在结构修复前先补完整专家重放与五阶段路标重放两个控制实验，区分环境错误和路径信息缺失。
- 2026-08-24：独立统一重放推翻了上一条中的Linker结论：同一候选仅峰值10.63 cm、末段-7.31 cm、末段接触率0，不能记为成功。根因是CEM环境把每个70帧目标直接保持3个物理步，而统一重放在3步内由前帧线性插值到当前帧；已让CEM观测和执行共享同一插值函数，并补纯函数测试。Wuji独立成功仍成立，XHand两例仍成立；Linker须在修正时序后重新搜索。
- 2026-08-24：停止继续投入Shadow完整PPO，转为复现并审计旧Temporal3。核对确认最终26.15%评测通过`ResidualDexGraspEnv.reset()`清空历史，因此不受旧`evaluation_loop.py`跨物体历史泄漏影响；旧入口仍已修复并通过最小测试。更重要的是26.15%属于官方峰值成功：源码允许`goal_dist<=0.12 m`即置成功，且不要求末段保持或接触，不能与目标手严格末段30 cm指标直接比较。评测器已新增并行输出严格指标：最后20个策略步均抬升至少30 cm、至少2个指端接触、峰值到终点回落不超过3 cm。
- 2026-08-24：进阶数据上限审计完成。500条测试轨迹的原重定向专家严格成功分别为Linker 81（16.2%）、XHand 257（51.4%）、Wuji 295（59.0%）；成功过滤后训练集分别只剩89、256、270条，Linker缺失14/50个类别。当前自主模型35/500、42/500、30/500的主要瓶颈不是单一超参数，而是专家覆盖不足、粗14维形状描述和开环`initial_state+phase`表示。下一轮顺序冻结为：仅在训练集修复失败专家以补覆盖；按几何/形状路由教师并蒸馏统一学生；加入当前DexRep式手物几何、Temporal历史和phase；最后在可信状态邻域内做state-aligned DAgger，测试时仍不得读取专家轨迹。
- 2026-08-24：按用户要求停止并行推进不同任务，固定顺序为Shadow策略→基础重定向→重定向进阶策略。旧Shadow Temporal3在冻结8物体、216轨迹、seed2025上精确复现历史结果：官方峰值59/216、overall 27.31%、object-macro 26.11%；新增严格末段指标为0/216。由于严格指标同时包含30 cm、末20策略步保持、至少2接触和回落≤3 cm，尚不能直接归因于策略；已增加18/20/25/30 cm末段高度、接触保持和低回落的分项统计，以及GraspM3专家动作同环境重放模式。下一控制实验先测专家上限，再决定Temporal3应继续组合还是重构。
- 2026-08-24：专家同环境重放达到官方200/216（92.59%）；末20步保持≥18/25/30 cm分别196/180/133条，低回落207条，但连续20步每步≥2个力传感器接触为0，证明该接触读数不能作为硬成功门。严格主指标调整为“末20策略步均≥30 cm且峰值到终点回落≤3 cm”，接触仅作诊断。Temporal3在该指标下为29/216（13.43%），而专家由集合下界可知至少124/216（57.41%），仍有约四倍差距。旧实验审计确认Phase、GRU、Temporal5/7、辅助动作块、继续训练、on-policy数据与类别Temporal专家均未稳定超过原Temporal3，停止重复这些路线。
- 2026-08-24：启动Shadow新候选`Temporal Chunk8 Demo80`。它保留DexRep、Task-ID、两帧状态/动作历史和R1/R2在线数据，但把离线监督由100%弱类别教师改为80%原始示范+20%教师，并真正预测未来8步动作块；推理时对最近8次预测中指向当前时刻的动作作指数时间集成，而非像旧chunk实验那样只训练辅助头、仍执行单步动作。已实现独立动作块推理状态、episode清空和配置，CPU构造/时间对齐测试通过；下一步从冻结Temporal3精确warm-start训练4轮，再只在开发集比较严格运输指标。
- 2026-08-24：用户质疑严格指标会误杀末段仍上升的成功轨迹，复审确认质疑成立。此前“最后20步每一步均≥30 cm”会漏掉末段才越过阈值的轨迹；更严重的是旧评测让只在70帧数据上训练的Temporal3继续自主输出到第122步，而专家重放在70帧后保持末动作，协议不对称。因此撤销29/216作为最终严格基线，只保留为旧协议诊断。新统一协议为：策略自主输出70个20 Hz动作，随后固定第70步动作仿真52步；最终高度≥30 cm且峰值到终点回落≤3 cm即为稳定运输成功。上升过程允许继续变化，接触与末段高度波动只作质量诊断。
- 2026-08-24：`restart_shadow_chunk8_demo80_v1`训练完整结束，4轮共5152 step、耗时81.2秒，4个逐轮checkpoint与`last.ckpt`齐全。训练总loss从0.0974降至0.0860，示范当前动作loss从0.0337降至0.0248；但验证loss从0.0343持续升至0.0422，说明继续训练正在偏离验证分布，不能默认选择最后一轮。开发集先比较旧Temporal3与新模型第1轮；若新模型有优势，再检查后续轮次，而不是按训练loss选模型。
- 2026-08-24：统一70步输出+52步末动作保持协议下，冻结12物体开发集共313条：旧Temporal3官方67/313（overall 21.41%，object-macro 23.13%）、稳定运输33/313（10.54%，macro 10.78%）；Chunk8 Demo80第1轮官方123/313（39.30%，macro 39.06%）、稳定运输52/313（16.61%，macro 15.70%）。稳定成功净增19条、相对提高57.6%，平均最大抬升8.75 cm升至12.04 cm。官方指标四类别全升；严格指标Bottle/Mug/Camera提升而Bowl由12.50%降至10.59%。新路线通过开发集首轮门槛，下一步只筛现成的后续3轮，再做最小组件消融，不能把Demo80与动作块集成的联合提升错误归因于单个组件。
- 2026-08-24：Chunk8 Demo80四轮闭环筛选中第2轮最佳：官方150/313（47.92%），稳定运输84/313（26.84%），四类别严格指标均超过旧Temporal3；第3/4轮分别仅35/313和45/313，确认训练更久会退化，冻结`epoch=001-step=2576.ckpt`。使用同一checkpoint关闭重叠动作块时间集成后，官方降至51/313（16.29%），稳定运输降至26/313（8.31%），不仅远低于完整模型，也低于旧Temporal3的33/313。这证明未来头本身不够，推理时利用多个时刻对同一动作的预测进行集成是当前提升的关键组件；下一控制项评测只改80%原示范监督、完全不含动作块的模型。
- 2026-08-24：纯`Temporal3 Demo80`两轮稳定运输分别31/313（9.90%）和21/313（6.71%），均未超过旧Temporal3的33/313（10.54%）；因此不能声称“提高原示范监督比例”独立有效。完整Chunk8的84/313来自未来动作块训练与重叠预测时间集成的组合，其中关闭集成直接跌至26/313，时间集成是已验证的必要组件。冻结网络结构后仅允许一次推理超参数搜索：在开发集比较均匀集成`decay=0`、当前`0.2`和较偏重最新预测`0.5`，随后停止Shadow开发并冻结。
- 2026-08-24：动作块集成衰减搜索完成：`decay=0/0.2/0.5`的稳定运输分别90/313（28.75%）、84/313（26.84%）、62/313（19.81%），官方峰值分别149/313、150/313、123/313。按预先确定的稳定运输主指标选择等权平均`decay=0`。Shadow最终冻结为Chunk8 Demo80第2轮checkpoint、8步未来动作块、重叠预测等权时间集成、70步自主输出+52步末动作保持；开发集不再继续搜索。冻结信息及SHA256写入`custom_tools/configs/restart_shadow_final_lock_v1.yaml`，下一步只做8物体最终报告集三种子评测，结果不得反向用于改模型。
- 2026-08-25：纠正上一条的过早冻结。用户指出项目仅进行一天且稳定成功28.75%仍低，没有理由在只验证一个新体系后停止；最终8物体评测尚未运行，因此没有发生测试集反馈污染。原lock改为`superseded_candidate_snapshot`，Chunk8等权集成仅作为当前B1最佳快照。第二轮方法空间固定为互补而非重复调参的三条主线：显式阶段+动作块；DexRep条件扩散动作块；由当前最佳策略重新采集的state-aligned DAgger后再训练动作块。另保留同构checkpoint参数平均作为低成本旁路，但不得替代真正的新体系。
- 2026-08-25：已实现`PhaseActionChunkTemporalActorCriticDexRep`，把显式0→1进度同时送入当前动作头和未来8步头，推理仍对重叠动作块等权集成；支持从当前最佳Chunk8精确扩展，新增phase输入权重以0初始化，因此训练前行为不变。训练数据按真实帧号提供phase，在线数据按采集frame提供phase。构造、checkpoint扩展、推理计数与历史清空测试通过。该组合与旧“Phase单步策略”不同，检验阶段信息是否能帮助未来动作块区分接近、闭合和抬升。
- 2026-08-25：Phase+Chunk8两轮训练完成，验证loss从0.0391升至0.0407，先在固定4物体/118轨迹screen分别做闭环判断。Chunk8相邻轮次参数Soup的两种权重均为24/118稳定成功（20.34%），当前单checkpoint为23/118（19.49%）；仅净增1条，属于“接近但证据很弱”，暂只保留macro更高的等权Soup，不立即耗费完整313条评测。只有Phase候选或后续新体系没有更明显提升时，才把该Soup作为低成本候选扩测。
- 2026-08-25：Phase+Chunk8两轮screen稳定成功为16/118（13.56%）和18/118（15.25%），均低于当前23/118；显式phase未与动作块形成互补，候选停止。随后实现DexRep条件`Residual Diffusion Chunk8`：冻结当前确定性Chunk8及DexRep编码器，以其8步输出作为基准，只对“目标动作块−基准动作块”的残差做50步DDPM噪声学习；推理用8步确定性DDIM近似采样残差，再与基准块相加并继续做重叠时间集成。这样扩散失败时仍有可控的残差幅度，不会像旧目标手Diffusion那样从纯噪声重新生成整条动作。网络构造、真实checkpoint扩展、冻结范围、采样形状和有限值测试均通过，下一步训练5轮后先在118条screen评测。
- 2026-08-25：Residual Diffusion首次启动在sanity validation暴露验证集无teacher时未生成动作块，已修正为验证集始终生成demo chunk并以demo填teacher占位；实际数据读取形状`(8,28)`验证通过。5轮重训完整结束，train/val去噪MSE从0.706/0.480降至0.206/0.214，尚未出现验证反弹；冻结基座保持不变。离线诊断显示采样残差绝对均值0.199，而真实目标残差均值0.075，故不盲测过大的1.0幅度，先用最终轮在固定118条screen比较scale=0控制、0.25与0.5。
- 2026-08-25：扩散初评中scale=0/0.25/0.5分别为24/118、2/118、0/118；大残差显然破坏抓取。但scale=0理论上逐动作等于当前Chunk8，却与既有23/118相差1条，控制实验暴露评测seed与模型构造耦合：评测器先设seed、再构造模型、最后创建仿真，不同架构初始化消耗不同数量随机数，使仿真起点并非严格同随机状态。已在模型加载后、仿真创建前再次设定同一seed。旧结果保留为开发诊断，但跨架构严格对比须按新公平协议至少重跑基座与零残差控制；控制一致后再测试小残差0.05/0.1。
- 2026-08-25：仿真seed重置后零残差控制仍为基座23/118、扩散外壳24/118。进一步确认两checkpoint的34个共享张量逐元素完全相同；扩散改用独立Generator后不再推进全局随机流；固定同一70步观测直接比较输出，最大动作差仅`1.79e-7`，属于浮点运算顺序误差。GPU PhysX接触动力学会放大该微差，因此单seed边界轨迹存在约1条自然波动。代码另对scale=0直接返回base以消除该浮点差，但方法判定不再要求逐轨迹完全相同；后续候选至少需超过23–24条控制带数条才算有效。
- 2026-08-25：发现并纠正Shadow主质量指标的任务定义错误。官方把目标中心放在桌面上方30 cm，但成功条件是`goal_dist<=0.12 m`或达到目标高度；因此30 cm是目标中心，不是最低成功抬升。专家216条中末段保持≥18/30 cm分别为196/133，证明把30 cm作为唯一主门槛会误杀大量官方有效专家。评测器现以“官方瞬时条件在末20步持续成立且峰值到终点回落≤3 cm”为稳定官方成功；30 cm降级为目标中心到达诊断。此前所有名为`strict_terminal`的30 cm数字保留作历史诊断，不能再作为主成功率。
- 2026-08-25：同一问题可能影响基础重定向和进阶任务。题面只要求提高抓取重放成功率，并未给出30 cm；旧`stable_success_protocol_v2`的30 cm来自项目自定义而非已核实官方门槛。按任务顺序暂不修改重定向结果，但G0重新打开；进入重定向阶段前必须从参考仓库、数据轨迹高度分布和任务联系人材料三方复核，再确定“离桌稳定抬升”的统一阈值，禁止继续默认30 cm正确。
- 2026-08-25：基础重定向成功口径完成源码审计。题面没有数值门槛，而参考仓库`test_success_all.py`对三手统一设置`LIFT_GOAL=0.15`；其公共runner只要播放中连续越过15 cm就锁定成功，末段滑落不会撤销。项目因此采用v3折中口径：15 cm为主高度，仍要求末30个60 Hz物理步持续、接触不中断、峰末回落≤3 cm、末段波动≤1 cm及掌物相对滑移门；30 cm降为严格诊断。无需重跑Isaac，直接用既有1000条位置/接触/位姿trace重算。当前选定方法的稳定/运输质量为Linker 203/158、XHand 546/513、Wuji解剖修正版448/429；严格30 cm分别为193/153、516/501、431/421。15 cm并未使数字暴涨，因为成功轨迹通常直接抬到约35 cm，主要瓶颈仍是能否建立并保持抓取。Linker相对参考基线在15 cm下由181/145提高到203/158；XHand仍以官方法最佳；Wuji旧法虽有608/575但含反向远端弯曲，不能与解剖合规方法只按成功数择优。
- 2026-08-25：用户重新冻结任务顺序：在用户明确说“进入重定向”前，只允许继续ShadowHand，不能因一次候选或最终参数校准结束而自行换任务。长动作块与固定抬升校准已结束，当前最佳仍为Chunk8 Demo80第2轮、等权时间集成、step40开始0.20归一化Z补偿，固定118条screen为46条稳定官方成功。下一结构测试把固定时间抬升改为接触触发：只有检测到至少1或2个指端接触后才锁存并执行同一抬升曲线，检验旧补偿是否会在尚未抓牢时把手提前拉离物体；除触发逻辑外checkpoint、动作块、集成、幅度和评测协议全部不变。
- 2026-08-26：接触触发抬升Gate1/Gate2分别为43/118和40/118，低于固定抬升46/118。Gate1让Bottle从10升至11，但Bowl从21降至18、Camera从9降至8；Gate2还把Mug从6降至4。原因是指端力接触并非稳定抓取的可靠早期门，一些本可借腕部上升形成包覆/托举的轨迹被延迟，路线停止。固定方案的官方峰值为51、末段稳定46，明确只有约5条属于“曾达标但末段掉落”；下一项只修改70步动作结束后的52步保持段，沿第40步到末动作的闭合方向小幅继续收紧手指，测试能否恢复这5条而不破坏既有稳定抓取。
- 2026-08-26：保持段继续收紧0.1/0.2分别只有39/118和40/118；0.2虽得到50条峰值成功，却只有40条末段稳定，说明继续挤压增加了短暂抬起，同时造成掌内滑移或物体被挤出。固定方案仍为46/118。至此接触触发和末段收紧均被配对否定，停止继续猜测控制规则。新增逐轨迹诊断：在完全复现当前最佳的同时记录每条轨迹的首次指端接触/首次官方成功时刻、最小掌物和平均指尖物体距离、最大接触指数量、峰值/末值/回落、最终目标距离，以及原始和加补偿动作相对专家动作MSE；下一结构选择必须由67条从未成功轨迹的失败分型决定。
- 2026-08-26：逐轨迹诊断复跑为43条稳定、7条峰值后掉落、68条从未达到官方区域；同方法历史为46，说明单seed GPU PhysX约有数条边界波动，候选需超过49条才晋级。68条未成功中61条最高抬升不足2 cm，主瓶颈明确不是保持阶段；成功组最小掌物距离中位数约0.109 m，而未抬起两组约0.42–0.43 m。40条失败虽然平均指尖距物体≤8 cm，但掌心仍远，说明局部指尖接近不等于腕位姿正确；另28条连指尖也未有效接近。远离失败组的策略—专家动作MSE中位数0.127，约为成功组0.048的2.6倍。力传感接触计数在42/43条成功轨迹上仍为0，确认该信号在当前仿真设置下不适合做门控。源码复核同时纠正“原损失把28维完全同权”的初步说法：官方`cal_loss`实际是`2*wrist_translation + wrist_orientation + fingers + L1`，已经提高了平移权重；因此下一方法不是简单改一个权重，而是冻结当前Chunk8全部手指与几何能力，仅增加零初始化、幅度受限的8步腕位姿残差头，用原示范学习系统性腕轨迹修正，确保训练前行为与当前基座完全相同且不会改坏22维手指动作。
- 2026-08-26：冻结基座的腕残差Chunk8三轮screen稳定成功为43/42/40，统一替换失败；但第1轮的类别结果为Bottle9、Mug4、Bowl24、Camera6，而基座重复测量约为10、5、19、9，说明腕残差对Bowl有约+5的明确局部收益，却对另外三类有害。该模式符合Bowl宽口沿/包覆抓取对掌心定位更敏感的诊断，但不能在同一screen直接拼接成48并声称提升。下一步只在未参与本次残差选择的confirm Bowl对象上配对比较第1轮与已有基座；若Bowl增益不复现则整体淘汰，若复现才训练Bowl专用腕残差并重新做独立路由确认。
- 2026-08-26：腕残差在独立Bowl确认对象上与统一基线均为5/16，成功轨迹索引也完全相同，开发集局部增益未复现，整条腕残差/类别路由路线淘汰。进一步核对100维本体观测发现其保留了归一化当前28维关节位置和物体xyz+quat，仅删除关节速度、力及物体速度；旧注释把216:222误写成“物体位置和角速度”，实际是物体线速度和角速度。对1726条训练示范统计，8步内目标相对当前状态的绝对增量99%分位为腕平移0.254、腕旋转0.065、手指1.204，故不能使用很小的统一残差边界。下一结构改为状态相对Chunk8：输出为`当前归一化关节状态 + 网络修正量`，不人为限幅；从当前最佳加载几何与隐藏层，清零当前/未来输出头，并冻结DexRep编码器训练4轮。它仍只读取当前闭环观测，不读取未来专家轨迹，目的是把绝对姿态回归改写为基于实际跟踪状态的纠错问题。严格加载、输出头重置、冻结与动作块回归测试均已通过。
- 2026-08-26：状态相对Chunk8四轮在固定screen上的峰值/稳定成功依次为36/31、44/37、47/40、40/36，最好稳定40/118，未达到49条晋级线；验证loss持续下降但第4轮物理表现回落，判定结构无效而非单纯欠训练。训练增强审计发现，当前全部Temporal3/Chunk候选沿用统一±0.05噪声，并把同一数值无差别加到归一化关节、米制位置、弧度、四元数和历史动作；现有无噪声实验只有不同结构的官方单帧BC，不能构成当前体系消融。下一对照从同一Temporal3 checkpoint重新训练两个Chunk8：完全无噪声，以及按单位分组的噪声（关节0.02、手/物位置2 mm、手腕欧拉0.01 rad、历史动作0.01，四元数与DexRep不直接扰动），其余训练数据、蒸馏、在线聚合、网络和seed完全一致。
- 2026-08-26：无噪声Chunk8四轮稳定成功为34/46/39/34，分单位噪声为33/42/38/30；无噪声第2轮与旧统一噪声最佳同为46/118，分单位噪声更差，因此噪声不是当前低上限的主因。旧/无噪声最佳类别分别为10/6/21/9和12/6/20/8，遂测试不按类别路由的双策略逐步动作平均，并把实际平均动作同步回两个Temporal3历史；无噪声权重0.25/0.50/0.75分别稳定39/41/43，均低于单模型，说明连续平均破坏各自协调动作，集成关闭。下一步拆解当前最佳的数据来源：从相同Temporal3初始化分别训练“只用离线原始成功示范”和“离线示范80%+类别教师20%”，两者都完全移除在线聚合；检验弱教师或DAgger状态是否正在拉低Chunk8。
- 2026-08-26：移除在线聚合后，纯示范4轮稳定22/27/32/35，离线示范80%+教师20%为21/29/34/39；教师有效但两者均显著低于完整管线46，证明在线聚合也是正贡献。离线蒸馏因物理结果仍逐轮上升而续训至8轮，新增4轮为27/32/39/34，仍未突破且验证误差平台上升，正式停止。下一结构加入可微指尖几何辅助损失：严格按Isaac/MJCF上下限把归一化28维动作还原为3维平移、3维XYZ欧拉角和22关节角，经官方HandModel FK得到掌根+五指各4点共21个世界关键点；在8步有效动作块上最小化预测与示范关键点的平均欧氏距离。真实batch中原动作损失0.0378、关键点误差0.0119 m，故权重1.0约占31.5%；从当前最佳初始化、冻结DexRep编码器、学习率5e-6微调3轮。
- 2026-08-25：在固定4物体/118轨迹上按纠正后的指标复评：专家重放稳定官方成功102/118（86.44%）；Chunk8为40/118（33.90%），后期z补偿为46/118（38.98%）；冻结GRU为43/118（36.44%），加补偿反而仅44/118（37.29%）。旧30 cm诊断分别为96、22、45、24、44条，说明此前补偿的大部分表观增益来自跨越错误高度门槛；但Chunk8补偿仍带来6条真实稳定净增。专家上限与当前最佳仍相差56.5个百分点，不能宣布达到方法上限。下一步只复评现成的Soup、DAgger、Phase和Transformer候选，不先重训或扩大测试集。
- 2026-08-25：纠正指标下现成候选复评完成：Soup、DAgger025、Phase分别为45/42/40条稳定成功，均未超过Chunk8+抬升的46条；Transformer scale0.5虽有55条官方峰值，但仅31条末段稳定，加抬升后进一步降至17条，说明其残差扰动破坏已形成抓取。按预先冻结的49条门槛全部淘汰。下一结构候选为“类别路由Chunk8专家”：从当前Chunk8初始化，保持DexRep/动作块/等权时间集成，分别按四类对齐后的离线、教师和在线样本微调；先冻结几何编码器且只训1轮，避免小类过拟合。
- 2026-08-25：类别路由Chunk8只有37/118，Bottle由11升至12，但Bowl由17降至15、Camera由8降至6，证明主要矛盾不是类别间共享。初始接近方向四分箱也未呈现一致失败集中；训练/验证共2160条轨迹全部满足官方最终成功，排除失败示范污染。下一候选改为离散多模态Chunk8：冻结当前基座，增加3个动作块残差候选和观测门控，训练采用best-of-4 Huber并在episode开始固定模式；它不读取或检索测试参考轨迹，直接针对确定性回归平均多种正确动作的问题。
- 2026-08-25：离散多模态Chunk8第1/3/5轮稳定成功为41/41/38，均未达到49条门槛；训练回归loss继续下降但物理表现后退。门控并未完全塌缩：第3轮在1024个验证样本上选择四候选877/3/24/120次，而按专家误差计算的oracle分配为710/59/82/173，说明残差候选在约31%样本上离线更优，但门控明显少用它们。发现训练按每帧最优候选监督，推理却把第0帧选择锁死全episode，语义不一致；在淘汰前做唯一一次无重训修复，改为逐步动态路由并由原动作块时间集成平滑。
- 2026-08-25：多候选动态路由第1/3/5轮仍为41/41/38条稳定成功，正式停止该路线。现有策略在开发集呈现类别互补：Bottle的GRU为12条、Mug/Camera的Chunk8+抬升为6/9条、Bowl的Soup为22条，冻结组合为49/118。该49条来自同一开发集选路，不能当提升；下一步在未参与选择的`scaled_development_screen4_confirm.yaml`上，与统一Chunk8+抬升配对确认。路由只使用任务已知类别，不读取未来专家动作。
- 2026-08-25：类别策略路由在独立确认集为37/88，与统一Chunk8+抬升的37/88完全相同，且官方峰值41低于基线42，故淘汰；开发集49条确认为选择过拟合。下一候选转向ACT式长动作块：从Chunk8最佳权重初始化，分别训练16步和32步未来动作块，用更长的连贯计划覆盖接近—闭合—抬升，同时保留DexRep、Temporal3历史和重叠预测等权集成。
- 2026-08-25：Chunk16两轮稳定成功28/37，Chunk32为27/26，均低于Chunk8的40和Chunk8+抬升的46；更长块使早期陈旧预测在等权集成中占比增加，削弱当前闭环修正，路线停止。Phase、GRU、Transformer、扩散、DAgger、类别专家、多候选和长动作块均已完成结构级比较，按契约回退当前最佳做最后一次受限校准：抬升幅度只测0.10/0.15/0.25，另固定0.20把启动从40延后到45/50；仍以稳定官方成功排序，禁止按30 cm诊断挑参数。
- 2026-08-26：可微关键点几何辅助损失三轮在118条screen上稳定成功44/42/46（峰值51/53/53），最好只追平当前Chunk8+抬升46，未越过49条门槛，停止该结构。随后进行收尾前实现审计：原始预处理仅写入`obs[0:69] -> grasp_seqs[1:70]`，分配的第69帧动作从未写入，物理零姿态归一化后成为固定极端手形；数据集又把每条末帧的观测/示范动作重定向到第30帧，却保留第69帧`sample_index`，造成当前观测、教师标签、Temporal历史和Chunk8目标互相错位。真实1726条训练轨迹全部存在该末帧伪标签，且8步动作块使每条轨迹最后8个样本（11.4%）受到污染。现已移除末帧回跳，令末帧真实观测重复最后有效PD目标作为保持动作，并把旧教师文件末帧同步为该保持目标；数据集、动作块mask及语法测试通过。观测裁剪、动作尺度、20 Hz控制频率、Temporal3历史顺序、Chunk8重叠集成和reset/评测协议未发现其他确定性错位。只允许从原Temporal3初始化干净重训一次，与当前46/118做最终配对验证，随后结束Shadow开发。
- 2026-08-26：末帧对齐修复后的干净Chunk8四轮在固定118条screen上，峰值/稳定成功依次为38/33、54/42、51/41、51/40；最好42仍低于旧模型46，说明该错误应修复以保证未来数据正确，但不是当前闭环成功率的主瓶颈。按停止条件关闭Shadow开发：最终保留原`restart_shadow_chunk8_demo80_v1/epoch=001-step=2576.ckpt`，推理采用8步重叠预测等权集成、step40开始的0.20归一化腕部Z补偿、70步自主动作和52步末动作保持；纠正指标下开发集为峰值51/118、稳定46/118，专家稳定上限102/118。冻结文件已更新为`final_shadow_development_lock`；最后只在从未参与选型的8物体holdout上进行Temporal3与冻结模型三seed一次性报告评测，结果不得再用于调参。
- 2026-08-26：冻结模型在Final8三seed的官方物体宏平均成功率为39.81%/39.08%/39.81%，均值39.56±0.34%；轨迹总体官方成功率均值42.75%，稳定官方成功率均值39.04%。旧Temporal3在同一70步自主+52步保持协议下官方宏平均为12.05±0.33%。报告主指标按用户确认改为仓库官方成功判据下的物体宏平均，稳定官方成功作为质量副指标继续公开，不再使用30 cm作为主门槛。收尾消融冻结为：仓库发布BC、同80物体公平训练的官方BC、Temporal3、Temporal3+Demo80、Chunk8不执行时间集成、Chunk8等权时间集成、最终Chunk8+Z补偿；全部在同一Final8、三seed、70+52协议上只作报告，不用于进一步选模。
- 2026-08-27：Final8基线与组件消融完成。仓库发布BC、同数据官方BC、Temporal3、Temporal3+Demo80、Chunk8不集成、Chunk8等权集成、最终加Z补偿的三seed官方物体宏平均依次为2.46%、6.76%、12.05%、11.60%、15.16%、36.02%、39.56%。关键结论是Demo80单独下降0.44点，未来动作块但不集成只比Demo80高3.56点；重叠动作块等权时间集成净增20.86点，是主要有效组件；Z补偿再增3.54点，且稳定官方成功率也由35.80%升至39.04%，不是只提高瞬时峰值。原始汇总、CSV及PNG/SVG图统一保存于`custom_tools/results/restart_shadow_final_ablation_v1/`。Shadow开发、基线对比和主要消融至此完成。
- 2026-08-27：开始Shadow阶段代码收尾。当前唯一主线配置合并为`custom_tools/configs/shadow_chunk8_mainline.yaml`，最终checkpoint复制到`custom_tools/checkpoints/shadow_chunk8_final.ckpt`并复核SHA256不变。`custom_tools/minimal_impl`已从旧Temporal3更新为DexRep+Task-ID+Temporal3+Chunk8+重叠等权集成+线性Z补偿，离线损失口径纠正为80%原示范/20%类别教师、在线采样占25%；全部函数已有中文输入、输出、逻辑和作用说明。CPU回归测试通过，且同一最终checkpoint下新旧实现的完整8步动作块逐元素最大误差为0。零基础主线文档统一为`custom_tools/LEARNING_GUIDE.md`。不可恢复的旧checkpoint、候选结果和脚本删除仍待用户对精确清单作一次确认；重定向目录因下一阶段尚未重审，当前不做方法级删除。
- 2026-08-27：用户明确进入第二阶段，停止并行修改ShadowHand，基础重定向从G0重新审查。旧代码和1000条结果只作为历史基线，不默认代表正确方法。进一步核对参考Isaac主流程后纠正口径：目标点在桌面上方30 cm，进入其15 cm邻域或达到目标高度即累计成功；本轮同时报告该参考判据、末段稳定15 cm质量和稳定30 cm诊断，禁止把MuJoCo辅助脚本的`LIFT_GOAL=0.15`误称为Isaac官方定义。
